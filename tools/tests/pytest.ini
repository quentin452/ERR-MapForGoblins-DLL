[pytest]
# Run from the repo root with:  python3 -m pytest tools/tests/ -q
# conftest.py handles sys.path so the tools/*.py scripts import by name.
addopts = -q
python_files = test_*.py
python_classes = Test*
python_functions = test_*
