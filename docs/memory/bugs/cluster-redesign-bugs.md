---
name: cluster-redesign-bugs
description: "Open bugs in the by-location cluster redesign (caves misplaced, count stale, etc.) found in live test 2026-06-19"
metadata: 
  node_type: memory
  type: project
---

By-location cluster redesign (see [[cluster-runtime-queue]]) is built+deployed but live test 2026-06-19 surfaced 4 bugs. Investigating, NOT yet fixed.

**Bug1 — cave/catacomb/underground markers at WRONG overworld spots ("in the sea").** e.g. Scorpion River Catacombs, Fog Rift Catacomb. 4421 minor-dungeon rows reprojected to overworld (project_dungeon_row_to_overworld). Per-location clustering makes each cave ONE obvious pile at a wrong centroid. Root suspected in the projection (uint8_t gridXNo wrap if gx negative/>255, or huge in-dungeon offset on exact-match) — investigator running.

**Bug2 — clusters only on overworld page, never underground.** Siofra/Ainsel etc. Either those project to overworld too, or underground page not exercised. Same projection/map-page investigation.

**Bug3 — many markers still un-clustered with everything checked.** Threshold=8 → locations with ≤8 markers stay exact (BY DESIGN, sparse stay normal). Location id is ONLY ever in textId2 (plain loot) or textId3 (enemy-drop) per LocationAlt comment — my textId2/3 scan is COMPLETE, NOT a slot gap. So loose markers = sub-threshold locations + loc_id<=0. Lever: lower threshold, and add a replan diagnostic counting loc_id<=0 vs sub-threshold to quantify.

**Bug4 — cluster count is STALE permanently (restart does NOT fix — user corrected my wrong "reload fixes" theory).** ROOT: count = b.members.size() at replan = TOTAL members incl. collected. Only pieces/kindling get an inject-time collected-skip (goblin_inject.cpp ~1604); regular LOOT (e.g. Lionel's Set) is never filtered, so collected loot stays counted forever. FIX: count only UNCOLLECTED (loot collected = its textDisableFlagId1 event flag set; orp_flag_set). Best done in the 1s depletion refresh (cluster_refresh ~2586, flag API warm there): remaining = total - (#member_flags set); repoint textId2 = CLUSTER_TEXTID_BASE + remaining. "Lionel's Set not counted" may ALSO be wrong-loc_id splitting Royal Capital across piles (legacy-dungeon, ties to bug1).

**Crash NON-issue: all 4 crash dumps same addr 0x141EB9999, span BEFORE+AFTER redesign → pre-existing map-render crash, not the redesign.**

**FIXES BUILT+DEPLOYED 2026-06-19 (UNCOMMITTED, redesign+these all uncommitted on master, NOT yet in-game verified — restart game):**
- A (bug2): project_dungeon_row_to_overworld now BLACKLISTS area 12 (Siofra/Ainsel/Deeproot/Nokron/Mohgwyn/Lake of Rot) + areas 40-43 (DLC underground caverns) → early return false → they stay on native underground page. Legacy dungeons (10/11/13/14…) + catacombs/caves (30-39) still project. DLC 20-28 LEFT projecting (uncertain; revisit if user reports DLC-in-sea). Authoritative data: data/tile_region_map.json (area 12 entries have majorRegion:null + own tabIds 12000-12002).
- B (bug1): projected-dungeon clusters now sit at the dungeon ENTRANCE (conv dst base point) not the member centroid. project_* gained out_ent_x/out_ent_z params; SectionRow gained ent_x/ent_z (captured at inject push_back, -1 if not projected); replan Bucket tracks ent_x/ent_z, uses entrance for pile pos when >=0 else centroid.
- C (bug4): live REMAINING count. cluster_refresh (~2618) now counts collected_n (member_flags set), remaining=total-collected_n, repoints st->textId2=CLUSTER_TEXTID_BASE+remaining when g_cluster_debug. total = count_textid - CLUSTER_TEXTID_BASE. Fixes frozen total-incl-collected count.
- D (bug3): default clusterThreshold 8→4 (schema + IniEntry default + user ini line 81). Added replan diagnostic log line: "[CLUSTER] stayed-exact: N no-location, M unchecked-category, K locations sub-threshold (<= thr)" — use next test to quantify bug3.

**VERIFY NEXT TEST (restart): caves/catacombs piles at correct entrance (not sea); Siofra/underground markers on underground page not overworld; count decrements as you loot (with "show counts" on); fewer loose markers at threshold 4; read "[CLUSTER] stayed-exact" log to see why remaining markers stay exact. THEN commit. Lionel's-Set: re-check if now in correct Royal Capital pile + counted.**

**GRACE-ANCHOR REWORK BUILT+DEPLOYED 2026-06-19 (UNCOMMITTED, NOT verified — this REPLACES the textId loc_id heuristic that left 5041 markers homeless):**
- ROOT confirmed by diagnostic: "[CLUSTER] stayed-exact: 5041 no-location, 0 unchecked, 47 sub-threshold" → textId-based loc_id missing for 5041 markers (most have no location text line). User ordered: real positions, remove heuristics.
- Authoritative data FOUND in repo: data/grace_position_index.json = 444 graces (from BonfireWarpParam) with areaNo/gridX/gridZ/x/y/z + subCategoryId (region PlaceName) + subRegion name. Area histogram: 60=172 (overworld), 61=60 (DLC), 12=34 (underground), dungeon areas 10-45 have own graces. 75 distinct subCategoryId (some 0=no label). Also available: data/map_name_regions.json (249 MSB region volumes w/ center+box), data/WorldMapPointParam.json (556 pins), data/PlaceName_engus.json. Generators in tools/ (build_grace_index.py etc.) consume regulation.bin+MSB extracts — extracts present, no regen needed.
- NEW: tools/build_grace_anchors.py → src/generated/goblin_grace_anchors.{hpp,cpp} (also copied to generated_vanilla/, added to CMakeLists ~line 106). struct GraceAnchor{area,wx,wz,placename_id}; wx=gridX*256+x.
- goblin_inject.cpp: is_location_id REMOVED → find_nearest_grace(area,wx,wz) + entrance_cluster_key(ex,ez). SectionRow loc_id→grp_key+grp_pname. At inject: compute nearest grace on PRE-projection area/coords (catacomb matches own area-30 grace not surface). grp_key = entrance_cluster_key if projected dungeon (ONE pile/dungeon), else nearest-grace index, else -1 homeless. grp_pname = nearest grace's region PlaceName (label). replan buckets by grp_key, label textId1=b.pname(>0)else -1, position=entrance if projected else centroid.
- EXPECT next test: "stayed-exact no-location" drops 5041→~0 (only areas with zero graces). Underground area-12 markers → area-12 graces → underground page. Catacombs → one pile at entrance.

**KNOWN ROUGH EDGES to verify/iterate:** label is REGION-level (graces share subCategoryId, 75 distinct, some 0=count-only) — coarse, may want per-grace or sub-region (map_name_regions volumes) labels later. No distance cap on nearest-grace → grace-sparse area could make big piles. Per-grace granularity in overworld = up to 172 piles.

**GRACE-ANCHOR COMMITTED e4b7c6c. Live test 2: stayed-exact no-location=0 (WORKS). New issues + this round (UNCOMMITTED on top):**
- "?PlaceName?" labels FIXED: 111/444 graces had subCategoryId = a map TAB id (6800-6880, underground/DLC), not a real PlaceName → rendered "?PlaceName?". build_grace_anchors.py now validates vs data/PlaceName_engus.json → invalid→0 (count-only label). Regenerated.
- Added per-area pile diagnostic: "[CLUSTER] piles per area: a60=.. a61=.. a12=.." → tells which page stray sea-piles + underground clusters land on. UNVERIFIED — awaiting user's next log.
- Sea piles (image was DLC map area 61): Enir-Ilim=area20→conv dst 61 (valid name). DLC legacy 20/21/22/25/28 still PROJECT to 61 (not blacklisted) — suspect bad/placeholder conv coords land them in DLC sea. DLC underground 40-43 ALREADY blacklisted (had placeholder 0,0/256,256 coords). DECISION PENDING: blacklist 20-28 too? or their coords ok? Need per-area diagnostic + confirm if image is base(60) or DLC(61) map.
- Underground (Siofra area 12) shows ZERO cluster icons but markers ARE on the underground page now (blacklist worked). Hypothesis: SUB-THRESHOLD (34 area-12 graces span Siofra+Ainsel+Deeproot+Nokron+Mohgwyn → ~4 markers/grace, under thr=4). per-area log a12=N will confirm: a12>0=render bug, a12=0=sparse. If sparse, lower thr or coarser underground anchors.
- "ERR objects in zero Cities, invisible for cluster AND normal" — VAGUE, likely = objects parked into a mislocated (sea) cluster → invisible at real spot (same root as sea piles). NEED concrete example (item name + where) from user.

**PAGE-LEAK ROOT FOUND + FIXED (UNCOMMITTED, deployed, NEEDS VERIFY) 2026-06-19:**
- Symptom: DLC + underground clusters all rendered on the BASE overworld map (in the sea); zero clusters on DLC/underground pages. "Invisible ERR objects" = items parked into a cluster that drew on the wrong page → gone from real spot (SAME root).
- Per-area log PROVED clusters ARE created per page: "piles per area: a12=30 a19=1 a40=3 a41=3 a42=4 a43=3 a60=228 a61=58" (330 piles, 9243 parked). So b.area correct; RENDERING was wrong.
- ROOT: WORLD_MAP_POINT_PARAM_ST has a SECOND coord set — areaNo_forDistViewMark/gridX/gridZ/posX/Y/Z_forDistViewMark (the zoomed-out / page-select position). Cluster POOL rows are copied from a base-area-60 template (entries.front()); replan only set the MAIN areaNo/grid/pos, never the _forDistViewMark set → every cluster drew at the template's base-60 dist-view → base map. Normal rows carry their own dist-view coords so they paged fine.
- FIX (goblin_inject.cpp replan ~961): mirror all 6 _forDistViewMark fields to the cluster's real area/grid/pos. Also park dist-view areaNo=99 on template creation (~1145) AND teardown (~903) so unused/stale pool rows can't leak onto base map.
- VERIFY NEXT: DLC clusters on DLC map (R-stick), underground clusters on underground page, base map clean of sea piles, ERR DLC/underground items reappear in their piles. Then commit (this + ?PlaceName fix + per-area diag, all uncommitted since e4b7c6c).
- NOTE: projected-dungeon NORMAL (non-cluster) rows: project_dungeon_row_to_overworld does NOT set _forDistViewMark — but individual catacomb markers render OK (Bug A works), so leave unless reported.

**distViewMark fix FAILED (still all-on-base). REAL FIX = SEED FROM MEMBER (UNCOMMITTED, deployed, NEEDS VERIFY) 2026-06-19:**
- Decisive diagnostic from user: clustering OFF → individual markers page CORRECTLY on DLC/base/underground. So per-row data fine; ONLY pool rows wrong. Pool rows = copy of base-area-60 template (entries.front()); setting areaNo+grid+distViewMark was NOT enough → some other unknown page-selecting field stays base.
- FIX (goblin_inject.cpp replan fill ~954): seed the pool row from a real member: `*cd = *(ST*)g_section_rows[b.members[0]].ptr;` then clear eventFlagId/clearedEventFlagId/textDisableFlagId1-8/textId5-8 and override area/grid/pos/distView/icon/textId1-4. Inherits EVERY page field from a correctly-paging member. (distViewMark mirror + park-99 kept too, harmless/complementary.) PARAM_DATA_SIZE==sizeof(ST) so struct-copy is full-row.
- VERIFY: DLC clusters on DLC map, underground on underground page, base map clean. If THIS works, commit everything since e4b7c6c (?PlaceName fix + per-area diag + distView + seed-copy).
- ?PlaceName fix CONFIRMED KEEP (not an artifact): 111/444 graces store a map tab id 6800-6880 in subCategoryId, genuinely not a PlaceName.

**ERR-COUNT FIX — VERIFIED LIVE + COMMITTED ae75d4b 2026-06-19.** ClusterRow now stores std::vector<ClusterMemberRef>{flag,row_id,is_piece,is_kindling} (was vector<uint32_t> member_flags). cluster_refresh (~2798) counts collected via flag OR collected::is_row_collected(row_id) [pieces] OR kindling::is_row_collected(row_id) [kindling]. ERR items = Reforged Rune Pieces (is_piece) / Kindling Spirits (is_kindling) — row-id tracked, no event flag → now counted. Needs "show counts" ON. VERIFY: collect an ERR rune piece/kindling from a pile → count drops. If still stuck, the items are flagless regular ERR loot or lot-backed (handle lotId next). Then commit.

**(ORIGINAL diagnosis kept) cluster count doesn't decrement for ERR-added items:** remaining = total - (#member_flags set); member_flags = each member's textDisableFlagId1 (collect event-flag). ERR items lack that flag (or use lot/collected:: tracking) → collecting them never registers → count only moves for base items, AND total stays inflated by them forever. FIX LATER: track collected per member via flag OR (is_piece&&collected::is_row_collected) OR (is_kindling&&kindling::is_row_collected) OR lotId — needs storing row_id/is_piece/is_kindling in ClusterRow member list, not just the flag.

**PAGE FIX WORKED — COMMITTED 783c1cb (seed-from-member + ?PlaceName + per-area diag + distView). "rendering bug mostly fixed" per user 2026-06-19.** Clustering OFF was the key diagnostic (individual rows page fine → only pool rows were wrong → seed from a member fixed it).

**REMAINING (live test 3, NOT fixed — investigate next):**
- UNDERGROUND SUB-MAP PRECISION: area 12 is actually 3+ sub-maps sharing areaNo=12, split by tabId (12000=Ainsel/Lake-of-Rot, 12001=Nokron/Mohgwyn/Siofra, 12002=Deeproot — per tile_region_map.json). User sees TWO "Ainsel River" clusters underground (one good spot, one wrong) and ZERO Siofra River / Deeproot Depths clusters. Hypotheses: (a) Siofra/Deeproot SUB-THRESHOLD (fewer markers, spread across their area-12 graces) → lower thr or coarser; (b) precision — cluster grid centroid lands on wrong sub-map (area 12 sub-maps may overlap in grid space); (c) two distinct Ainsel-named graces split markers. NEEDS: per-grace/per-tabId breakdown in underground. The seed-from-member SHOULD page to the right sub-map if member fields distinguish tabId — verify they do.
- NAMELESS CLUSTER in overworld (count-only pile, no label): a grace with subCategoryId=0 (3 such in data) or one of the 111 invalidated. Cosmetic. Could fall back label to nearest MSB region-volume name (map_name_regions.json) or area_region_label.

**CLUSTER DIAGNOSTICS ADDED — COMMITTED 36b2521 (deployed):** grace tabId now in pipeline (build_grace_anchors.py GraceAnchor.tab_id; area12 splits 12000=Ainsel/12 graces, 12001=Siofra/Nokron/Mohgwyn/14, 12002=Deeproot/6). Logs: "[CLUSTER] piles per area/tab:" (always on; missing tab=0 piles there) + "[CLUSTER-DUMP] #i key/area/tab/grid/pos/pname/members/mode" per-pile + "[CLUSTER-DUMP] sub-threshold per area/tab:" — last two gated behind config::debugLogging (ini debug_logging=true OR overlay "Verbose logging", RESTART required). grp_tab is DIAGNOSTIC ONLY (not used for grouping). NOTE: subagents are WRITE-BLOCKED in this env (general-purpose agent couldn't Edit/Write) — design via agent, APPLY edits in main thread.
NEXT: user enables debug_logging, opens overworld+underground map, shares the area/tab + underground CLUSTER-DUMP lines → tells us Siofra/Deeproot = sub-threshold (sparse) vs mis-paged. Then fix underground precision.

**UNDERGROUND POSITION MATH FIXED (UNCOMMITTED, deployed, NEEDS VERIFY) 2026-06-19:**
- Diagnosis via data: ONLY area 12 (underground) has LARGE posX/posZ (range ~[-877,1618]/[-680,1910]); overworld/DLC/catacombs are small/tile-local. The cluster centroid did wx=gridXNo*256+posX then split gx=floor(wx/256) — valid only where tiles are 256. Underground tiles are NOT 256 (area 12 uses gridXNo 1/2 with posX to ~1900), so the split invented grid indices (5,6,7…) that don't exist on the underground page → misplaced piles. (This is what "two Ainsel, one misplaced" was — not the 2D-vs-3D-grace theory; that was wrong.)
- ALSO learned: WorldMapPointParam has isOverrideDistViewMarkPos bool; area-12 rows have it false + _forDistViewMark all 0 → game uses MAIN coords underground. So the earlier distViewMark writes were correctly no-ops; seed-from-member is what fixed paging.
- FIX (replan fill ~973): non-dungeon piles now keep seed member's gridXNo/gridZNo and average posX/posZ over members sharing that SAME tile (no 256 math). Projected dungeons keep entrance (256-tiled). Dump log uses cgx/cgz. b.sx/b.sz now unused-but-harmless.
- WATCH: minor overworld pile drift possible (same-tile avg vs old cross-tile world centroid) — negligible for tight grace clusters; check overworld still looks right.
- VERIFY: underground piles (Ainsel/Siofra/Deeproot) land at correct spots now. Then commit.

**UNDERGROUND POSITION COMMITTED c28bb76. NAMELESS-LABEL FALLBACK (UNCOMMITTED, deployed, NEEDS VERIFY) 2026-06-19:** find_nearest_grace now also tracks nearest grace WITH a valid placename; out_pname = grouped grace's name if it has one, else nearest-named grace's name, else 0. Reduces count-only piles (some overworld subCat=0 graces + underground/DLC tab-id graces). Grouping key unchanged (still physical-nearest grace). Graceless-area piles still count-only. VERIFY then commit. Remaining cluster bugs after this: ERR-item count not decrementing; DLC legacy 20-28 sea re-verify.

**NAMELESS-LABEL (UNCOMMITTED, deployed, NEEDS VERIFY) 2026-06-19 — two layers:**
- (a) find_nearest_grace borrows nearest NAMED grace's placename. Fixed OVERWORLD nameless piles.
- (b) DLC still blank: ALL area-61 graces have subRegion=None + subCategoryId=tab-id(6800+) → zero named graces in area 61. ALSO the marker's own textId2/3 are NOT locations (vanilla WorldMapPointParam slots hold enemy/item/status names e.g. 5100="Encountered", 35301700="Ancient Hero of Zamor") — so marker-self-location is useless (confirms why old textId loc_id was garbage).
- FIX (b): NEW region-anchor table from MSB volumes — tools/build_region_anchors.py → goblin_region_anchors.{hpp,cpp} (188 anchors: a61=110, a12=14, a60=52, a20/21 etc.), from data/map_name_regions.json (parse mAREA_gridX_gridZ key, wx=gridX*256+pos.x, validate textId vs PlaceName, skip pos 0,0,0). find_nearest_region_pname() used at inject as LABEL fallback when grace has no name (on pre-projection area/coords). CMakeLists + generated_vanilla updated.
- COMMITTED 35312de. MOSTLY FIXED per user: most piles named via the layered fallback (grace name → nearest-named grace → nearest MSB region → projected-dungeon entrance region). A FEW piles still show count-only ("unknown") — they sit in region-anchor coverage gaps (188 anchors, 110 DLC, not exhaustive) or areas with neither named grace nor region. ACCEPTED as-is. Possible later polish: coarser major-region fallback (WorldMapPlaceNameParam ~10 anchors) or denser region anchors, but low priority.

**ANOMALY FIXES (UNCOMMITTED, deployed, NEEDS VERIFY) 2026-06-19:**
- Completionist "(20) invisible objects" = NOT a bug. Log: thr20 → 171 piles, 7072 parked, 0 dropped, CLUSTER-CHECK 0 wrongly-hidden → items are CLUSTERED not lost. Grace anchors aggregate ~40 markers each, so thr20 still clusters most. KEY INSIGHT: detail/individual needs HIGH threshold (~50-60), clustered needs LOW (<~10). Fixed presets: Completionist 20→60 (off), Explorer near 18→50/far 4→6, Performance near 8→24/far 2→3.
- LATENT BUG icon resized: pile seed-copy inherited isAreaIcon (range icon = map-sized) → oversized pile. FIXED: cd->isAreaIcon=false + cd->distViewIconId=CLUSTER_ICON_ID after seed (replan ~1155).
- LATENT BUG no tooltip: nameless piles had textId1=-1 + (counts off) textId2=-1 → no hover. FIXED: nameless piles fall back textId1=cnt_textid (named=name+count-toggle; nameless=count on line1). replan ~1167.
- VERIFY: Completionist=everything individual; piles not oversized; nameless piles show count on hover. THEN commit.

**MORE FIXES (UNCOMMITTED, deployed, NEEDS VERIFY) 2026-06-19:**
- LEYNDELL-EMPTY-at-Completionist ROOT: ALL projected dungeons were force-bucketed into ONE entrance pile (grp_key=entrance_cluster_key when ent_x>=0), so Leyndell (hundreds of markers > any threshold) was always 1 pile — never individual even at Completionist 60. FIXED: grp_key now prefers grace_idx (dungeons WITH graces, e.g. Leyndell area-11 has 18, bucket by grace → high threshold shows sub-area individuals); entrance_cluster_key only for GRACELESS projected dungeons (catacombs). replan position: entrance only for entrance-key buckets (kv.first>=1000000), else centroid (projected member tiles). [NOT the -2 bug — the user guessed -2 but it was the entrance-bucketing.]
- INTERMITTENT ICON-RESIZE ROOT = RACE (user guessed "memory error" — correct): the pile seed-copy `*cd=*member` momentarily makes the pool row an on-page copy of a real member (its areaNo/icon/isAreaIcon/size); render thread reading mid-write → oversized/dup icon; reopen → consistent. FIXED: after seed-copy set areaNo=99 (+distView 99), write all fields, PUBLISH areaNo=b.area (+distView) LAST so the row only goes visible fully-formed. (isAreaIcon=false + distViewIconId=cluster also kept.)
- -2 LIURNIA: this is the PLAYER position offset in NESTED overworld regions (immediate-block grid off ~+2,-2 vs display grid) — affects ONLY distance-adaptive band centering in those regions, NOT Leyndell-empty. RE-BLOCKED: needs the block-registry tree-ROOT singleton (re_v25 open task, not yet pinned) to replicate FUN_1408775e0 re-parent walk; OR a per-region offset table via live sampling. Both = Windows/live-RE. Low priority (coarse bands tolerate ±2 tiles).

- PHANTOM ICONS after changing cluster settings (UNCOMMITTED, deployed, NEEDS VERIFY) 2026-06-19 — another race. replan_clusters ran on the watcher thread the instant a setting changed, EVEN with the map open; its teardown un-parks every old member (flash) then re-parks into new piles → render mid-apply = phantom/dup icons (cleared on reopen). FIXED: watcher now applies g_cluster_replan_dirty AND g_cluster_expand_dirty ONLY while !world_map_open() (keep flag set otherwise → applied during the close→open gap). g_cluster_debug_dirty stays live (textId flip, no parking → render-safe). The inject-time first replan (closed) + movement replan (already !map_open gated) unaffected. So the live rebuild was the CAUSE (ran mid-render), now deferred to map-closed.

**ACCUMULATED UNCOMMITTED since af56cbf (commit when stable): Completionist preset 60, Explorer/Performance near-thr bump, isAreaIcon=false + distViewIconId, nameless-pile tooltip fallback, seed-copy publish-areaNo-last race fix, Leyndell grace-bucketing (dungeons-with-graces bucket by grace not entrance), phantom-icon defer-replan-while-map-open.**

**DISTANCE-ADAPTIVE LIVE-VERIFIED GOOD 2026-06-19 (committed dd6f47d + 673a6a3). Works overworld + DLC; underground does NOT (known limit).**
- Verified-good config (now Explorer preset + defaults): near_radius=1 tile, near_threshold(detail)=60, far_radius=2, base/far=4. TIGHT radius = only your immediate area detailed, rest clustered (covers just the useful part). Wide radii are useless (cover whole map = all detail). Performance: same radii, near_threshold 30, base 2. Completionist: off, base 60.
- UNDERGROUND distance-adaptive **SOLVED 2026-06-19 (commit 36b3c0e, branch feat/underground-subpage-clustering; LIVE-VERIFIED base+DLC+underground).** Was BROKEN (coordinate trap): player pos mgr+0x70 = leaf-BLOCK-local (re-parented in nested blocks, ±32); markers MSB-local (~1900); AND marker param gridXNo coarse (1/2) while player MapId gridX = 1-7 → neither Euclidean nor tile distance separates underground sub-regions → whole page read "near"/detail. FIX = discriminate by map SUB-PAGE (tabId), not distance: player MapId TILE is reliable everywhere; tile_region_map.json maps each m{area}_{gx}_{gz} → tabId 1:1 (Ainsel 12000 / Nokron+Siofra+Mohgwyn 12001 / Deeproot 12002 / DLC 6800..). New tools/build_tile_tabs.py → src/generated/goblin_tile_tabs.{hpp,cpp} (87 non-60/61 {area,gx,gz,tab}; profile-independent, generate_data.py mirrors canonical into non-default bakes). get_player_map_pos now also out's reliable gx/gz. replan: area 60/61 → Euclidean ramp (unchanged); else → tab_for_tile(player) ; same tabId as pile's grace tab = near_thr (detail), other sub-pages = base_thr (clustered). Movement replan triggers on tile change. So distance-adaptive now works ALL pages. (Block-reparent RE no longer needed for this.) NOTE m12_06 has no tabId → that tile falls to base_thr.
- "Leyndell, Royal Capital 53 no family" = NOT a bug: with near_radius=1, only the immediate tile is detail (thr 60); that 53-pile is a Leyndell sub-area >1 tile from player → base thr (clustered). Looks alone because neighbors are individual. Correct.

**BACKLOG (user asked, not built):**
- (offered, not yet accepted) DEBUG GUARD: log when a cluster row is mutated while world_map_open() — cheap stand-in race detector (TSan/Valgrind can't instrument the game host). Would have caught both today's races.
- DISTANCE-ADAPTIVE cluster size — COMMITTED af56cbf 2026-06-19 (on f3812c8). Includes bug2 fix (near=high detail/far=low clustered, renamed clusterNearThreshold), bug1 fix (movement-based in-world replan), and LEYNDELL/legacy-dungeon fix: get_player_map_pos now PROJECTS the player to overworld (project_dungeon_row_to_overworld on the player's MapId/local) so player area matches the projected piles (was area 11 vs piles area 60 → distance-adaptive skipped → all clustered). VERIFY: Leyndell now detail-near; Liurnia bands ~±2 tiles loose (re-parent caveat, area 60 not projected — minor, acceptable). Original build notes: Config: cluster_distance_adaptive(bool), cluster_far_threshold(12), cluster_near_radius(4 tiles), cluster_far_radius(16) — schema+hpp+ini. replan fill loop: pile world centroid computed BEFORE threshold; per-pile thr = lround(lerp(base, far, clamp((dist-nearR*256)/((farR-nearR)*256),0,1))) when clusterDistanceAdaptive && get_player_map_pos ok && b.area==player_area. Overlay (Clustering, if en): "Scale cluster size by distance" checkbox + Near radius / Far cluster size / Far radius SliderInt + Preset SmallButtons Completionist(thr20,off)/Explorer(thr4,on,far12,nr4,fr16)/Performance(thr3,on,far24,nr2,fr12). ui::request_cluster_replan() added (overlay writes config::* directly then requests replan). VERIFY: enable+open map → fewer far piles / detailed near; walk to new region, reopen → bands recompute; debug_logging [PLAYER] map-pos feeds it. THEN commit.
  TWO BUGS FOUND+FIXED 2026-06-19 (deployed, NEEDS RE-VERIFY): (bug2 INVERTED) ramp was backwards — low thr near = clustered near. FIXED: renamed clusterFarThreshold→clusterNearThreshold (ini cluster_near_threshold, default 18 high=detail); replan thr=lerp(near_thr[high], base_thr=clusterThreshold[low], t) so near=detail, far=clustered. Overlay slider "Detail near you", presets+tooltips updated, clusterThreshold tooltip now says "FAR size when adaptive". (bug1 NO REFRESH on reopen) replan only fired on setting changes; moving never replanned. FIXED: movement-based IN-WORLD replan in watcher (~2s throttle, while map CLOSED, when player moved > max(256, nearRadius*128) or area changed → dirty). Doing it AT open would show one-open-late (game reads rows at open). User's old ini has stale cluster_far_threshold=12 key → re-sync drops it, writes cluster_near_threshold=18; tell user to hit a preset. DESIGN was: DESIGN: LINEAR RAMP threshold = lerp(clusterThreshold[near], clusterFarThreshold[far]) over t=clamp((dist-nearR*256)/((farR-nearR)*256),0,1); dist = euclid(pile world centroid, player world) SAME-AREA only (b.area==player_area via get_player_map_pos). New config: cluster_distance_adaptive(bool off), cluster_far_threshold(u8~12), cluster_near_radius(u8 tiles~4), cluster_far_radius(u8 tiles~16). Trigger = map-open replan only (player barely moves in menu). PLAYER-PROFILE PRESETS (overlay buttons set all knobs): Completionist(clustering off / huge threshold), Explorer(balanced + distance-adaptive on), Performance(aggressive far-merge). Overlay: collapsible under Clustering (near radius / far size / far radius sliders) + preset buttons. IMPL NOTE: in replan fill loop, compute pile world centroid (cgx*256+cpx) BEFORE the threshold check (move the position calc up, reuse for both distance + final pos; don't alloc pool row until thr passes). Underground (area 12, non-256 tiles) radius scale is off but same-frame relative distance still ~works — overworld is the main use case.
  ORIGINAL: DISTANCE-ADAPTIVE cluster size (user idea 2026-06-19): opt-in slider — bigger cluster threshold for FAR clusters (fewer distant piles → perf), smaller near player. BLOCKED: needs live player world-pos (Player Proximity), which we do NOT have — see [[mapforgoblins-map-open-freeze]] (WorldChrMan player-pos RE blocked on Linux) and [[cluster-runtime-queue]] (proximity clustering paused, get_player_world_pos dormant). Revisit if/when player-pos RE lands.
  - STATUS UPDATE 2026-06-19 (re-read docs/re_findings_playerpos.md): TARGET A player pointer is CONFIRMED live (app 2.6.2.0/ERR 2.2.9.6 = current build), NOT hard-blocked anymore. Read: mapId=*(u32*)(*(uintptr*)(base+0x3d691d8)+0x2c) → gridX=(mapId>>16)&0xff, gridZ=(mapId>>8)&0xff; mgr=*(uintptr*)(base+0x3d69ba8); localX=*(float*)(mgr+0x70), localZ=*(float*)(mgr+0x74) (+0x78=height). Player world ≈ gridX*256+localX (marker space). ER has NO global pos float (block-local+MapId only). CAVEAT: nested regions (Liurnia) immediate-block grid off ~(+2,−2) tiles vs display grid (game re-parents up block tree FUN_1408775e0); Limgrave/Weeping exact — NEGLIGIBLE for coarse distance sizing. PROD GAPS: (1) statics 0x3d691d8/0x3d69ba8 drift per patch → must AOB-anchor (doc has func AOBs, not the globals yet); (2) accept nested ±2-tile error OR finish re-parent walk. ⟹ distance-adaptive cluster size is now IMPLEMENTABLE.
  - AOB ANCHORING DONE + LIVE-VERIFIED + COMMITTED f3812c8 2026-06-19 (First Step: area=60 world=(10748.5,9218.6) ≈ expected ~10740/~9263 ✓; MapId gridX genuinely 42, localX≈−3.5; the diagnostic's floor(world/256)=41 is a harmless display quirk when local is negative — use raw gridX/gridZ from MapId, not floor(world/256), if exact tile needed). Details: both Target-A statics now AOB-resolved (no hardcoded RVAs → patch-safe). Player-MapId singleton (was 0x3d691d8): NEW AOB "48 8B 0D ?? ?? ?? ?? 48 8D 54 24 20 E8 ?? ?? ?? ?? F2 0F 10 05 ?? ?? ?? ??" relative_offsets{{3,7}} — found via x86_64-w64-mingw32-objdump on eldenring.exe (ImageBase 0x140000000, the singleton is referenced MANY times so needed unique trailing bytes: the site at RVA 0x243c3f that loads it then calls the +0x2c MapId getter FUN_140243a60); python-verified UNIQUE across both .text sections + resolves to 0x143d691d8. Map-pos mgr (0x3d69ba8 = CSWorldGeomMan): REUSED existing goblin_collected world_geom_man_slot AOB "48 8B 0D ?? ?? ?? ?? 48 8D 53 10 E8 ?? ?? ?? ?? 4C 8B E8". NEW fn goblin::get_player_map_pos(int&area,float&wx,float&wz) in goblin_inject.cpp (SEH-guarded): area/gridX/gridZ from MapId, localX/Z from mgr+0x70/+0x74, world=grid*256+local. Throttled [PLAYER] diagnostic in cluster refresh (debug_logging-gated). LIVE-VERIFY: enable debug_logging, stand on First Step → expect "[PLAYER] map-pos area=60 ... [tile 42,36]"; tile should track as you walk. THEN commit + build distance-adaptive. (eldenring.exe @ ~/.local/share/Steam/steamapps/common/ELDEN RING/Game/, disassemble with x86_64-w64-mingw32-objdump; AOB-uniqueness via python .text scan.)
  - ⚠️ COORDINATE TRAP for Player Proximity / any distance math: world coord = gridXNo*256 + posX is ONLY valid where map tiles are 256 wide (overworld 60/61, legacy dungeons, catacombs). UNDERGROUND area 12 tiles are NOT 256 wide — area-12 posX runs to ~1900 within gridXNo 1/2 (see commit c28bb76). So `grid*256+local` is NOT a usable world coordinate underground; distance comparisons must be done WITHIN the same area (and ideally same tabId sub-page), using each area's real frame, NOT a global grid*256 assumption. Same reason the cluster centroid keeps a member's tile underground instead of re-splitting by 256.
- Item-pickup EVENT TOAST (user idea 2026-06-19): show a toast when an item is collected. Pairs with the uncollected-count panel.
- SEARCH BAR in the ImGui overlay (user idea 2026-06-19): filter/find by name; esp. useful for NPCs (jump to / highlight an NPC, filter the category or Quest Browser lists). Lives in goblin_overlay.cpp.
- COMPASS / on-screen distant markers for clusters+items (user idea 2026-06-19, [Image bar HUD]): the top compass bar = the game's DistViewMark system, driven by the SAME WorldMapPointParam fields we already edit (isOverrideDistViewMarkPos, *_forDistViewMark, isAreaIcon) — that's how graces show there. PLAN: param-first spike — set isOverrideDistViewMarkPos + _forDistViewMark coords + a world-renderable iconId on cluster rows, check if they appear on the compass (game distance-gates it, so clutter self-limits → clusters are right granularity). RE only if the game hardcodes compass display to grace/objective marker TYPES and ignores our rows despite the flags (then hook the dist-view filter). Cheap experiment before any RE.
- UPDATE ERR COVERAGE to 2.2.9.6 — INVESTIGATED 2026-06-19, BLOCKED ON LINUX (Oodle). Current data bake is against ERR **2.2.1.2** (per docs/messages_offline_research + windows_re_briefing); game/install mod is **2.2.9.6** → coverage stale (worth updating). Pipeline = tools/build_pipeline.py (Linux equiv of build.bat snapshot): reads ERR_MOD_DIR (regulation.bin + map/MapStudio MSBs + event + msg) → extract_all_items → generate_*_massedit → generate_data → src/generated/goblin_map_data.cpp (+ grace/region/location anchors). ERR 2.2.9.6 SOURCE IS PRESENT: <ERR_ROOT>/mod/ (regulation.bin, map/MapStudio/*.msb.dcx, event, msg). Needs tools/config.ini (copy config.ini.example, set err_mod_dir=<ERR_ROOT>/mod) OR env ERR_MOD_DIR/GAME_DIR.
  RESOLVED 2026-06-19 (Windows agent): data is ALREADY 2.2.9.6 — full pipeline rerun reproduced the committed bake BYTE-FOR-BYTE (regulation md5 89610e84… == ERRv2.2.9.6-541 download; 9296 markers, no category changes). The "stale at 2.2.1.2" premise was WRONG (that was just an RVA-capture note in docs, not the data bake). No data commit; only a docs commit 02f9e3b (push). config.ini err_mod_dir=<windows_downloads>\ERR_mod. CORRECTION: pipeline uses bundled pythonnet + Andre.SoulsFormats (NOT soulstruct) — but still WINDOWS-only (pythonnet/.NET + Oodle). My Linux soulstruct/Oodle analysis below was the right conclusion (Windows-only) for the wrong specific lib.
  BLOCKER: ERR MSBs are DCX_KRAK (Oodle), regulation encrypted+DCX. soulstruct pip-installs on Linux (2.3.2) BUT its Oodle is WINDOWS-ONLY ("Can currently only load Oodle DLL on Windows") — only tools/oo2core_6_win64.dll present, no Linux Oodle. So the DATA pipeline CANNOT run on Linux (unlike the DLL build which works via clang-cl+xwin). PATHS: (1) run build.bat snapshot on WINDOWS (native oo2core); (2) Linux: pre-decompress .dcx via WitchyBND/UXM under Wine, or Wine-wrap oo2core — non-trivial. No data/.build_cache.json here (clone never built locally; data/ committed pre-baked).

- ImGui panel: full count of UN-COLLECTED items per type + UN-KILLED mobs.
- AUTO-HIDE ERR-duplicate categories — DONE (opt-in toggle) COMMITTED b7dc4d7 2026-06-19. Overlay "ERR integration" section + "Hide boss markers" checkbox (ui::err_hide_bosses, wraps WorldBosses visibility / show_bosses; default off=bosses hidden). RESEARCHED 2026-06-19 (err.fandom Quality_of_Life page). ERR natively adds to the world map: BOSS markers, ENEMY-CAMP markers (appear on first engaging the camp guardian), and COMPLETION markers (defeated bosses, cleared camps/minor-dungeons, ruins w/ seals broken, towers w/ puzzle solved). Grace icons resized/dark-hued underground; player markers blue. NO merchant icons found (user's "merchants" assumption WRONG). OVERLAP with MapForGoblins = only WorldBosses (we have no enemy-camp category; completion markers aren't item markers). CAVEAT: ERR's are completion/engagement markers (show on defeat/engage), ours are pre-discovery LOCATION markers for navigation → COMPLEMENTARY, not pure duplicates. DECISION: do NOT auto-hide; add an OPT-IN toggle "Hide boss markers (ERR already marks bosses)" default OFF. Source: https://err.fandom.com/wiki/Quality_of_Life_Changes

**PRE-EXISTING PHANTOM STALE ICONS (unsolved 2026-06-19, in master 9a2c34d).** Big (isAreaIcon=true) + no-tooltip (textId -1) + DUP (item icon AND cluster glyph for the same pile). Deterministic repro: disable clustering → open map → enable clustering → open map. ROOT = replan_clusters seeds each pool row from a LIVE member (`*cd = *member` → the pool row is momentarily a visible, oversized, tooltipless copy until areaNo=99 + fields set), AND publishes the glyph (areaNo=b.area) BEFORE parking its members — both race the game's icon-build reading mid-replan. ATTEMPTED + REVERTED (didn't fix): delta teardown (snapshot old members, un-park only those that left, no all-visible flash), publish-glyph-after-members, build-row-in-local-temp-then-assign (cd stays 99→99), re-gate replan to map-closed only. Phantom persisted through all → deeper than the seed/publish order (maybe the game build itself, or PR#6's build-fn hook 9a2c34d interacting). Memory-noted earlier "phantom-icon fixes" (publish-areaNo-last, defer-replan-while-map-open) clearly regressed or were insufficient. NEXT: isolate via live_refresh_world_map=false (rule out PR#6 hook) vs bisect to before PR#6; likely needs replan fully off the render path + atomic visibility swap. See [[thread8-mapopen-bottleneck-re]] problem 3.

**User pref: dislikes piecemeal probing commands — synthesize from investigation, then act. Interrupted a slot-histogram bash 2026-06-19. Gets heated/profane when frustrated — stay terse, act decisively, don't over-ask.**
