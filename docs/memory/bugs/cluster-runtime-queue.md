---
name: cluster-runtime-queue
description: Prioritized work queue for the runtime-clustering rework (handoff after context filled 2026-06-19)
metadata: 
  node_type: memory
  type: project
---

**HANDOFF QUEUE for runtime clustering (ERR-MapForGoblins-DLL, branch master).** Context filled at 88% mid-debug 2026-06-19 (~04:40). State: runtime clustering shipped (commits 27fcae2 fully-runtime refactor, c1522ef type-name labels) but BUGGY. User is frustrated — the per-category clustering was NEVER wanted. See [[mapforgoblins-map-open-freeze]] for the cluster architecture + the live-refresh lead.

**THE CORE REDESIGN the user actually wants (do this, it subsumes most bugs):** NOT per-category separate piles. Clustering = ONE MIXED pile per dense map cell (the OLD pre-eaa60ca one-pile behavior). The per-category "cluster" CHECKBOX = does THAT category's markers JOIN the mixed pile (checked) or stay shown NORMALLY on the map (unchecked). So: remove the SOFT (per-category) mode entirely; clustering is always mixed (current HARD logic); the existing `category_clustered_cfg`/cluster_exclude already controls per-category inclusion — wire the overlay per-category "cluster" checkbox to it LIVE (re-plan). Keep the live rebuild (replan_clusters on toggle, applied on map-reopen). Goal verbatim: "recuperer le meme system de cluster que avant mais avec cette logique [checked=clustered, unchecked=normal] + le live rebuild preservé".

**COMMITTED 2026-06-19 — a79dcb4 "fix(cluster): re-enableable toggle + threshold-0 crash guard" (master, built+deployed, NOT yet in-game verified — restart game to load):**
- Bug1 FIXED: overlay clustering block restructured — master "Enable clustering" checkbox now ALWAYS shown (was gated on clustering_active → vanished, unre-enableable). Sub-controls (size/hard/counts) under `if(en)`; "no dense piles" is just a hint now. set_clustering_enabled() now sets g_clusters_expanded + g_cluster_replan_dirty (was a DEAD toggle — set config only, no replan). Dropped the redundant clusters_expanded "collapse" checkbox (enableClustering already == collapsed intent). goblin_overlay.cpp ~693, goblin_inject.cpp set_clustering_enabled.
- Bug2 FIXED: clamp threshold min 1 in set_global_threshold + set_category_threshold (was min 0) + defensive `if(thr<1)thr=1` in replan_clusters. Threshold 0 = every cell clusters → pool blowout = crash.
- Bug3 NOT fixed (user deferred 2026-06-19): I had a world-coord fix (gridXNo*256+posX binning/centroid) but REVERTED it before commit — user wants to redo wrong-locations via the CORE redesign "ONE cluster per LOCATION + location NAME" (not the 60-unit cell grid). So replan still bins/averages tile-LOCAL posX w/o gridXNo (the latent misplacement bug). NOTE for the redo: bug3 is SEPARATE from TARGET A re-parent caveat (that bites only from PLAYER MapId; markers already in icon/display grid post-projection) — the grid*256+local world-coord formula is the lever if cell-based, but user wants location-NAME-based grouping instead.

**CORE REDESIGN SHIPPED 2026-06-19 (UNCOMMITTED on master, built+deployed, NOT yet in-game verified — restart game):** "one mixed pile per LOCATION + location name". Bugs 1+2 already verified in-game + committed (a79dcb4). Design how:
- Group markers by LOCATION id (not 60u cell). Location id = the marker's location-line PlaceName id, captured at inject into new SectionRow.loc_id via is_location_id() scan of textId2/textId3. is_location_id(id)= id>0 && (id<50M || id>=999M) — location ids <50M, COMPOSE synth ids ≥999M; excludes item names(50-600M), npc(700M+), cluster count/catname(952M).
- NO new FMG: cluster row textId1 = loc_id directly → location NAME renders free (same string markers use). textId2 = pre-baked count string (gated by "show counts" toggle, textId1 always shows). apply_cluster_debug unchanged (flips textId2, keeps textId1).
- replan_clusters fully rewritten: bucket by loc_id (mixed); skip loc_id<=0 (no location→exact) and unchecked categories (read LIVE g_category_cluster[]→normal); threshold = global clusterThreshold per-location member count (sparse stay exact); centroid in WORLD coords (grid*256+local) split back to grid+local — THIS fixes bug3 wrong-locations. domcat kept only for section gating.
- Per-category checkbox now LIVE: set_category_clustered fires g_cluster_replan_dirty; replan reads g_category_cluster[] not stale config::clusterExclude. Checked=join pile, unchecked=normal (exactly user's ask).
- SOFT mode REMOVED: always mixed. Overlay "Hard clustering" checkbox deleted; per-category ">thr" input deleted (single global threshold now). cluster_hard/set_cluster_hard/config::clusterHard + cluster_threshold_for_cfg + CLUSTER_CELL + CLUSTER_CATNAME now dead/unused but left in (harmless, no warnings).
- Files: goblin_inject.cpp (SectionRow+loc_id, is_location_id, push_back capture ~1532, replan_clusters ~860-940, set_category_clustered ~2127), goblin_overlay.cpp (per-cat checkbox tooltip ~449, clustering section ~682).

**VERIFY IN-GAME (restart): piles labeled with LOCATION name + count; one pile per location; per-category checkbox toggles live (join vs normal); piles land in correct spots (bug3); COMPOSE-id locations (999M+) render name OK; location-less markers stay exact. THEN commit. Open Qs: 2-line label (name / count) acceptable? COMPOSE render? underground still backlog.**

**PRIORITY BUGS (fix first, in order):**
1. **Disabling clusters DELETES the ImGui clustering section.** When clustering disabled → replan clears g_clusters → `clustering_active=false` → overlay shows the else-branch ("No dense piles…") instead of the enable toggle → user can't re-enable. FIX: the enable/disable checkbox must ALWAYS show (don't gate it on clustering_active; gate only the count-label/debug extras). goblin_overlay.cpp ~line 704 `if (clustering_active())` block.
2. **Crash after setting cluster size (threshold) to 0.** threshold 0 → every cell with >0 markers clusters → likely pool overflow (CLUSTER_POOL_SIZE=1024) or pathological member counts → crash. FIX: clamp threshold min to 1 (set_global_threshold), and make replan_clusters handle pool exhaustion gracefully (already has `dropped` counter — verify it doesn't UB; also the per-bucket member vector could be huge). Repro: overlay threshold → 0.
3. **Clusters in WRONG locations.** replan_clusters reads each row's pos via `st->posX/posZ` from the live blob. Suspect: those are TILE-LOCAL coords (±~500, see tile-system in [[mapforgoblins-map-open-freeze]]), and the centroid/cell math needs the row's gridXNo/gridZNo combined (full pos = tile + local), OR the old init plan projected via project_dungeon_row_to_overworld(&tmp) on a COPY while the live row may not be projected the same way. Compare the OLD plan (#if 0 block in goblin_inject.cpp, which used `entries[i].data` + project_dungeon_row_to_overworld) vs replan reading g_section_rows ptr. The cluster centroid posX/posZ likely needs the same projection/tile handling the old plan did.

**BACKLOG:**
- **Re-add Location/region NAMES to cluster labels.** c1522ef restored TYPE name (textId1 = category name) + count on textId2, but lost the REGION ("Smithing Stones — Limgrave (12)"). Full "type — region (count)" in one line needs RUNTIME FMG string rewrite (strings are baked into the PlaceName FMG at setup_messages init via cluster_label_storage which is LOCAL/copied → not mutable). Approach: pre-inject fixed-width padded label slots (one per pool row, textId = base+poolindex), expose a writable buffer in goblin_messages, rewrite in-place at replan (point-1 in-game finding: textId/label changes ARE live, so FMG content rewrite may show live too). 
- **Underground clusters shown on the overworld** — ALREADY in [[mapforgoblins-map-open-freeze]] ("FIX #2 BACKLOG underground/dungeon clustering"): underground (area 12/15) markers project to overworld OR stay sub-threshold per-cell → no underground clusters. Design call: cluster underground on its own page vs overworld entrance.

**KEY CODE FACTS (current state):** goblin_inject.cpp: replan_clusters() (~line 824) is the runtime planner (buckets g_section_rows → fills g_cluster_pool, parks members + g_cluster_member_ptrs, calls apply_cluster_expanded). Pool reserved at inject (CLUSTER_POOL_SIZE=1024 spare rows, parked); OLD init plan is in an `#if 0` block. g_cluster_replan_dirty → watcher calls replan. CLUSTER_CELL=60, default threshold=8 (UNCHANGED from one-pile era — verified). Labels: CLUSTER_TEXTID_BASE+count (numbers), CLUSTER_CATNAME_TEXTID_BASE+cat (type names). is_section_hidden_ptr keeps collapsed members parked vs fragment/collected/royal eviction (fix 7e4f153). config: clusterHard (mix), enableClustering, clusterThreshold. Build: clang-cl+xwin in build-linux/, deploy to <ERR_ROOT>/dll/offline/MapForGoblins.dll, RESTART game to load (DLL loads once). [CLUSTER] / [CLUSTER-CHECK] log lines for diagnosis; log has a "NEW SESSION" separator + is daily-append (grep last NEW SESSION). DLL version string is "vpre-1.0.18" (not the filename — non-issue, user asked).
