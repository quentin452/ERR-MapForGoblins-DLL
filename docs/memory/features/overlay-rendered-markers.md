---
name: overlay-rendered-markers
description: "Architectural direction (user idea 2026-06-19): render goblin markers ourselves via the ImGui/DX12 overlay projected onto the game map — bypasses the WorldMapPointParam page-build, killing freeze + live-refresh + phantom at once"
metadata: 
  node_type: memory
  type: project
---

## Import Reconciliation 2026-06-28

This memory was imported into `master@931438d`, so the old `HEAD 4a7716d` pointer below is stale. The
architectural conclusion remains current for this checkout: ERR no-bake Phase 2 is on master and the
generated bake is retired. Use `docs/nobake_scoreboard.md` and the checked-out code as source of truth for
current counts.

## 🤝 SESSION HANDOFF (2026-06-27 — NO-BAKE COMPLETE + F1 SEARCH + LIVE OFFSETS/CLASSIFIER)
**Historical HEAD `4a7716d` (older master; current import checkout is `931438d`).** Big milestones since #15:
- **No-bake Phase 2 DONE (ERR):** static `MAP_ENTRIES` bake RETIRED — `src/generated/goblin_map_data.cpp` is a 17-line stub, **baked residual = 0, 61/61 categories 🟢 off-bake** (disk 8381 / live 469 / live-cls 74 = 8850). Markers now come from the live DiskMSB parser + live game memory. See [[msbe-parser-supersedes-bake]] (parser shipped), [[loot-identity-stable-err-additive]] (loot now off-bake, position too), [[one-dll-externalize-mapdata]] (no ERR bake to externalize). Vanilla/ERTE/Convergence variants still baked.
- **F1 item/object search bar:** results list + map highlight + cursor locate; cross-page AUTO page-switch locate (game-thread marshal; page-switch RE: write flag c1fc0=DLC, c7900=UG directly); English name-alias index + word-order-independent token matching + accent-fold ([[disambiguate-bug-symptoms-first]]); zoom-in + edge-centring on locate; grace-gate recomputed live (not latched across saves).
- **Live param-offset source-of-truth:** read field offsets LIVE from the exe via embedded find-what-accesses (goodsType, sortGroupId, pickUpItemLotParamId, isEnableRepick offset+bit, BonfireWarp textId1 via consensus) — no hardcoded offsets. Supersedes hand-derived offsets in [[param-struct-offset-verification]].
- **Live taxonomy classifier:** items/objects classified by ER's own (goodsType, sortGroupId) + curated exceptions, not hardcoded id ranges (runes, spirit ashes, key/quest routing, region-map double-bucket fix).
- This session's smaller fixes also landed: unresolved-RE-signature F1 error, English error windows, font arrow/math glyphs, discovered-graces-bypass-require_fragment parity. Deployed build = `build-linux` → `dll/offline/`.

## 🤝 SESSION HANDOFF (2026-06-23 #15 — MERGED TO MASTER; loot identity stable; graces bake stripped)
**Branch `perf/rpm-thread-move` FAST-FORWARD MERGED → master + pushed (GitHub master = `11b5fcc`, verified ls-remote).** 18 commits. Session work all on master now.
- **Loot identity probe (no-RE, dormant):** `resolve_loot_item_textid` + `probe_loot_identity` (map_entry_layer, diag_loot_flags-gated). Result: **ERR is ADDITIVE, identity does NOT drift (98% baked==live)** — see [[loot-identity-stable-err-additive]]. The 81 "drifts" = an ammo encoding-convention mismatch (baker uses +100M per-marker convention = CORRECT; encode_live_item uses the liveLootLabels-raw convention; dormant, never shipped) — NOT a baker bug, NOT real drift (docs/re/loot_ammo_encoding_finding.md). **NO phase-2 live-identity feature** (~0 benefit). Real future-proof for an additive mod = COVERAGE diff (live lots w/o baked marker), not identity.
- **427 dead baked WorldGraces rows STRIPPED** from goblin_map_data.cpp (GraceLayer draws live; overlay_layers excludes graces from MapEntryLayer). MAP_ENTRY_COUNT now **8653** (was 9296: −216 boss −427 grace). NOT the Siofra-extra-grace cause (these aren't drawn).
- **Biggest remaining baked surface** = lot-backed loot (~4316 rows) but identity+position both stable → not worth live-sourcing; positions don't drift (chests don't move, docs/re/windows_live_loot_position_re_prompt.md §0).
- **OPEN:** [[extra-graces-siofra]] (live BonfireWarpParam gating or projection, NOT the stripped rows). Loot COVERAGE diff (catch ERR's added items) if wanted.

## 🤝 SESSION HANDOFF (2026-06-23 #14 — boss bake RETIRED, drift was MapGenie's, NOT ours)
Branch `perf/rpm-thread-move`. DLL deployed md5 `5f4f6453`. **Boss markers now built LIVE + verified in-game.**
- **🎯 DUNGEON-BOSS "DRIFT" = NOT A BUG (user-confirmed in-game).** The boss-far-from-entrance was **MapGenie's wrong reference** — OUR placement is correct/more reliable. Kills the whole NEXT-1 track: A/B (collapse-to-entrance vs faithful) MOOT (keep faithful), T1/T2 WorldChrMan enemy-RE ABANDONED (never needed). See windows_enemy_boss_runtime_pos_re_findings.md RESOLUTION.
- **Bosses LIVE (commit da895b7 + d4c4a26):** `map_entry_layer.cpp build_live_bosses()` iterates live `WorldMapPointParam textId2==5100` → boss markers (shared `push_marker()` helper). Auto-fixed the 3 coverage anomalies (Flamelost Knights area16/31 swap + missing area-60 boss) + future-proofs vs ERR updates. NO join flags needed (live = source, nothing to join). Live row self-sufficient: pos + real clearedEventFlagId(510010, vs the kill flag we baked) + textId1 name + iconId.
- **Bake physically retired:** 216 WorldBosses rows STRIPPED from generated/goblin_map_data.cpp (MAP_ENTRY_COUNT 9296→9080, verified by compile), generate_data.py skips "World - Bosses" MASSEDIT. **boss_list.json KEPT** (loot_massedit + relocating_boss_fix consume it). ⚠️ live rows still dungeon-frame → projection unchanged (but that's correct, not drift).
- **probe_baked_drift REMOVED** (commit d4c4a26): served its purpose (found the 3 anomalies → motivated going live); target (boss bake) gone. Result preserved in the findings doc.
- **Probe finding recap:** runtime 5100 data sound (217 rows); bake had 3 anomalies (all generate_boss_list.py matching bugs). Per-boss position validation = offline tools/diag_t3_bossframe.py (bridges via boss_list.json: cleared↔live, kill↔baked).
- **Earlier this branch (#13):** RPM harvest moved off game thread → probe poll (`background_harvest_tick`); game-thread RPM walks bench-wrapped; rpm_walk_audit.md.
- **OPEN:** [[extra-graces-siofra]] bug (graces in Siofra absent from vanilla — ungated dev graces or projection miscalc; noted, not started). Branch not yet PR'd/merged.

## 🤝 SESSION HANDOFF (2026-06-23 #13 — RPM thread-move + bench wraps + enemy-pos RE prompt)
Branch `perf/rpm-thread-move` off master (3 commits, NOT in-game verified yet). DLL built+deployed md5 `f070036d` (mtime 11:43:11).
- **a7714fe docs:** `docs/rpm_walk_audit.md` (full RPM-site inventory: wrapper, thread, bulk-vs-per-node, bench-wrapping, publish globals+mutexes) + `docs/re/windows_enemy_boss_runtime_pos_re_prompt.md` (the NEXT#1 RE prompt).
- **5e6dda2 perf:** MOVED `harvest_repo_icons` (+twin) OFF the game thread (`find_detour` tail) onto the worldmap_probe 100ms poll via new `goblin::background_harvest_tick()`. Walk was already read-only RPM-on-self + self-throttled (400ms) + resolves repo from static anchor + publishes under mutexes → thread-safe, no new race (writer/reader were already cross-thread). find_detour keeps the g_icon_repo stash + dumpIconTextures diag.
- **f2f2953 perf(bench):** wrapped remaining game-thread RPM WALKS: `harvest.resident_walk` (harvest_resident_icons, enum_detour) + `bindtest.group_walk` (res_walk_groups). `icon_find_gpu_tex`=DEAD (no callers). Small per-call sites (warp_pin/get_warp_area) + throttled engine-call grace-force = not cliffs, left unwrapped.
- **⚠️ VERIFY in-game (DLL loads at launch → restart ERR):** graces+map symbols still draw (harvest now from probe thread); micro-lag gone navigating menus/map-open; PR#10 native icons unregressed. Behavioral change: harvest now runs always (100ms when icon config ON), not only menu-open → wider coverage.
- **NEXT-1 (the dungeon-boss DRIFT RE prompt is WRITTEN, not run):** `windows_enemy_boss_runtime_pos_re_prompt.md`. KEY LEAD = **T3**: boss pos today is baked from `boss_list.json` = MSB-internal entity coords (NOT the WorldMapPointParam field-boss row's own pos). If the vanilla `textId2==5100` row is already in OVERWORLD frame → trivial fix in `tools/generate_boss_list.py` (bake the param-row pos), ZERO RE, fixes unloaded bosses too. T2 = decisive live-read experiment (loaded dungeon boss → push runtime (MapId,local) thru FUN_1408775e0/FUN_140876140, compare vs baked vs entrance). Reuses player-pos RE (WorldChrMan er+0x3D65F88, MapId er+0x3D691D8+0x2C, block-tree transforms — all in re_findings_playerpos.md). Decide A(collapse-to-entrance) vs B(faithful) after T2/T3.

## 🤝 SESSION HANDOFF (2026-06-23 #12 — native map icons SHIPPED, PR #10)
Branch `feat/map-point-native-icons` (PR #10 → <user>/master, 5 commits). SHIPPED + in-game verified (OW+UG+DLC):
- **MapPointProvider** wired (`overlay::native_map_point_icon[_by_name]` → `map_icon_rect` repo-walk → sheet-as-atlas SRV). `category_gpu_iconId`/`category_gpu_icon_name` sparse tables (category_meta.cpp).
- **WorldBosses → MENU_MAP_ERR_Boss** (real ER boss symbol draws). `map_symbol_scale` cfg (2.2×).
- **GPU graces**: overworld=MENU_MAP_01_Bonfire, dungeon(iconId==44)=MENU_MAP_ERR_GraceUnderground (captured in harvest_repo_icons "Bonfire"/"GraceUnderground" branch → cache_map_sprite_from_img sets g_grace_sprite/g_grace_dungeon_sprite). NOT MENU_MAP_GOBLIN_Grace (CreateImage→0, doesn't exist) nor SB_ERR_Grace_*_Color (wrong morning variant). Dungeon grace UNPARKED (draw_marker m.dungeon→s_grace_dgn_tex). Discovered grace = green check (draw_check), not faded tint (ImGui can't desaturate a texture). High-zoom size cap zf≤2.0.
- **Defaults ON**: grace_overlay/grace_gpu_sprite/grace_suppress_native (toggles kept as escape hatches; suppression draw-only, teleport OK).
- **Decoupled from dump_icon_textures + Wine de-lag**: see [[linux-rpm-walk-danger]].

**NEXT SESSION (user-requested):**
1. **RE Boss/Enemy positions via Windows agent** — for the dungeon-boss DRIFT: dungeon-internal bosses (e.g. Stonedigger Troll, areaNo=32/m32 in boss_list.json, internal pos projected to overworld via LEGACY_CONV) land far from the entrance/grace where MapGenie marks them. Runtime enemy pos does NOT fix it (same internal coords, projected same; + unloaded enemies have no pos; big WorldChrMan RE). Real choices: A) collapse dungeon bosses to the dungeon entrance/grace pos, or B) keep faithful. Confirm overworld bosses (m60/61) DON'T drift (only dungeon ones). Decide A/B.
2. **RPM thread move** — kill residual micro-lag: move harvest_repo_icons + harvest_twin_map_icons OFF the game thread (find_detour) onto a background poll (the worldmap_probe loop "always runs" — hook there), publish to g_map_icon_rects/g_grace_sprite under existing mutexes, render/by-name read cache. AUDIT all existing RPM walks first (icon_rpm/ReadProcessMemory: build_fog_pieces, res_walk_groups, collected read_geof/read_wgm already bulk'd, the two harvest walks) + wrap each in GOBLIN_BENCH_QUIET to expose any other Wine cliff. See [[linux-rpm-walk-danger]].

## ⚠️ GRACE SUPPRESSION + LAG (2026-06-23 #11, corrections)
- **Teleport WORKS on GPU graces with grace_suppress_native ON** (user-verified). The old "pin+0x60 broke teleport" warning is SUPERSEDED — the shipped draw-only hide keeps teleport. grace_overlay + grace_gpu_sprite + grace_suppress_native now DEFAULT true (commit d3e0632); toggles kept as escape hatches only.
- **Linux-only icon lag ROOT CAUSE**: `harvest_repo_icons()` (goblin_inject.cpp ~2204, called from find_detour) walks the FD4 image-repo std::map with a `ReadProcessMemory` per node-field (~1100 nodes × several reads = thousands of RPM), re-walking on every `_Mysize` churn. Wine makes RPM ~expensive (same cliff as the read_geof fix, [[collected-geof-bruteforce-scan]]) → micro/big freezes. Windows: RPM cheap, no lag. BENCH (`debug.dump_icon_textures`) is BLIND because the walk runs on the game thread inside find_detour, not the overlay-render bench scope. This same walk feeds GPU graces + map_icon_rect, so it's gated on dump_icon_textures (line 2206) = the coupling. FIX (pending): throttle/limit the walk (walk once/few after map-open, not per churn) + ungate for production (graceOverlay||nativeItemIcons) so GPU graces+symbols work without the dev flag AND without the RPM cliff. Then remove the F1 grace/icon TEST menus (goblin_overlay.cpp:1307/1493/1510, gated dump_icon_textures). See [[native-icon-render-pipeline]].

## 🤝 SESSION HANDOFF (2026-06-23 #10 — map-point iconId→rect SOLVED)
**HEAD = `570d0ce`, branch docs/worldmap-projection-re-brief.** Map-point `iconId→rect` cracked via the FD4 image-repo by-name walk (`repo+0x80`), commit `9f9a028`, runtime-confirmed + cross-validated vs offline `SB_MapCursor.layout` dump (Church `1660,150,1800,338`). `goblin::map_icon_rect(iconId,...)` now live, auto-fills on map open. Sblytbnd/Oodle-decompress avenue = dead end (freed after boot). Linux offline bake blocked (Oodle KRAK, oo2core won't load natively). **NEXT = wire `MapPointProvider` to draw from `map_icon_rect` (hook = `category_gpu_iconId` in category_meta.cpp + `MENU_MAP_%02d`→category iconId map).** See [[native-icon-render-pipeline]]. Below = prior #9 handoff.

## 🤝 SESSION HANDOFF (2026-06-22 #9 — GRACE-SPRITE push (user-driven, reconstructed from git, NOT my in-game verify) + PERF session)
**HEAD = `0688d52`, branch docs/worldmap-projection-re-brief. Deployed DLL = user's own build (md5 28b8679; my last build-linux md5 98cbcbac may differ — user rebuilds/deploys their grace work themselves).**
**🕯️ GRACE-SPRITE BLOCKER LARGELY RESOLVED (17 commits 202a8fc..0688d52, all grace).** The old "grace overlay-sole BLOCKED on RE / GPU harvest grabs wrong time-of-day variant" notes below (#6) are SUPERSEDED — the user cracked the real sprite:
- **Force the CANONICAL vanilla grace** `MENU_MAP_01_Bonfire` (commit 9002809) + force-create the real `MENU_MAP_GOBLIN_Grace` gfx sprite, NOT the pin maps (7ca386c); walk the TWIN map repo `+0xb0` for the real grace pin icon (ed38298, §5i).
- **GPU-INDEPENDENT candidate capture** for vkd3d/Proton consistency (479f5f2; an earlier attempt 83c7e63 was reverted ad79782 then redone) — fixes "fewer candidates on vkd3d". F1 viewer warns when the candidate-force gate is off (d4f2f39).
- **grace GPU offset rides the projection** (zoom-coherent, dacd664); native-vs-imgui compare offset `grace_offset_x/y` + F1 sliders (39065a1) for side-by-side calibration vs the native pin.
- **ERR dungeon-grace icon** `s_grace_dgn_tex` for dungeon graces (181df08) → narrowed to **caves only (m31)** not all dungeons (5f1beb4) → then **PARKED** (0688d52): every grace now draws the BASE grace sprite; the ERR cave icon + `m.dungeon` gate + [GRACE-AREA] log KEPT in code, deferred until the GPU/CPU grace paths are unified (then per-type icons return as a deliberate feature).
- New config: `grace_icon_scale` (1.2 default), `grace_offset_x/y`; scale-slider max raised to 10 + precise typed input + bigger defaults (eddd755/e2a4524).
- **STATE:** grace overlay now has a real sprite source + Proton-consistent candidates; final unification of GPU vs CPU grace paths still pending (the parked cave icon waits on it). Not my in-game verify — summarized from commit messages.
- **🔑 OVERLAY-SOLE GRACE — native-pin SUPPRESSION RE-SOLVED + IMPLEMENTED (2026-06-22, pending runtime validation).** The draw-gate brief (commit 21ae8fe, `windows_grace_warppin_suppression_re_prompt.md`) was ANSWERED: **the real draw-gate is `pin+0x60` (the pin's state field), NOT the cached `pin+0xC`** (findings 68f33a7 `windows_grace_warppin_suppression_re_findings.md` — pin+0xC was a copy the renderer doesn't read). Suppression rewired in `warp_pin_detour` to write `pin+0x60` (commit d8d2125, deployed md5 6730855). **VALIDATE in-game: grace_overlay + grace_suppress_native ON → native discovered-grace pins vanish, overlay = sole grace source (no doubling); player-dot/objectives/fog intact.** If it works → the direct swap to overlay-sole graces is UNBLOCKED (drop the hybrid). **⚠️ REGRESSION found in-game (2026-06-22): zeroing `pin+0x60` ALSO kills the teleport-to-grace fast-travel — +0x60 gates BOTH the draw-gate vt[3] AND the warp callback. → `grace_suppress_native` kept OFF; the +0x60 write is SUPERSEDED (commit 4f922e6 comment).**
- **✅ TELEPORT RE ANSWERED (commits 55e000f findings + 4f922e6, `windows_grace_warppin_teleport_re_findings.md`):** draw + click are COUPLED at the GFx `widget._visible` (← pin+0xC ← pin+0x60); the map cursor only snaps to a `_visible` widget → no separate draw-vs-hit flag. **The ONLY draw-only lever = the pin's `Icon_0`/`IconImage` CHILD widget.** Global registry: ONE container (WorldMapViewModel warp list `VM+0x2d8`, stride 0x350, vtable 0x2ad82a8), ONE widget per pin; renderer + cursor walk the SAME list → no separate draw/callback list to split.
- **🎯 TWO TACTICS for overlay-sole graces (the durable fix, + kills the hybrid Z-FIGHTING the user still sees):**
  - **A (RECOMMENDED, agent's recipe, lighter):** hook `vt[1] SetTo FUN_14087ae20` (er+0x87ae20); after orig, for a discovered WorldMapWarpPinData, fetch the `Icon_0` child (`FUN_14074a2f0(widgetRoot,&child,"Icon_0")` @0x74a2f0) and hide ONLY it (`FUN_140733340(&child,0)` @0x733340), release the proxy (`FUN_140d7f850` @0xd7f850); leave pin+0xC/+0x60 untouched. → native ICON gone (no z-fight, overlay draws it), native widget stays `_visible` = **teleport preserved**. ⚠ risk = the GFx stack-proxy release layout (must match SetTo's locals) — runtime-test for leaks/crash. Resolve fns + WorldMapWarpPinData vftable by AOB. Generalises to any pin type (Point pins share the same vt[1]/vt[3]).
  - **B (user's idea, fallback, heavier):** fully suppress native (pin+0x60) AND re-call the fast-travel ourselves on our overlay icons — needs RE of the warp-trigger fn (`pin+0x54` id / `pin+0x240` sub-record / IsOpen vt[13] FUN_14088bd70) + our own click/hit handling. Use only if A's proxy handling proves unsafe.
  - **DEFAULT until A lands: hybrid (grace_suppress_native OFF).** NOTE the user reports residual Z-FIGHTING in hybrid (native discovered icon vs overlay) → A is the real fix (hide native icon).
- **❌ TACTIC A CRASHED in-game (0xC000001D illegal-instruction) → DISABLED (commit 3af312e, deployed eecf096, SAFE).** The inferred GFx stack-proxy ABI was wrong: crash chain = our detour → get_child(0x74a2f0) → set_visible(0x73334f) → release(0xd7f88c) → jumped to garbage (release deref'd a bad vtable ptr out of our proxy buffer). Gated behind `kSetToHookEnabled=false` → hook not installed, grace_suppress_native harmless again (hybrid). **FOLLOW-UP RE BRIEF (commit 3af312e): `windows_grace_warppin_setto_abi_re_prompt.md`** — needs the EXACT get_child/set_visible/release signatures + proxy struct (size/layout/init, decode SetTo's local_78[40]/local_50[56]) + "Icon_0" string encoding (utf8/16/FName), with the crash addresses; OR the safer route = hook the icon-image setter `FUN_14074bcc0` (0x74bcc0) + feed an invalid descriptor (frame<1/zero rect) so the engine self-hides IconImage — but needs a grace-only filter (the descriptor doesn't carry the pin; maybe a thread-local set by our SetTo detour). Re-enable A once the ABI is verified. CODE + resolves kept.
- **(historical) TACTIC A first cut (commit 7d62d8c) — user chose "OUR sprite on all graces":** New `warp_setto_detour` hooks vt[1] SetTo (er+0x87ae20): after orig, filter to WorldMapWarpPinData vtable (er+0x2ad8228), discovered = pin+0x60 & 7, then hide ONLY the "Icon_0" child via the GFx fns get_child FUN_14074a2f0(0x74a2f0) → set_visible FUN_140733340(0x733340,0) → release FUN_140d7f850(0xd7f850), called in-context on the engine thread (stack proxy over-allocated 256B zeroed). Outer widget stays _visible → teleport preserved; native icon gone → overlay sprite sole (no z-fight). The old pin+0x60/+0xC builder-hook writes DELETED (warp_pin_detour now phase-A log-only). Both hooks gated grace_suppress_native (default OFF, installs at launch) + graceOverlay. **VALIDATE: INI grace_overlay+grace_suppress_native(+grace_gpu_sprite) = true BEFORE launch → discovered graces show OUR sprite (no native double), fast-travel still works, no crash; log "[WARPPIN] SetTo hooked".** ⚠ RISK = the GFx stack-proxy layout/signatures inferred from the RE pseudocode — opt-in flag contains any crash (revert flag). If A crashes → fall to tactic B (re-call warp ourselves).
- **✅ GPU-grace size vs zoom FIXED + VERIFIED in-game (commit 5f6cda6, user "match parfaitement, 2 bugs fixés en 1"):** the GPU grace icon drew constant-px → read huge zoomed-out / tiny zoomed-in. Now scales with the live view zoom (`zf = view.zoom / kGraceZoomRef(0.25)`, clamped 0.3..4, `s_grace_zoom` sampled per frame). Fixed BOTH #2 (zoom scaling) AND #3 (tiny at max zoom) at once. graceIconScale stays the overall dial; kGraceZoomRef(0.25) calibration held. History: discovered graces = `CS::WorldMapWarpPinData`, builder `FUN_14088b7b0` @ er+0x88b7b0 (hooked, phase A identifies discovered via source state `warpData+0x8 +0x1E` bits0-2 + iconId +0x08); the CSWorldMapPointMan `[er+0x3D6E9B0]+0x390` map = CATEGORY pins (areaNo→99), NOT graces.
- **🐛 GPU-grace 2% misplacement (user 2026-06-22):** visible ONLY with `grace_gpu_sprite` ON → suspected the GPU-grace draw path / the `graceOffsetX/Y × s_grace_off_sx/sy` projection-factor calibration scaffolding in map_renderer.cpp (the "duplication of projection code" = that zoom×canvas nudge, TEMPORARY — deleted once overlay-sole, offset→0). SEPARATE from native-pin RE; investigate after suppression. Was flagged out-of-scope in the brief.
- **SRV refactor (commit eac1fae):** the 3 ensure_*_srv fns factored into record_sprite_copy / submit_and_wait / write_inline_srv helpers (−150 dup lines, behavior preserved). NOT in-game verified (GPU).

## 🤝 PERF SESSION (2026-06-22 #8 — Wine-RPM cliffs, see [[collected-geof-bruteforce-scan]] for full detail)
Added an aggregate **[BENCH] session report** (b3a3260, dumps every 30s — DLL_PROCESS_DETACH never fires under Proton) + `GOBLIN_BENCH_QUIET`. It exposed RPM-on-self being a cheap kernel path on Windows but a wineserver IPC round-trip under Wine → Linux-only perf cliffs:
- **read_geof** brute-force scan → bulk-read via the RE'd DLFixedVector (145a41a): **153ms → 0.28ms (×550), VERIFIED.**
- **read_wgm** RPM tree/instance chains → bulk-read (5bef273): max 962ms → 397ms.
- **render.worldmap**: cull off-screen markers BEFORE the per-marker visibility gates (e98636b).
- Page-transition OW↔DLC flicker FIXED (3616a35 — our own view-delay, see [[page-transition-flicker]]); F1-close input-dead FIXED (34fdb61, re-clip cursor).
- ⚠️ **The felt worldmap lag's true cause = a dev `dump_*` flag left ON in the user's INI** (probe_loop RPM storm, uninstrumented → no [BENCH] line); reset config fixed it. Wrapped the dumps in GOBLIN_BENCH_QUIET (202a8fc). LESSON: unexplained lag → suspect an uninstrumented config-gated debug/dump flag first.

## 🤝 SESSION HANDOFF (2026-06-22 #7 — icon-rect + fragments VERIFIED, stale-deploy caught)
**✅ VERIFIED IN-GAME (2026-06-22 #7):** (1) **icon-rect fix `650c0f8`** (snap BC sub-rect to 4-block bounds + UV-crop) — most category markers now show REAL icons, "work". (2) **`require_map_fragments` `f45fbfc`** (one toggle gates fragment AND fog) — "work". Both verify-backlog items CLEARED.
⚠️ **STALE-DEPLOY GOTCHA recurred (the recurring one):** deployed DLL md5 `1dbd56` was built 10:07 BEFORE the icon-fix commits (`650c0f8`@10:14) → user would've tested stale. `cmake --build build-linux` relinked goblin_inject.cpp → NEW md5 `fb8980` (size 5323776→5324800). REDEPLOYED. **Lesson: after ANY commit, `cmake --build` + re-cp BEFORE asking user to test — never trust a prior md5 across commits.** DLL loads at game LAUNCH → restart ERR.
**NEXT (non-RE unblocked):** region-name banner suppression · 6 circle-fallback categories→real icons (FFDEC atlas regen) · endgame baked→runtime getters. (Chapel cluster mis-placement = FIXED per user 2026-06-22 #7.)

**🐛 INPUT-DEAD-AFTER-F1-CLOSE (2026-06-22 #7) — FIXED ✓ (user "work" in-game after the patch):** user hit "souris+inputs plus clickable" after opening then closing F1; was intermittent (gone after relaunch). STRUCTURAL GAP FOUND + PATCHED (commit 34fdb61, md5 9f7b81) — re-clip on close confirmed working: the overlay had ZERO falling-edge cleanup on `g_show` — while F1 open, `hk_clip_cursor` forces the cursor UNclipped on every game ClipCursor (goblin_overlay.cpp:615), and the game only re-clips on focus change → after close the cursor stays free, leaving mouse-look/map-drag dangling. Fix = falling-edge handler re-confines cursor to window client rect (o_clip_cursor with the screen rect). Defensive — may not be THE cause (could be an init-race in the cursor/WndProc/ImGui_ImplWin32 hook install order). **If it recurs: grab the log, check whether the cursor hooks (SetCursorPos/ClipCursor @ ~goblin_overlay.cpp:1948) installed before the first F1, and pin the exact dead-input scope (map-only vs gameplay vs all).** The whole input-suppression design (WndProc swallow + raw-input blank + DI zero + cursor freeze, all gated on `g_show`) is fragile = the root reason for the user's REDESIGN idea below.

**💡 REDESIGN IDEA (user 2026-06-22 #7, BACKLOG):** "intégrer F1 directement à la carte au lieu d'un menu séparé, pour éviter de bloquer les inputs de tout." Current F1 = a separate ImGui panel that, while open, GLOBALLY swallows ALL mouse+keyboard (heavy hooks) so the game gets nothing — fragile (see bug above). Desired = controls that live with the map and only capture input when actually interacting (e.g. WantCaptureMouse over the panel), never a global block. NOTE: ER's world map is Scaleform, so "in the map" still = an ImGui overlay; the real win is dropping the global input-block in favour of scoped capture. Meaty UX refactor; weigh vs simply hardening the current toggle. Not started. RE-blocked + PARKED: grace native-pin suppression (WarpPinData list scan) + discovered-grace sprite (resident-icon enum) — both need Windows RE agent.

## 🤝 SESSION HANDOFF (2026-06-22 #6 — grace pins + icon harvest + Phase 2b finale)
All built (clang-cl/xwin Linux) + deployed dll/offline/MapForGoblins.dll (latest md5 1dbd56, after baf2517). Branch docs/worldmap-projection-re-brief. NOT in-game verified except where noted.
**✅ MAP-PROJECTION RE = DONE, NOT a blocker (memory was stale, corrected):** live WorldMapViewModel projection wired + default-ON + in-game verified ([[worldmap-projection-re-solved]]); only leftover = delete baked LEGACY_CONV table after a map-closed cross-check (deferred). Killed the "waiting on map-projection RE" notes.
**NATIVE-PIN SUPPRESSION (graces):** diag `dump_native_pins` PROVED `[er+0x3D6E9B0]+0x398` std::map is EMPTY in overlay-sole mode (no native category pins double the overlay ✅). Native discovered graces are a SEPARATE system = **`CS::WorldMapWarpPinData`** (RE answered my brief: commit e4b3f6a `windows_native_grace_pin_manager_re_findings.md` — builder FUN_14088b7b0, discovered gate `[warpData+0x8]+0x1E` bits0-2, iconId +0x08, sprite SB_ERR_Grace_Morning_Color via world-map movie §8). **Step3 suppression: phase A (hook+log) CONFIRMED identification; phase B (zero pin+0xC) FAILED** (our pin still stacks over vanilla → pin+0xC not the draw field) → PARKED; real fix = clear the WarpPinData LIST / its show-predicate (needs the runtime container scan, e4b3f6a §5). Code committed but flags OFF (hybrid restored).
**GRACE OVERLAY RENDER (step1+2, committed, PARKED):** harvest grace sprite via CreateImage path (ebd3f04); overlay draws all graces + CPU/CPU-vs-GPU toggle + F1 debug viewer (d17a39c). **Both icon sources wrong:** CPU baked `show_graces` = the UNDISCOVERED-style art; GPU engine = time-of-day variant + the CreateImage harvest only ever yields an UNUSED frame (LateDay), never the drawn one. → grace-overlay-sole BLOCKED on RE (proper discovered sprite + reliable suppression). The ORIGINAL HYBRID (native=discovered lit-gold, overlay=undiscovered baked) is visually correct → kept (grace_overlay/grace_suppress_native default OFF).
**ITEM-ICON HARVEST:** browse-to-fill find-hook (FUN_140d63c30) WORKS (icons render in F1 P2b panel). §8 proactive walk-all PARKED (FUN_140d69640 only hits load movies; CreateImage list ≠ the marker-draw path). Fence-batching optim committed (27f9eea: one ExecuteCommandLists+fence-wait per frame vs per-icon). 2 RE prompts pending Windows agent: `windows_resident_icon_enumeration_re_prompt.md` (+addendum: drawn-marker→sprite-frame) + the WarpPinData runtime list (e4b3f6a §5).
**✅ require_map_fragments FIXED (f45fbfc):** one toggle now gates BOTH the map FRAGMENT (m.fragment_flag=GetMapFlagFromTile, was computed-but-unused) AND fog (marker_fogged). Test on an early save.
**✅ PHASE 2b COMPLETE — native map fully gone, overlay UNCONDITIONAL:** removed dead live_loot_flags/icons (25b739f), live-refresh machinery + live_refresh_world_map (95d04ba), native_map_injection toggle (baf2517). See the ENDGAME section below.

## 🤝 SESSION HANDOFF (2026-06-22 #4 — earlier, big session)
All built+deployed (clang-cl/xwin Linux) to dll/offline/MapForGoblins.dll; **deploy gotcha recurred — always `stat` the REAL mtime sandbox-off after a `cp`, not just md5 (an intermediate deploy didn't land → user tested stale)**. DLL loads at game LAUNCH → restart ERR to pick up a redeploy.
**SHIPPED + VERIFIED in-game:** player-pos FULLY RESOLVED incl. frame (`[[WCM+0x1E508]+0x6C0]`=WorldMapPointParam posX/posZ, commit a2e3c27) → minimap + distance-adaptive work ALL pages; **distance-adaptive REWORKED to the RAW per-area frame** (get_player_raw_pos/grace_anchor_raw, commit 6d9de59 — the projected unified frame OVERLAPS overworld↔underground so euclid removed near-Siofra markers; user "définitivement fixé"); region LABELS Limgrave fix (was area-10 mis-baked → project anchors via marker_world_pos like markers, commit d0324b2); region NAMES via MapNameOverride point-in-volume + nearest-volume fallback (commits 3387358/ef979e9 — names RESOLVE fine, NOT an FMG bug).
**✅ NEWLY VERIFIED in-game (2026-06-22 #5):** atlas PNG-embedded (5db8995) + anonymous_loot (76e2f2a) + minimap foundation→all-pages (52288bf/c176b0f) — all "work perfectly" per user.
**SHIPPED, STILL NOT verified:** SetSecondaryFlags+Ashen (373fcdc); UI polish (bb14fb0: F1 topmost via BackgroundDrawList, minimap hides when map open + anchor/offset settings, 3 split debug toggles cluster_debug_radius/debug_cluster_anchors/debug_region_volumes).
**ENDGAME native-map removal:** Phase 1 (native_map_injection default→false, deb0257) + Phase 2a (apply_map_logic+goblin_logic patchers, 64c0fff) + 2b-1/2b-2 (decouple + delete inject_map_entries -777L, eb750bc/68e02bf) DONE. **✅ PHASE 2b COMPLETE (2026-06-22):** native map fully gone, overlay UNCONDITIONALLY the map. Final cleanups this session: dropped dead `live_loot_flags`+`live_loot_icons` (25b739f; kept `live_loot_labels` = randomizer relabel); removed the dead live-refresh machinery (wm_build_detour/FUN_140a82a80/refresh_world_map_icons/install_live_refresh_hook + `live_refresh_world_map` config, 95d04ba — was "kept for Plan B" but Plan B uses FUN_14088b7b0 now); removed the `native_map_injection` toggle (baf2517 — inject was already gone so true=blank-map footgun; probe + overlay now always on). Earlier 2b (replan_clusters/eviction/loot fns/menu-loop surgery/native globals) already deleted in prior sessions. KEPT: overlay_markers_proto (dev), project_dungeon_row_to_overworld (shared), WORLDMAP_POINT_CTOR sig (harmless landmark). Obsolete ini keys auto-comment on load.
**WAITING ON RE AGENT (briefs written + committed):** (1) ~~map-projection~~ **✅ DONE — NOT waiting (re-confirmed 2026-06-22):** RE answered + objdump-verified + IMPLEMENTED + IN-GAME VERIFIED (`config::liveProjection` default **true**; worldmap_probe::project + live LegacyConv fold goblin_legacy_fold.cpp + DLC eyeball deleted; "fixed hundreds of misplaced objects in a blink"). Baked LEGACY_CONV/affine kept ONLY as map-closed fallback; only leftover = delete baked table after rigorous live-fold cross-check. bug-3 Chapel handled (live no-match→gate). See [[worldmap-projection-re-solved]]. (Was waiting on `docs/re/windows_world_to_mapspace_projection_re_prompt.md` (e69d0a0) → kill ALL baked projection (LEGACY_CONV + mapspace affine + DLC-UG eyeball + marker_group_from); ALSO fixes bug-3 Chapel (m19 has NO LegacyConv — game param confirmed 0 area-19 rows → prologue, no overworld pos → our raw projection mis-places a "Chapel of Anticipation" cluster right of Stormveil; cluster pos is NOT zoom-dependent, that's the fixed-wrong-spot illusion). (2) **native-pin suppression** — ⚠️ **RE PREMISE WRONG, re-diagnosed 2026-06-22.** Built a SAFE diag `dump_native_pins`/`dump_native_pin_map` (goblin_worldmap_probe.cpp, config `dump_native_pins`, ReadProcessMemory walk of the std::map w/ corrected `_Mysize@+0x3A0`; RVAs in re_signatures `ICON_MGR_SLOT_RVA`/`_SIBLING`). **RUNTIME RESULT (in-game, map open + discovered graces drawn + panning): `CSWorldMapPointMan [er+0x3D6E9B0]+0x398` is EMPTY (size=0, valid sentinel head) across all samples.** Logic: injection off → WorldMapPointParam ~empty → manager builds empty → NO native category pins double the overlay (overlay already sole for categories ✅). The native DISCOVERED-grace pins the overlay dedups against are NOT in this manager → a SEPARATE system (BonfireWarpParam→runtime→Scaleform, per goblin_inject.cpp:911). SIBLING [er+0x3D6F558] = garbage at +0x398/+0x3A0 (not that shape). **So areaNo→99 (targeted our injected rows, gone) AND clear-+0x398 (empty) both INAPPLICABLE to graces.** User CHOSE to pursue it anyway → brief `docs/re/windows_native_grace_pin_manager_re_prompt.md` → **✅ ANSWERED (commit e4b3f6a `windows_native_grace_pin_manager_re_findings.md`):** native discovered graces = **`CS::WorldMapWarpPinData`** (warp pin list off the WorldMapViewModel/dialog, vtables 0x2ad8228/0x2ad82a8; source `WorldMapWarpData` 0x38B). Discovered gate = `[warpData+0x8]+0x1E` byte bits 0/1/2 (FUN_140d25540); iconId `[warpData+0x8]+0x08` (FUN_140d25650). **Suppression (grace-only, keeps player/fog/objectives):** hook the WarpPinData builder **`FUN_14088b7b0` (0x88b7b0)** → skip discovered (non-destructive, engine thread), OR clear the list via its own method. **§6 discovered-grace sprite = `SB_ERR_Grace_Morning_Color` (74×74, 2048×1024 ERR sheet) → harvestable via the §8 image-list walk on the WORLD-MAP movie** (hook FUN_140d69640 on that movie, not inventory). Runtime TODO: pin the live WarpPinData list (scan dialog/VM for stride-0x38 vector size=N-discovered), point dump_native_pins at it. (§6 walk = commit 6913ec4 `windows_runtime_item_sprite_re_findings.md`.)
**IMPL PROGRESS (2026-06-22):** Step1 grace-sprite harvest DONE (ebd3f04 — captured via the CreateImage path/dump_icon_textures_live, NOT the find fn; getter harvested_grace). Step2 overlay grace rendering DONE+committed (d17a39c, NOT in-game-final): config `grace_overlay` (overlay draws ALL graces, discovered=colour/undiscovered=grey) + `grace_gpu_sprite` toggle (CPU baked atlas `show_graces` icon = clean/constant DEFAULT, vs GPU engine sprite copied 74×74 BC7 snapped-4-align+UV via ensure_grace_srv/set_grace_sprite); draw_marker grace branch + drop-gate keyed on grace_overlay (off=old hybrid); F1 checkboxes + "Grace sprites (GPU debug)" viewer (full-sheet SRV per SB_ERR_Grace_* candidate). ⚠️ **GPU path FRAGILE: the engine grace sprites are time-of-day variants (Morning/LateDay/Night Color); harvest grabs whatever is RESIDENT — often an UNUSED variant (LateDay = never-drawn per user) not the one actually shown → wrong/time-tinted look. → FAVOR CPU baked** (sidesteps it). Build clean, deployed. **STEP 3 native-pin suppression — ATTEMPTED, BLOCKED (2026-06-22):** Hooked the WarpPinData builder `FUN_14088b7b0` (er+0x88b7b0, config `grace_suppress_native`, install_grace_suppression_hook). Phase A [WARPPIN] log CONFIRMED: builder fires per grace from a stack-temp WarpData (0x11f4f0)→pin(0x11f560); source entry `warpData+0x8`, **state byte +0x1E = 1 (registered/drawn) for real graces, 0 for the few never-drawn (iconId 4001950, 11991958 = prologue/special)**; iconId @ src+0x08 (region-coded 10001950/11001950/...). **Phase B FAILED (commit 77af750): zeroing the built pin's `pin+0xC` (the copied state byte) via WPM does NOT suppress** — the native pin still draws (our overlay pin just stacks ON TOP = still doubled). So `pin+0xC` is NOT the draw-gate field. NEXT suppression attempts: try `pin+0xA` (iconId→invalid), or the real fix = clear the WarpPinData LIST / its show-predicate (needs the runtime container scan deferred in e4b3f6a §5 — the static builder-hook isn't enough). **STEP 3 PARKED pending more RE.**
🐛 **BAKED GRACE ICON IS THE UNDISCOVERED STYLE (2026-06-22):** the mod's `show_graces` atlas icon (CPU path) = the UNDISCOVERED-grace art; using it for discovered graces too = wrong look. So neither source is right for a proper discovered grace: CPU baked = undiscovered-style; GPU engine = time-tinted/unused-variant ([[bug 1]]). A correct overlay-sole grace needs either a NEW baked discovered-grace icon OR the reliable engine discovered sprite (blocked on the resident-icon RE). **The ORIGINAL HYBRID was actually visually correct** (native=discovered lit-gold, overlay=undiscovered baked) → reverting grace_overlay/grace_suppress_native to OFF restores it. Step3/Step4 = PARKED pending RE (proper discovered sprite + reliable suppression).
(was: NEXT: user picks CPU vs GPU; Step3 suppression; Step4 drop iconId-370/dedup.) Item-icon §8 walk-all still PARKED (repo-walk RE pending).
**🐛 TWO BUGS NOTED (2026-06-22):** (1) **GPU icon-harvest reads the WRONG sprite** — the CreateImage-hook harvest (`dump_icon_textures_live`, made repeatable+dedup) only EVER yields `SB_ERR_Grace_LateDay_Color` (an UNUSED time-of-day variant, never the drawn frame) and NO other SB_ERR_ map markers, even panning. The CreateImage image-list isn't the path that selects what a marker draws → need RE for how a DRAWN marker (WarpPinData iconId `[warpData+0x8]+0x08`) → its actual sprite RECT + where the time-of-day grace variant is SELECTED. **Addendum appended to `docs/re/windows_resident_icon_enumeration_re_prompt.md`.** → confirms CPU-baked grace is the right default; GPU grace blocked on this RE. (2) **require_map_fragments — FIXED (commit f45fbfc, NOT in-game verified).** User: fog-of-war != fragment. The toggle only gated marker_fogged() (fog reveal); now ONE toggle enforces BOTH (worldmap+minimap): hide unless (a) region MAP FRAGMENT acquired (`m.fragment_flag`=GetMapFlagFromTile, read live — was computed-but-unused) AND (b) tile not fogged (marker_fogged). Test on an EARLY save (100% reveals everything). Making overlay the SOLE grace source needs BOTH: this grace-manager RE + the discovered-grace sprite ([[overlay-icon-atlas]] DX12 readback / §8 GFx harvest). Until then the hybrid (native=discovered, overlay=undiscovered) is CORRECT, not a bug. Diag built+deployed (md5 04df79…), flag set false after proving empty. (3) **region-name banners** (vanilla "Limgrave"/"Caelid" text) suppression = no RE yet. (4) grace-from-memory RE (DX12 texture readback) backlogged in [[overlay-icon-atlas]].
**NEXT (map-projection ✅ shipped):** native-pin suppression (selective areaNo→99, keep graces) + draw-all category overlay; then Phase 2b cleanup; later delete baked LEGACY_CONV table after live-fold cross-check. Temp files left: <scratch>/mfg_atlas_full.png + mfg_grace_cell.png.

## 🤝 SESSION HANDOFF (2026-06-21 #3 — earlier)
**ALL session-#2 + #1 parity work VALIDATED in-game by user** (region-labels, quest-NPC gating, ≥2³⁰ census, redify boss, marker_group de-dup + graying/grace-dedup/census numbers/cluster depletion/distance-adaptive overworld) → VERIFY BACKLOG CLEARED.
**DEBUG VIZ STRIP = ON HOLD waiting for the RE agent** (cluster_debug_radius player-pos viz: magenta dot + cyan grace anchors + [PLR] HUD in map_renderer render_markers). Keep until the underground player-pos CT lands; do NOT strip now.
**SHIPPED: SetSecondaryFlags + Ashen Capital post-event gate ported to overlay (built+deployed md5 9bfe…, NOT in-game verified).** Last real behavioral parity gap closed. New `Marker.secondary_flag` (marker_layer.hpp, after discover_flag) = a story flag that must be SET for the marker to show (inverse of discover_flag). Filled by `secondary_story_flag(area,gx,gz)` in map_entry_layer.cpp anon ns (replicates legacy GetMapFragment SetSecondaryFlags tile table + apply_map_logic ashen branch): area35 (Ashen Capital, whole area) + post-burn Leyndell(11,gx5)/Chapel(19) → StoryErdtreeOnFire 118; Caelid-ish (21,gx0/1/2)+(22) → StoryCharmBroken 4927; (20,gx1) → StorySealingTreeBurnt 330; all need gz==0. Render gate in map_renderer.cpp render loop AFTER quest_npc gate, BEFORE world_to_mapspace: `if (m.secondary_flag && !read_event_flag(m.secondary_flag)) continue;` read LIVE. **DECOUPLED from require_map_fragments** (legacy coupled SetSecondaryFlags to it; ours is unconditional — it's a story-spoiler gate, correct regardless). No new config (gate-only, like discover_flag). ⚠️ Note: gates Chapel m19 markers behind flag 118 → pre-burn Chapel content hidden (partial prologue-blank parity, intended). **VERIFY in-game: pre-event → no markers in Ashen Capital / post-burn Leyndell / Chapel / charm-broken / sealing-tree-burnt tiles; after the story event fires → they appear live.**
**CLUSTERING STAYS OPT-IN (default false) — do NOT flip to true (user 2026-06-21 #3).** It was MANDATORY only for the NATIVE map's lag; the ImGui overlay has zero freeze → clustering is now a pure optional declutter tool, off by default. (Earlier "flip enable_clustering default→true" suggestion = retracted.)
**CONFIG MIGRATION SCOPE DECIDED (user #3):** of the ini-only configs, `native_map_injection` + `live_loot_*` = DELETE after the native-map removal (not worth exposing). `project_dungeons` = DROP as OBSOLETE (like clustering — born of native-map constraints; the overlay projects all minor-dungeon content onto the overworld BY DESIGN, that's its whole value; remove the flag at endgame). **Only `anonymous_loot` gets migrated** (overlay honor + F1) — spoiler-free gray "?" + generic label on lot-backed loot markers.
**SHIPPED: anonymous_loot ported to overlay (commit 76e2f2a, built+deployed md5 35a3…, NOT in-game verified).** Lot-backed loot markers honor anonymous_loot in overlay-only mode: gray "?" disc + "?"+location label (no real icon/name). New `Marker.lot_backed` (set in map_entry_layer build for lot-backed loot, excludes pieces/kindling like goblin_inject is_lot); draw_marker anon "?" branch + marker_label "?" branch (map_renderer.cpp); F1 "Spoiler-free loot" checkbox (goblin_overlay.cpp:619, anonymousLoot already in ini schema → persists). Category show/hide unaffected; collected still grays. **VERIFY in-game: enable Spoiler-free loot in F1 → loot markers become gray "?", tooltip shows only "?"+location; pieces/kindling/bosses unaffected; toggle off → real icons return.**
**SHIPPED: overworld minimap HUD foundation (commit 52288bf, md5 96ba…, NOT in-game verified).** See [[minimap-future-feature]] for full detail. Opt-in corner HUD (north-up, overworld-only), reuses overlay layers/atlas/draw_marker + hide-gates; reads get_player_map_pos live during gameplay. **DOUBLES AS the map-CLOSED player-pos test** (the ⚠️ this task hinged on): if markers track player movement with the map closed → get_player_map_pos is a viable live source. F1 "Minimap (in-game HUD)" header + show_minimap/minimap_zoom/size/opacity configs.
**NEXT (non-RE, unblocked):** verify minimap tracks map-closed (in-game); then ENDGAME (remove native map: flip native_map_injection default→false, strip legacy inject + project_dungeons + live_loot* configs; baked→runtime getters). Minimap follow-ons (heading RE for heading-up, UG player-pos RE) deferred. (⚠️ verify map-CLOSED player-pos update). Then ENDGAME (remove native map, baked→runtime). Debug-viz strip still ON HOLD for RE.

## 🤝 SESSION HANDOFF (2026-06-21 #2 end)
All commits BUILT + DEPLOYED to dll/offline/MapForGoblins.dll (clang-cl/xwin Linux); **NONE in-game verified except the player-pos debug screenshots + the loot-flag log analysis**. Latest deployed md5 ≈ b49d… (commit 69527c1). HEAD branch = docs/worldmap-projection-re-brief.
**SHIPPED THIS SESSION (non-RE parity + fixes):** region-labels (dc86199), quest-NPC quest-aware gating (bbb7177), ≥2³⁰ temp-flag census exclusion (204b62b), redify boss icons + F1 toggle (9022ea9), `marker_group_from` de-dup refactor (69527c1), debug-scaffolding strip (0265a27). F1↔init parity AUDIT = PASS (endgame #1 ✓).
**RE BLOCKER #2 (collected loot flag) = CLOSED, NO BUG.** [LOOTDIAG] proved the resolution is correct; the 100%-save over-report = a VANILLA save ported to ERR → different regulation.bin → flag IDs don't correspond (cross-regulation mismatch). The ≥2³⁰ exclusion (only real fix) shipped. See [[overlay-pending-re]] #2.
**RE BLOCKER #1 (underground player pos) = STILL OPEN, narrowed conclusively.** In-game proved: grace/marker projection pipeline CORRECT underground (cyan-anchor debug); the bug is the player INPUT FRAME — MapId tile + manager local underground ≠ WorldMapPointParam grid. Axis fix (+0x74→+0x78, ecc14dd) needed but insufficient. Overworld player ≈ correct. → handed to RE: pursue the CURSOR PROVIDER (cursor+0x90 + FUN_140d82770 = the yellow-dot source) via the runtime-CT prompt (`docs/re/windows_player_pos_runtime_ct_re_prompt.md`, narrowed 0164acb). Distance-adaptive stays OVERWORLD-gated. Full saga in [[overlay-pending-re]] #1.
⚠️ **DEBUG VIZ STILL IN CODE** (map_renderer render_markers, gated `cluster_debug_radius`, currently TRUE in the user INI): magenta PLAYER dot + cyan page-gated grace anchors + [PLR] HUD. **Strip when the CT lands, or now for clean code** (set ini false meanwhile).
**NEXT (non-RE, unblocked):** strip player-pos debug viz; secondary story flags (SetSecondaryFlags) + Ashen Capital gating; expose ini-only configs in F1 (nativeMapInjection/projectDungeons/liveLoot* — pre-native-removal); flip enable_clustering default→true; overworld minimap foundations (overworld player-pos works; ⚠️ verify it updates map-CLOSED). 2 minor UG data issues noted (cross-page grace leak, Deeproot offset).
**VERIFY BACKLOG (in-game, nothing verified this session):** region-labels, quest-NPC gating, ≥2³⁰ census, redify boss, + everything from session #1 (graying, grace-dedup, census numbers, cluster depletion, distance-adaptive overworld).

## 🤝 SESSION HANDOFF (2026-06-21 #1 end)
All this session's commits BUILT + DEPLOYED to dll/offline/MapForGoblins.dll (clang-cl/xwin, Linux) but **NONE in-game verified** unless noted. Latest deployed md5 ≈ 8308… (commit 5a97336). Commit chain (oldest→newest): cbd786d graying · c312912 grace-dedup · 3e529d2 cluster-depletion+census-badges · 60adbd2 live-loot resolver · c9f6abd/b8a9c61 census diag+gate · 4c26c76 census←overlay-buckets · 4e1df37 distinct-flag+cleared-flag census · 6945ac0/038b81c/60db488 [FLAG-SRC] probe(+removed) · fffdcc4 try_get(no-spam) · d71d19f marker-dedup→**REVERTED 9ab2c01** · d3f9d32 distance-adaptive+F1-reframe · 294346d/4792410 distance-adaptive OVERWORLD-ONLY · 4c46423/d16ba90/95a42a9/afaeba9 debug-radius viz + underground attempts(reverted to OW-only) · 5a97336 F1 docs · cf268ca RE prompt docs.

## 🚫 PLAN B BLOCKED on runtime RE (2026-06-21 #3) + 🆕 LOCATION-NAMING opportunity (user idea)
**PLAN B (suppress native pins) = BLOCKED.** RE findings (commit 1fa0910, `docs/re/windows_suppress_native_pins_re_findings.md`): +0x398 = std::map<int,CSWorldMapPointIns*>, mgr=[er_base+0x3D6E9B0], real build = undefined fn ~0x140a82c30, show-predicate vt[1] FUN_140a81450 runs before insert; rec = areaNo→99 filter (reuses the parking code Phase 2b is removing) OR clear +0x398. **[PINSET] runtime diag (commits 7f3d641+0c8f532, config dump_native_pins) PROVED the static offsets DON'T hold at runtime:** mgr0=[er+0x3D6E9B0]=heap but size 0 + NO std::map candidate in its 0x300-0x600 window; mgr1=[er+0x3D6E9D8]=a module-static self-ref (er_base+0x3d6e9f0). So the real CSWorldMapPointMan / icon container must be found by a **runtime pointer-scan** (Windows RE agent: CE find-what-accesses on the render walk [R15+0x398] @0xa8397e, or pointer-scan from a visible native pin). **NEXT for B: write a follow-up RE brief with the [PINSET] runtime result; hand to agent.** Phase 2b cleanup stays PAUSED only because rec#1 (areaNo) might reuse parking — but B is blocked anyway, so Phase 2b CAN resume independently if desired.
**✅ RELIABLE LOCATION NAMES + UNDERGROUND RAW-FRAME DISTANCE-ADAPTIVE (2026-06-21 #3, in-game iterated):**
- Region names (commit 3387358 + ef979e9): MapNameOverride point-in-volume; the [REGION] diag PROVED it's NOT a frame bug (markerPos shares the volume frame) — the gaps were COVERAGE (volumes are small named sub-boxes, most markers sit outside). FIX (ef979e9): `region_name_pname` falls back to the NEAREST volume in the SAME MSB map when nothing contains (regions contiguous → nearest named volume = right region; a map with Nokron+Siofra volumes disambiguates by proximity). Only volume-less maps (m10/m11 legacy) → grace fallback. **RESOLVED — it was a DIFFERENT feature (the big REGION LABELS, not the tooltip naming).** `region_name_pname` (MapNameOverride tooltip/cluster naming) WORKS — the debug_region_volumes viz showed all names CYAN (textIds RESOLVE) → the FMG-resolution theory was WRONG, NO re-home needed. The real bug 1 = `draw_region_labels` (the big gold region text, uses MAJOR_REGION_ANCHORS): **Limgrave was mis-baked as area 10 (Stormveil) with m10-local negative coords** → draw_region_labels doesn't re-project → off-screen (Caelid/Altus area-60 drew fine). FIX (commit 650f29a): Limgrave → area 60 (10731,9553) = centroid of its MapNameOverride volumes + an OVERRIDE in build_major_regions.py. ⚠️ STILL OPEN: the underground anchors (area 12 Ainsel/Deeproot/Siofra) carry raw m12 coords that draw_region_labels doesn't project → likely mis-placed on the UG page (overworld user didn't see it). (was: STILL NOT FIXED in-game — Limgrave/Siofra/Ainsel/Deeproot wrong:) Likely a textId→FMG RESOLUTION issue, NOT a coverage/frame bug: `region_name_pname` returns the MapNameOverride `textId` (PlaceName id, e.g. Siofra=12070, Ainsel=12010, Deeproot=12030) and the label calls `lookup_text_utf8(textId)` — but the region-name strings live in a DIFFERENT FMG bank than lookup_text_utf8 reads (MAJOR_REGION_ANCHORS RE-HOMES its strings into fresh PlaceName ids via setup_messages for exactly this reason). Caelid/Altus (overworld 6xxxx ids) resolve; underground 12xxx may not. **NEXT: confirm by checking lookup_text_utf8's FMG bank vs the MapNameOverride textId bank; fix = either resolve via the region-banner FMG, or re-home the strings like MAJOR_REGIONS (but the baked `name` is vanilla English, not ERR's rename — to keep ERR's modded names, must resolve the LIVE textId in the right bank).**
- **DISTANCE-ADAPTIVE CLUSTERING = FIXED, user-confirmed "définitivement fixé" (commit 6d9de59).** Root cause: the un-gate (c176b0f) ran the euclid ramp in the PROJECTED unified map-space, which OVERLAPS overworld↔underground (Siofra projects onto the Lands Between) → garbage distances → removed near-player markers in Siofra. FIX: measure player↔grace distance in the RAW per-area frame (`goblin::get_player_raw_pos` + `grace_anchor_raw` = MapId-area + gridX*256+ChrIns/posX, NO projection), gated to the SAME raw area (underground sub-maps stay distinct via gridX*256). Works ALL pages. The "must-have absolute-pos-like source for clusters" the user asked for = this raw per-area frame. Projected grace still used for pile PLACEMENT; projected player kept for debug rings.
- ⚠️ DEPLOY GOTCHA recurred: an intermediate `cp` didn't land (sandbox overlay) → user tested a stale DLL ("stale 18 min"). ALWAYS `stat` the REAL mtime (sandbox-off) after deploy, not just md5. DLL loads at game LAUNCH → restart ERR to pick up a redeploy.
- 🐛 OPEN bug 3: the "Chapel of Anticipation" cluster is mis-placed (decalé, drifts with zoom) — NOT red/CENTROID in the debug viz, so it HAS a grace anchor but the anchor (m19 Chapel) MIS-PROJECTS. Cluster-anchor debug viz added (cluster_debug_radius: anchor + member lines + name + d=/thr=, commits a9b606b/80dcb5b). Need where it appears to fix.
**(earlier) RELIABLE LOCATION NAMES SHIPPED (commit 3387358, md5 a759bd).** Baked data/map_name_regions.json → src/generated/goblin_name_regions.{hpp,cpp} via tools/build_name_regions.py = **219 MapNameOverride volumes** (Box/Cylinder, per-map MSB-local frame, keyed area/gridX/gridZ, smallest-first) covering m60/m61 overworld + m12 Eternal Cities + legacy. `goblin::region_name_pname(area,gx,gz,posX,posZ)` = inverse-yaw point into each matching volume + containment test → most-specific PlaceName textId (0 = none). `marker_cluster_key` now sets out_pname = region_name_pname() ?: nearest-grace pname (grouping key stays grace-based). Tooltip + cluster-pile labels now use the game's own region logic. **VERIFY in-game: hover a marker in Nokron/Siofra/etc + at a region border → correct name (was the neighbouring grace's region); cluster labels correct.** ⚠️ FRAME RISK: assumes a marker's raw posX/posZ shares the volume's MSB-local frame for the same (area,gx,gz) — true by construction (both are MSB-local for that map) but UNVERIFIED esp. underground; mismatch → no containment → falls back to grace (no regression). ⚠️ textId must resolve via lookup_text_utf8 (ERR FMG); unresolved → empty label (rare). ⚠️ build_name_regions.py writes to src/generated only (ERR bake); a vanilla profile bake-dir would need it too. (was: idea, UNBLOCKED, no RE) — ORIGINAL NOTE: the tooltip + cluster-pile location label (`loc_pname`) currently comes from a FRAGILE heuristic — `find_nearest_grace`→`find_nearest_region_pname`→`find_nearest_major_region` (nearest-anchor = wrong near region borders; underground = coarse "Underground"). FIX = the game's OWN region logic = **MSB MapNameOverride volumes** (point-in-VOLUME containment, not distance). **Data ALREADY EXTRACTED: `data/map_name_regions.json` (472KB, 360 maps; each MapNameOverride = Box shape + center + Width/Height/Depth/Radius + TextID=PlaceName)** via `tools/extract_map_name_regions.py` — but NEVER baked/used (grep empty). PLAN: bake → goblin_name_regions.cpp; assign loc_pname by point-in-volume in the marker's ORIGINAL MSB map/local frame (area+gridX+gridZ+posX+posZ, pre-projection — volumes are per-map local coords); handle rotated boxes + per-map keying. Replaces the find_nearest_grace chain for tooltips + cluster labels. Meaty but tractable; the best unblocked next feature.

## (history) 🎯 PLAN B: suppress native pins → overlay = sole icon source (user chose 2026-06-21 #3)
RE brief written + committed (873330b): `docs/re/windows_suppress_native_pins_re_prompt.md`. **Largely pre-RE'd:** native icon BUILD = `FUN_140a82a80` (AOB `WORLDMAP_POINT_CTOR`, ALREADY hooked by `wm_build_detour` in goblin_inject.cpp for live-refresh); built-icon map = **`CSWorldMapPointMan` (static 0x143d6e9b0) +0x398** (node key@+0x20). Plan: in wm_build_detour, after the orig build, EMPTY/filter +0x398 → no native pin draws. **User scope (kill vs keep):** KILL all category pins + native graces; KEEP player "you are here" dot + fog/fragments + player-placed markers + objective beacons. RE must report which of those live in +0x398 vs separate managers (to clear-all vs selectively-filter) + the safe clear recipe. **COUPLED overlay change (do WHEN suppression lands, else double graces):** drop the grace `discover_flag` dedup in map_renderer.cpp (currently draws only UNDISCOVERED graces, relies on native pin for discovered) → draw ALL graces. **NEXT: user gives RE findings → implement the +0x398 clear/filter in wm_build_detour + draw-all-graces.** Note: this REPURPOSES the FUN_140a82a80 hook (Phase-2 native-code-removal must NOT delete that hook now — it becomes the suppression mechanism).

## 🧨 ENDGAME PHASE 1 DONE + PHASE 2 SCOPED (2026-06-21 #3)
**PHASE 1 SHIPPED (commit deb0257, md5 c4e969):** `native_map_injection` default → **false** → overlay is the SOLE map out of the box (gate already clean at dllmain.cpp:201). Native path stays behind the toggle as fallback. (⚠️ a user INI with the key set explicitly overrides the default.)
**PHASE 2a DONE (commit 64c0fff, md5 e0e3d5, build green):** removed `apply_map_logic` + all native param-patcher statics from goblin_logic.cpp (SetSecondaryFlags/GetMapFragment/GetIconFlag/HideOnCompletion/SetupOverworldERR/Dungeon/Camps/Merchants/kRedSkullIconId/HasException/goblinIcons ranges) + the dllmain call/wrapper + hpp decl. **goblin_logic.cpp now KEEPS ONLY the SHARED helpers** (map_fragment_flag, cluster_region_label, area_region_label, GetMapFlagFromTile) — it was MIXED, NOT a whole-file delete (investigator was wrong on that). ⚠️ `register_fragment_gated_row` + `is_ashen_capital_row` are now uncalled (dead, exported → no warning) but stay until 2b; `g_ashen_rows` still used by inject.
**PHASE 2b PROGRESS (2026-06-21 #3) — native map FUNCTIONALLY GONE, ~1000 lines deleted, all building+committed:**
- 2b-1 (commit eb750bc): dllmain decoupled — removed init_inject_entries + init_live_loot steps + the 4 native eviction refreshes (fragment/royal/quest-npc/cluster-depletion) from the refresh loop; eviction loop keeps only refresh_category_census + refresh_quest_finishable (shared). Native injection NO LONGER RUNS.
- 2b-2 (commit 68e02bf, -777 lines): deleted `inject_map_entries` body. KEPT find_world_map_point_param_res_cap + world_map_param_ready (overlay init probe uses them).
- **2b-3 (commit 118ffde, branch cleanup/phase2b-native-removal, -398 lines, build GREEN, NOT in-game — pure deletion, no behavior change since these were already no-ops):** deleted ALL the zero-caller dead fns + their exclusively-owned globals — `set_param_injection_active`/`is_param_injection_active` + param-swap globals (g_file_ptr_ref/size_ref, g_vanilla/expanded_param_file/size, g_param_injection_active); `is_ashen_capital_row`+g_ashen_rows; `register_fragment_gated_row`/`refresh_fragment_eviction`+FragGatedRow/g_frag_rows; `refresh_royal_eviction`+g_royal_rows; `refresh_quest_npc_eviction`+QuestRow/g_quest_rows+`lookup_quest_gate`; `refresh_cluster_depletion`; `refresh_loot_from_itemlot`+LotBackedRow/g_lot_backed_rows; + matching hpp decls. Proven dead because their source vectors are never push_back'd anymore (inject removed). **KEPT (still live):** orp_flag_set, refresh_quest_finishable, refresh_category_census, g_section_rows, g_injected_row_ptrs+injected_row_ptrs() (read by goblin_messages.cpp:1026), g_lot_backed_set (read by apply_flag_or_pairs), g_clusters/g_cluster_members.
**2b STEP-2 CORE DONE → PR #8** (base docs/worldmap-projection-re-brief, branch cleanup/phase2b-native-removal, build GREEN, **✅ overlay VERIFIED IN-GAME 2026-06-22** — deployed DLL md5 3a2c32fe to dll/offline; overlay map/toggles/clustering/census all still work after −854 lines, confirming the native paths were inert; no behavior change). 3 commits on top of 2b-3/118ffde: **4785e55** gutted `menu_auto_toggle_loop` (removed apply_master/section/category_visibility + replan_clusters + apply_cluster_expanded/debug + refresh_world_map_icons calls; KEPT master-banner/section-persist/toast/Save/reset; deleted those 6 static fns + cluster_should_hide, −429). **4ee82be** simplified `refresh_category_census` → always worldmap::refresh_overlay_census; deleted SectionRow/g_section_rows + ClusterRow/ClusterMember/ClusterMemberRef/g_clusters/g_cluster_members + g_clustering_active/getter/hpp-decl (−27). KEY SAFETY PROOF: overlay visibility is 100% via the `goblin::ui::` getter facade reading atomics that F1 setters write DIRECTLY (grace_layer/map_entry_layer→category_visible+section_visible; map_renderer→clustering_enabled/category_clustered; goblin_overlay:540 skips render_markers when !icons_enabled) — native apply_* path was redundant. KEPT: is_section_hidden_ptr (goblin_collected.cpp:949 caller, now always-empty=correct), g_cluster_debug + cluster dirty atomics (live F1 config), FUN_140a82a80 hook.
**ASHEN/ROYAL DIFFERENTIATION (overlay): Ashen=area35 + post-burn-Leyndell(11,gx5,gz0)/Chapel(19) gated SHOW-when-StoryErdtreeOnFire(118) via `Marker.secondary_flag` (secondary_story_flag, map_entry_layer.cpp) + render gate `if(secondary_flag && !read_event_flag)continue` (map_renderer.cpp). The INVERSE (hide Royal area11 post-burn) was MISSING — native refresh_royal_eviction did it but only on native rows (dead in overlay-only, deleted 118ffde). CLOSED on PR #8 (commit abb1818, build GREEN, **✅ VERIFIED IN-GAME 2026-06-22**: pre-burn Royal shown/Ashen hidden, post-burn Royal vanishes + Ashen appears, no overlap): new `Marker.hide_when_flag` + `hide_when_story_flag(area,gx,gz)` = 118 for area11 EXCEPT (gx5,gz0) [mutually-exclusive w/ secondary] + render gate `if(hide_when_flag && read_event_flag)continue` in BOTH worldmap+minimap loops. VERIFY in-game: pre-burn Leyndell=Royal markers shown/Ashen hidden; post-burn=Royal markers vanish, Ashen(area35) appears, no overlap. Graces unaffected (separate layer).
**REMAINING 2b TAIL — PARTLY DONE (commit ce75bef, 2026-06-22 #5, build GREEN + deployed, no behavior change = all 0-consumer):** removed 7 dead flags — `project_dungeons`, `patch_{overworld_boss,dungeon_boss,camp,merchant}_icons`, `redify_dungeon_icons`, `hide_dungeon_icons_on_clear` — from goblin_config.hpp decls + schema var-defs + B()/BE() entries; collapsed the "ERR Markers" schema section to just `redify_boss_icons` (overlay-drawn, kept). Atlas-gen B()-macro risk = NON-issue: `generate_overlay_icons.py:parse_key_to_var` regexes B() entries but drops non-category keys, so removing misc flags can't break it. Obsolete keys in existing user inis auto-comment via load_config migration. **STILL DEFERRED (have live consumers / do last):** `live_loot_*` — entangled (shared def line + MFG_LL_DEF + MFG_PROFILE_VANILLA gating); `live_loot_labels` is LIVE (goblin_messages.cpp:726 = full-FMG-copy for randomizer relabel) while flags/icons are now dead (their native consumers were deleted in 2b-3) → needs a focused decision (make labels unconditional, or keep as the lone live_loot flag). `live_refresh_world_map` — flag gates install_live_refresh_hook (goblin_inject.cpp:1674); KEEP the FUN_140a82a80 hook for Plan B, so drop needs re-gating. `native_map_injection` toggle LAST (2 real sites: dllmain.cpp:239 + goblin_overlay.cpp:1338). KEEP project_dungeon_row_to_overworld + require_map_fragments/anonymousLoot/redifyBossIcons.
**(historical, pre-2b-3) STILL DEAD-CODE TO DELETE (interleaved with SHARED helpers → careful per-fn, do FRESH; current line #s after the -777 shift):** `replan_clusters`(1370, ~300L) + its menu-loop call(3161); the eviction/loot fns `refresh_fragment_eviction`(2493)/`refresh_royal_eviction`(2532)/`refresh_quest_npc_eviction`(2556)/`refresh_cluster_depletion`(2644)/`refresh_loot_from_itemlot`(2929); dead `is_ashen_capital_row`(2434)/`register_fragment_gated_row`(2485) — ⚠️ `orp_flag_set`(~2456) is SHARED and INTERLEAVED between is_ashen and register → NO bulk delete; `set_param_injection_active`(1934) + param-swap globals(g_file_ptr_ref/g_expanded_param_file/g_vanilla_param_file @152-156); native globals g_section_rows/g_lot_backed_rows/g_ashen_rows/g_royal_rows/g_injected_row_ptrs(163). **`menu_auto_toggle_loop`(3029) = SHARED watcher, MASSIVELY native (apply_master/section/category_visibility + apply_cluster_expanded/debug + replan_clusters + refresh_world_map_icons all mutate the live WorldMapPointParam blob — DEAD since overlay reads atomics directly)** → must surgically strip the native blob-mutation, KEEP persistence (persist_settings/save_section_states/reset) + toast; ⚠️ VERIFY F1 sets g_category_visible/g_section_visible atomics DIRECTLY (renderer reads those) before removing the watcher's apply path. Then simplify `refresh_category_census`(2730) to drop the g_section_rows native branch (always overlay). Then dead config flags (project_dungeons, live_loot_*, live_refresh_world_map, redifyDungeonIcons, hideDungeonIconsOnClear, patch*Icons — ⚠️ atlas generator parses B() macros). Remove native_map_injection toggle LAST. KEEP project_dungeon_row_to_overworld + FUN_140a82a80 live-refresh hook (Plan B).
**PHASE 2b ORIGINAL SCOPE (the big ~1000-1500-line chunk in goblin_inject.cpp):** inject_map_entries(1693, ~700L) + replan_clusters(1370) + find_world_map_point_param_res_cap(1248)/find_param_res_cap_by_name(2496) + the param-swap machinery (g_file_ptr_ref/g_expanded_param_file/g_vanilla_param_file, set_param_injection_active ~2700, swap @2394/2724) + the toggle call in menu_auto_toggle_loop + native globals (g_section_rows/g_lot_backed_rows/g_ashen_rows/g_royal_rows/g_injected_row_ptrs) + native refresh fns (refresh_loot_from_itemlot, refresh_*_eviction) + dead helpers (register_fragment_gated_row, is_ashen_capital_row, area_region_label) + **simplify refresh_category_census to ALWAYS use the overlay path** (drop the g_section_rows native branch) + remove dead config flags (project_dungeons, live_loot_*, live_refresh_world_map, redifyDungeonIcons, hideDungeonIconsOnClear, patch*Icons — ⚠️ careful: the atlas generator parses B() macros; ⚠️ KEEP the FUN_140a82a80 live-refresh HOOK for Plan B even though live_refresh_world_map flag goes) + remove the native_map_injection toggle LAST. KEEP project_dungeon_row_to_overworld (SHARED). Do sub-step-by-build.
**PHASE 2 (original investigator map):**
- REMOVE (native-only): `inject_map_entries` (goblin_inject.cpp:1693), `replan_clusters` (1370), `find_world_map_point_param_res_cap` (1248), `find_param_res_cap_by_name` (2496), `register_fragment_gated_row` (267), `install_live_refresh_hook`+`wm_build_detour` (2870/2859), the param-swap machinery (g_file_ptr_ref/g_expanded_param_file/g_vanilla_param_file @152-156, swap @2394/2724/2729); **ALL of `goblin_logic.cpp`** (apply_map_logic:230 + SetupOverworldERR/Dungeon/Camps/Merchants/GetMapFragment/HideOnCompletion/SetSecondaryFlags — native param patchers). Globals: g_injected_row_ptrs, g_lot_backed_rows, g_ashen_rows, g_royal_rows, g_section_rows. dllmain: delete the gated branch lines 201-205 + the init wrappers (init_inject_entries, init_apply_map_logic, init_live_loot, init_live_refresh).
- KEEP (SHARED, overlay needs — all in goblin_inject.cpp): marker_world_pos(1135), marker_cluster_key(623), grace_anchor_world(643), get_player_map_pos(936), get_player_world_pos(878), marker_group_from(79 inline), marker_fragment_flag(1123), marker_fogged(1099), resolve_loot_flag(3616), category_visible/section_visible/clustering_enabled/global_threshold/category_clustered(288-330), set/category_census + category_total/remaining(3557-3567), read_event_flag, capture_live_graces(112)/live_graces(116), refresh_category_census(3549 → delegates to worldmap::refresh_overlay_census), refresh_quest_finishable(3390), seed_runtime_gates. NOTE project_dungeon_row_to_overworld is SHARED (marker_world_pos + get_player_map_pos call it) — KEEP despite project_dungeons flag removal.
- DEAD CONFIG FLAGS to drop: project_dungeons, live_loot_flags/labels/icons, live_refresh_world_map, redifyDungeonIcons, hideDungeonIconsOnClear, patch{Overworld,Dungeon}BossIcons/patchCampIcons/patchMerchantIcons. KEEP: require_map_fragments, anonymous_loot, redifyBossIcons (all overlay-used). native_map_injection itself → remove the toggle last.
- both-mode refresh fns (refresh_loot_from_itemlot, refresh_*_eviction) currently no-op when their globals are empty → removing the globals means removing those fns too.

## ✨ POLISH / UI UPGRADES PASS (endgame, started 2026-06-21 #3)
**DONE (commit bb14fb0):** (1) F1 has independent checkboxes for the 3 debug views (cluster_debug_radius=distance rings, debug_cluster_anchors=pile→member lines, debug_region_volumes=region names red/cyan) — split so they don't drown each other. (2) **F1 TOPMOST:** overlay markers + minimap moved to ImGui BACKGROUND draw list (above game map, BELOW the F1 window) so F1 isn't covered by our markers (was GetForegroundDrawList → GetBackgroundDrawList in render_markers + draw_minimap). (3) minimap HIDES while the full world map is open (`world_map_open()` gate). (4) minimap POSITION settings: minimap_anchor_right/bottom + minimap_offset_x/y (config + F1 sliders) on top of zoom/radius/opacity.
**POLISH BACKLOG (UI/UX, gather more here):** region-name FMG resolution fix (re-home or right bank — underground textIds unresolved, see above); Chapel cluster mis-placement (m19 anchor); ER-native styling on overlay icons (frame/hover/discovered shading — the PRE-ENDGAME ER-STYLE item); element culling overlay-vs-overlay density; minimap heading-up (needs player rotation RE) + circular clip + fade-by-distance; expose remaining ini-only configs in F1; cluster pile name labels via loc_pname (loc_pname now from MapNameOverride); tooltip ER design (Plan A). The 6 by-design circle-fallback categories (talismans/whetblades/utilities/throwables/world-maps/quest-NPC) — give icons (regen atlas, needs FFDEC) — see [[overlay-icon-atlas]].

## 🏁 ENDGAME CHECKLIST (added 2026-06-21, do near the end — after overlay reaches parity)
The overlay (src/worldmap/) is the future map; legacy native-injection (goblin_inject) is being replaced via the `native_map_injection=false` toggle. Before flipping it the DEFAULT + removing legacy:
1. **PARITY CHECK F1 ↔ init settings** — audit that every F1 menu control + its startup/init seeding agree (no setting that the menu shows one way but boots another). seed_runtime_gates() (overlay-only) must match what F1 reads/writes for ALL gates (category/section visible, cluster opt-in, threshold, master, scales). Catch any menu-vs-init inconsistency.
2. **PORT remaining legacy features** to overlay (cluster NAME labels via loc_pname, tooltips, census badges, collected/cleared graying, region labels, quest-NPC gating) → reach parity.
3. **REMOVE the old native map** once everything proven: flip native_map_injection default → false (or remove the toggle + the WorldMapPointParam injection + dependent legacy code: inject_map_entries, g_section_rows, legacy replan_clusters, apply_map_logic, map_logic patches). The overlay becomes the sole map.
4. **BAKED → RUNTIME GETTERS (kill data drift on mod/ER updates).** Replace baked snapshots with LIVE reads so the mod self-adapts to ER patches + other mods (see [[live-param-vs-baked-data]]). Targets: MAP_ENTRIES (9296 baked rows), GRACE_ANCHORS (444 baked), generated category/icon/quest data, region anchors. Read params live (SoloParamRepository), FMG live (MsgRepository), hybrid for MSB/EMEVD. This ALSO resolves EXISTING drifts (transitive-conv misprojection, anomalies) at the source rather than patching baked data. Big effort; do as the final hardening.

## 🆕 PRE-ENDGAME PASS (user 2026-06-21, do BEFORE flipping native_map_injection default→false)
Two related visual-parity items, "probably lots of RE":
1. **ELEMENT CULLING** — overlay ImGui icons currently FIGHT with native ER icons (double-draw/overlap while native injection still on, AND overlay markers overlapping each other). Cull: (a) overlay-vs-native (don't draw overlay marker where ER already draws one, or inverse), (b) overlay-vs-overlay density. Native side already had de-clutter/pooling; replicate overlay-side. Likely RE the ER icon-cull/occlusion logic (which markers ER hides at a given zoom/density) to MATCH it.
2. **ER STYLE on ImGui icons** — make overlay icons LOOK native (vanilla ER map-icon styling: frame/border, hover-highlight, selected glow, discovered/undiscovered shading, drop-shadow). Beyond the atlas bitmap = the chrome/states ER draws around each icon. RE the ER icon render states.
Slots between PARITY PORTS and ENDGAME REMOVE (can't decommission native map until overlay matches functionally AND visually).

## 📍 WHERE WE ARE (2026-06-21, latest first)
- **2 minor underground DATA issues spotted (user, defer):** (1) a few graces from DLC/overworld LEAK onto the underground page (visible on the UG map though they belong elsewhere) = cross-page data mismatch (group classification or baked grace area wrong for a handful) — note for later. (2) Deeproot Depths graces slightly OFFSET = baking bug (the per-page converter), low priority. Both cosmetic, not blocking.
- **PLAYER-POS underground = INPUT-FRAME bug (not axis/projection), proven in-game** → see [[overlay-pending-re]] #1. Grace pipeline correct underground (cyan-anchor debug proved it); player MapId-tile+local is the wrong frame; pursuing the cursor-provider RE (§3). Overworld player ≈ correct. Added shared helper `goblin::marker_group_from(orig_area, projected_area)` (commit 69527c1, de-dup'd grace_layer + map_entry_layer page-gate).
- **REDIFY BOSS ICONS ported to overlay (commit 9022ea9, md5 bf56…, NOT in-game verified).** `redify_boss_icons` → draw_marker tints WorldBosses markers red (IM_COL32(255,70,70) on the atlas; red circle fallback) = overlay port of legacy red-skull iconId 374. One WorldBosses category covers overworld + dungeon bosses → both redden. Collected/cleared graying takes precedence (dead boss grays + checkmark). NEW F1 checkbox "Red boss markers" (live, persists). `redify_dungeon_icons` NOT ported — overlay projects dungeon CONTENTS, no separate dungeon-entrance marker/category to redden (native-only). **VERIFY: F1 toggle → boss icons turn red; killed boss still grays.**
- **ENDGAME CHECKLIST #1 (F1 ↔ init parity audit) DONE = PASS, zero mismatch (2026-06-21).** Audited F1 menu (goblin_overlay.cpp) ↔ seed_runtime_gates (goblin_inject.cpp:1680) ↔ persist_settings (:3052) ↔ ui:: getters/setters. Two consistent state types: runtime atomics (section/category/cluster/icons — seeded from config at boot, flipped live by F1 via the SAME atomic, re-serialized on Save) and config-direct (fragments/graying/region-labels/scales/distance-adaptive/quest-aware — F1 + renderer both read config::X). Threshold setters clamp ≥1 (no 0-cluster blowout). **3 non-bug notes:** (a) ⚠️ **prologue/Chapel blanking is GONE from the code** — `chapelGate`/`chapel_gate`/`get_player_raw_area`/`GROUP_CHAPEL` ALL absent (grep empty); the whole chapel/area-19/flag-120 saga below is STALE (reverted/superseded by the projection re-bake refactor). Prologue spoiler (full map at Chapel of Anticipation game start) likely REGRESSED — re-add is a parity gap if wanted. (b) ini-only configs with NO F1 control: nativeMapInjection, overlayMarkersProto, projectDungeons, liveLoot*, anonymousLoot → expose in F1 before removing the native path. (c) duplicate seed sites: seed_runtime_gates + inject_map_entries both seed g_icons_user_disabled/g_clusters_expanded (refactor candidate). 
- **DEBUG SCAFFOLDING STRIPPED (commit 0265a27, md5 cecc…).** Removed the YELLOWDOT*/HUD/dots/MAPPOINT_MGR_BUILDER sig/rpm probe/debug_map_pos_raw added during the underground-player-pos hunt; kept get_player_map_pos conv_underground=true + overworld distance-adaptive (overworld-gated). No more [SIG] MULTI noise. Underground player-pos handed to the runtime-CT RE prompt (see [[overlay-pending-re]] #1).
- **LOOT-FLAG OVER-REPORT (RE BLOCKER #2) PRIMARY FIX SHIPPED (commit 204b62b, md5 06f2…, NOT in-game verified).** RE findings (commit ca7f4e9, docs/re/windows_collected_loot_flag_re_findings.md) answered: ER temp/event-local flags (≥2³⁰) + `-1` read false FOREVER (their group never in the persistent manager) → repeatable loot (golden runes/crafting/consumables) over-counts as remaining. FIX: `resolve_loot_flag` returns 0 for resolved `==0xFFFFFFFF || >=0x40000000` → census excludes (if(!flag)continue), no graying (marker_done short-circuit). Offsets were CORRECT (not the bug). **RESIDUAL: unique-item cohort (armaments/talismans) still reads unset → wrong field per row; needs the RE field-dump diagnostic on a 100% save (per-slot getItemFlagId0N vs slot-1, or ERR baked/live mismatch) → see [[overlay-pending-re]] #2.** **VERIFY in-game (100% save): Golden Runes/Crafting/Consumables badges no longer stuck full (now excluded, no badge or lower total); armaments/talismans may still over-report until residual diag done.**
- **PARITY-GAP AUDIT (legacy native vs overlay, 2026-06-21).** Remaining UNPORTED legacy map features (all in src/goblin_logic.cpp): MISSING = SetSecondaryFlags (textEnableFlag2 post-event story flags: Erdtree-on-fire/CharmBroken/SealingTreeBurnt), HideOnCompletion (text-hide on boss kill — overlay GRAYS instead, arguably better), redifyBossIcons/redifyDungeonIcons (iconId→374 red skull cosmetic), hideDungeonIconsOnClear (overlay grays instead), Ashen Capital story gating (LeyndellAshen on StoryErdtreeOnFire @goblin_logic.cpp:251), ExceptionList per-paramId fragment overrides (overlay uses real fog now → mostly moot), SetupCampsERR/SetupMerchants (special textId2=5000/textId4=8800 setup — likely already covered by baked MAP_ENTRIES categories, verify). N-A = patch*Icons config gates (overlay always gates). PORTED = err_hide_bosses (via MapEntryLayer::visible/category_visible). **Most are cosmetic (redify) or superseded-by-better (graying vs hide); the only real behavioral gap = Ashen Capital + secondary story flags (post-event icon swaps). Low priority.**
- **QUEST-NPC QUEST-AWARE GATING PORTED to overlay (commit bbb7177, built+deployed md5 48de…, NOT in-game verified).** A WorldQuestNPC marker now hidden on the overlay while its questline INACTIVE (NONE of its quest-active flags set), mirrors legacy `refresh_quest_npc_eviction` (goblin_inject.cpp:3340, which parks areaNo→99). Opt-in via EXISTING `config::questNpcQuestAware` (default false) + EXISTING F1 checkbox "Quest-aware NPCs (legacy/unfinished)" (goblin_overlay.cpp:769) — no new config. NEW `quest_npc_gated_out(m)` in map_renderer.cpp anon ns: gates ONLY `m.category==(int)Category::WorldQuestNPC`; joins marker→gate by `name_id == QuestGate.nameId` (linear scan over `goblin::generated::QUEST_GATES[]`, ~34 rows, src/generated/goblin_quest_gates.cpp; struct `{uint32_t nameId; uint32_t flags[4]}`); shows if ANY gate flag set (read_event_flag), hides if gate found + none set, ALWAYS shows if no gate (legacy registered only gated rows). Cold-API safety: `!read_event_flag(6001)` → show as-is (never blank all quest NPCs on map open before flag mgr warm). Checked in render loop AFTER discover-grace gate, BEFORE projection → applies to clustered + exact. Includes added: goblin_quest_gates.hpp + goblin_map_data.hpp (Category enum). **VERIFY in-game: enable Quest-aware NPCs in F1 → quest-NPC markers for inactive questlines vanish, appear when quest active; NPCs w/o a gate always show; toggle off → all show.** Note: QUEST_GATES baked from MIT EldenRingQuestLog (34 NPCs) → some flags may be wrong/shared (legacy caveat). Uses BAKED gate data → endgame baked→runtime out of scope here.
- **REGION LABELS PORTED to overlay (built+deployed md5 8170…, NOT in-game verified).** Major-region names (Limgrave, Caelid, Liurnia, Altus Plateau, Mountaintops, + UG Ainsel/Deeproot/Siofra + DLC Realm of Shadow) drawn on the open map page, BENEATH markers. Source = `goblin::generated::MAJOR_REGION_ANCHORS[]` (9 rows, src/generated/goblin_major_regions.cpp): `{area, wx, wz, label_id, name}`. NEW `draw_region_labels()` in map_renderer.cpp anon ns: per anchor, `region_in_group(area,open_grp)` filter (grp0 base-OW=area 10|60, grp1 base-UG=area 12, grp2 DLC-OW=area 61, grp3 DLC-UG=none), then world→mapspace (exact converter, dlc_ug false for all anchors) → project_screen → cull off-screen → big shadowed gold AddText (font×1.6×uiScale, IM_COL32(238,226,188,205)+dark shadow), centred on anchor. Called in render_markers BEFORE the marker loop (under markers/piles). NEW config `showRegionLabels` (default true): hpp:41, schema def + B("show_region_labels") entry, F1 checkbox after collected-graying ("Show region labels..."). Used `name` string directly (NOT label_id/FMG lookup — string is baked in the anchor). Added `<cfloat>` for FLT_MAX. **VERIFY in-game: open OW map → Limgrave/Caelid/etc names appear under icons; UG/DLC pages show their region names; F1 toggle hides them + persists on Save. Check placement/scale not too big/small.** Note: uses BAKED anchor strings → endgame baked→runtime would re-home to live WorldMapPlaceNameParam.
- **DISTANCE-ADAPTIVE = OVERWORLD-ONLY (underground player pos unavailable — PROVEN) (commits 4792410 gate + 5a97336 F1; built+deployed md5 8308…).** [PLAYER-DIAG] on a Mimic-Tear-grace underground test PROVED the underground player position is unobtainable with current RE: `get_player_world_pos` → NaN (physics chain B null underground, candidate A returns -nan), `get_player_map_pos` → coarse garbage origin (area12 tile (2,0) local ~(7,0), raw≈(519,0)) = NOT the real location. So distance-adaptive can't gauge underground distance. RESOLUTION: gated distance-adaptive to overworld pages only (`!(open_grp&1)`); underground → normal threshold + per-category clustering. Removed the dead underground tab/tile-euclid branch + player reprojection + [PLAYER-DIAG] + the temporary spdlog include. Debug rings/player-marker now overworld-only (where valid). F1 tooltip notes overworld-only. helpers player_map_tab/grace_anchor_tab/grace_anchor_tile kept (unused now, for a future underground-player-pos RE). **Underground distance-adaptive needs WorldChrMan underground-player-pos RE (BLOCKED, see [[mapforgoblins-map-open-freeze]]) — deferred.** Distance-adaptive overworld WORKS (rings + ramp verified earlier). 
- **REAL UNDERGROUND DISTANCE-ADAPTIVE RADIUS (commit d16ba90, built+deployed md5 b2e6ae0; NOT in-game verified).** Viz (4c46423) revealed underground = all piles `thr4` except player's sub-page (binary per-tab, no radius). FIX: within the player's SUB-PAGE (tab) use TILE distance (player tile + grace tile via new grace_anchor_tile(key); reliable — the underground float is garbage) for a real near→far ramp; other sub-pages stay base_thr (different layers at same X/Z merge under Euclid). Debug rings now draw on BOTH pages (was overworld-only) so the zone is visible underground. NOTE default near=1/far=2 tiles = very tight underground → user tunes Near/Far radius sliders (viz shows it live). `thr{N}` debug label = the pile's chosen threshold (near_thr=detail vs base_thr=4=clustered). player_map_tab (tile-based) from prior commit was correct; the remaining gap was the binary-vs-radius within the tab.
- **DISTANCE-ZONE DEBUG VIZ + UNDERGROUND PLAYER-TAB FIX + CENSUS 100% DATA (commit 4c46423, built+deployed md5 1e393e6; NOT in-game verified).** (a) **Debug viz** config `cluster_debug_radius` + F1 "DEBUG: show distance zones": draws player marker (`A{area} T{tab}`), near(green)/far(orange) rings overworld, per-pile `thrN` (chosen threshold) → see the distance/tab decision to pin the underground bug. (b) **Underground player tab fix:** was `grace_tab_id` (uses garbage underground float) → now `goblin::player_map_tab()` = reliable MapId tile + tab_for_tile (like native). (c) **CENSUS 100%-SAVE [CENSUS-UNSET] LOG ANALYSIS (the #2 detection bug):** on a 100% save, MANY categories massively UNSET (Armaments 247/276, Talismans 111/139, Crafting 538/542, GoldenRunes 219/223) while OTHERS work (CrystalTears 35/38 set, Bosses 166/216 set). Unset flags = mix of 8-digit getItemFlagId AND **10-digit >2³⁰ (2048447800, 2050457730) = TEMP/event-local flags** (non-persistent → read_event_flag always false). → `resolve_loot_flag` (@0x80 lot-wide) returns wrong/temp flags for many lots. **#2 ROOT = loot flag mapping (resolve returns lot-wide @0x80 which is wrong for many; real pickup flag may be per-slot getItemFlagId01..08, OR flags ≥2³⁰ must be excluded/handled).** Needs dedicated RE — NOT a blind fix. Categories that work (CrystalTears/Bosses) use proper persistent flags. **NEXT for #2: RE the correct per-item pickup flag (per-slot vs lot-wide; handle temp ≥2³⁰).** This is the core collected/census correctness blocker.
- **DISTANCE-ADAPTIVE UNDERGROUND FIX + CENSUS DETECTION BUG (commits 294346d, 6994c63; built+deployed md5 db8e135; NOT in-game verified).** TWO issues from live test. **#1 distance-adaptive worked overworld, NOT underground.** First gated euclid to overworld only (294346d). Then user wanted it WORKING underground → wired the native sub-page (tabId) gradient (6994c63): underground has no usable euclid (non-256 tiles, garbage player float), so player's sub-page = near-detail (near_thr), other sub-pages = base_thr. Added `goblin::grace_anchor_tab(key)` (GRACE_ANCHORS[key].tab_id); player tab via grace_tab_id at player tile. map_renderer draw_clusters: overworld page → euclid ramp; underground page → tab gradient (gated `overworld_page = !(open_grp&1)`). VERIFY underground: same sub-page as player = individual, other sub-pages = clustered. **#2 CENSUS OVER-COUNTS ON 100% SAVE — DEEPER THAN COUNT (user: even items that SHOULD be gray aren't gray AND counted existing).** So the COLLECTED DETECTION itself fails for many items (graying + census share the predicate: collected_flag=resolve_loot_flag → read_event_flag). orp_flag_set is FINE (calls game IS_EVENT_FLAG, any range; golden rune 10007760 worked) → the RESOLVED FLAG VALUE is wrong for many items. Hypothesis: resolve_loot_flag returns lot-wide @0x80 (or baked fallback for multi-item lots) which isn't the real per-item pickup flag for many lots. **DEBUG shipped: `[CENSUS-UNSET]` one-shot logs per-category a sample of still-UNSET collect/clear flags — on a 100% save these = the wrong flags to investigate.** NEXT: user runs on 100% save, shares [CENSUS-UNSET] lines → cross-ref flag ids to find the correct flag source (maybe per-slot getItemFlagId, or a different param). This is the core blocker for collected-graying/census correctness.
- **CLUSTERING = READABILITY TOOL NOW (no lag); DISTANCE-ADAPTIVE WIRED INTO OVERLAY + F1 REFRAMED (commit d3f9d32, built+deployed md5 45bb07e; NOT in-game verified).** User: ImGui map = zero freeze, so clustering no longer fights lag → it's a DECLUTTER tool; keep it + clean F1. Decided KEEP + reframe. **Distance-adaptive is its own mode** (user: "feature à part, override les settings cluster per-category, clustering par distance"). It was NATIVE-replan only; now WIRED in map_renderer draw_clusters: per-location threshold ramps clusterNearThreshold (detail near player) → clusterThreshold (far/clustered) over near→far radius, EUCLID overworld pages (area 60/61) same page as player, via live get_player_map_pos; OVERRIDES per-category opt-in (eligibility check uses `dist_adaptive || category_clustered` → every category clusters by distance). Underground/cross-page = flat base_thr. F1 reframed: enable/distance-adaptive tooltips reworded (declutter not lag; note the override), removed overlay-dead controls (`show cluster bubbles` cluster_debug + `clustering_active()` status — native-only). Overlay clustering wiring confirmed: enable + global_threshold + category_clustered + (now) distance-adaptive; NOT wired before = distance-adaptive/presets-adaptive/bubbles. **VERIFY: distance-adaptive on → near player individual, far clustered, ignores per-cat checkboxes.**
- **MARKER DEDUP BY SHARED FLAG REVERTED (d71d19f shipped, REVERTED by user commit 9ab2c01 — "beaucoup trop agressif").** Dedup hid ~53 fortune locations (1 marker/flag) → too aggressive. REVERTED. So all markers show again; Fortunes count stays distinct-flag (~X/8 from refresh_overlay_census, commit 4e1df37, NOT reverted) but the map shows all 61 fortune markers + over-grays on pickup (inherent ERR shared-flag, accepted). Shared-flag is ERR data semantics, runtime can't fix (proven [FLAG-SRC]: all live@0x80, flag60=0). **DON'T re-attempt marker dedup.** **STILL LIVE (fffdcc4, NOT reverted):** `ParamTableSequence::try_get` (non-throwing) silences `[error] Param row ItemLotParam_enemy[X] not found` spam (LotReader probes lots ERR removed) → quiet baked fallback.
- **FORTUNES SHARED-FLAG = ERR DATA SEMANTICS, RUNTIME CANNOT FIX — PROVEN (user endgame Q 2026-06-21, [FLAG-SRC] probe, now removed commit 038b81c).** Q: fortunes baked? runtime fix the over-count? PROVEN ANSWER via in-game [FLAG-SRC] log: ALL 61 fortune lots resolve from LIVE ItemLotParam @0x80 (lot-wide getItemFlagId), src=live@0x80 for all (NOT baked-fallback). distinct flag80 = **8** (60320 shared across ~54 fortunes + 7 unique like 12027995/530630). Per-slot **flag60 (getItemFlagId01 @0x60) = 0 for ALL** (item2=0 → single-item lots) → NO per-fortune flag exists anywhere. So ERR GENUINELY shares one obtain flag (60320) across ~54 fortunes — looting one sets 60320 → the game marks all 54 obtained (the native map would gray all 54 too). **Runtime read does NOT help (we already read the live authoritative @0x80; there's no finer flag).** The endgame baked→runtime ([[live-param-vs-baked-data]]) fixes DRIFT, not this. **FINAL MITIGATION (shipped): distinct-flag census count** (Fortunes ~5/8) — best possible numerically. KNOWN residual: the MAP still over-grays (54 markers gray per pickup, inherent to the shared flag). Only real visual fix = **dedup MARKERS by flag** (show 1 fortune marker per distinct flag → ~8 markers instead of 61) — offered to user, endgame/optional, changes marker display. [[live-param-vs-baked-data]]
- **CENSUS REWIRED TO OVERLAY MARKERS + SHARED-FLAG/CLEARED-FLAG FIXES (commits 4c26c76 source-from-buckets, 6945ac0 distinct-flag diag, 4e1df37 distinct-flag count + cleared-flag; built+deployed md5 ad7a286; NOT in-game verified).** User: F1 count read native data + stuck. THE census `refresh_category_census` (goblin_inject.cpp) had a native branch (g_section_rows) + my overlay branch (recompute from MAP_ENTRIES) — a PARALLEL path that could diverge from what the overlay draws/grays. **REWIRED (4c26c76):** overlay branch now delegates to `goblin::worldmap::refresh_overlay_census()` (map_entry_layer.cpp) which walks the overlay's OWN marker buckets (g_buckets — the exact markers drawn, collected_flag resolved live at build) using the SAME predicate as marker graying, and publishes via new `goblin::ui::set_category_census(idx,total,looted)` into g_cat_total/g_cat_remaining (F1 badge reads these). Logs `[OVERLAY-CENSUS]` (full dump first publish + line per looted change). **Live-test [OVERLAY-CENSUS] revealed shared flags:** ERR shares ONE event flag across many markers (cat 41 Fortunes: 61 rows, distinct_flags=8 → 3 pickups set 3 flags each covering ~18 rows → looted=54 WRONG). **FIX (4e1df37):** flag-based categories count by DISTINCT flag (collected_flag else cleared_flag) not per-row → unique-flag cats unchanged (distinct==rows), shared-flag (Fortunes) corrected (~8 total / looted=distinct flags set). Piece/kindling (Material Nodes 1455/8flags, Ember/Rune Pieces, Kindling) KEEP per-row row_id counting (no per-item flag → distinct-flag would collapse wrongly). ALSO added cleared_flag fallback → **Spiritspring Hawks** (only clearedEventFlagId, no textDisableFlag → was collectible=0/no badge) now counted. **Spirit Springs / Stakes of Marika / Quest NPC / Graces = NO flag at all (iconId only) → genuinely not collectible → no badge BY DESIGN.** ⚠️ **KNOWN CAVEAT:** for shared-flag cats (Fortunes) the MAP still grays ~18 markers per pickup (graying is per-marker on the shared flag; the game itself can't disambiguate which fortune) → badge (distinct, e.g. 5/8) shows fewer done than grayed markers. User offered grouping if it bothers. **VERIFY: Fortunes badge ~5/8 + Hawks badge appears; loot decrements live.**
- **LIVE-LOOT FLAG + CENSUS/CLUSTER DIAGNOSIS (commits 60adbd2 live-loot resolver, c9f6abd [CENSUS-DIAG], b8a9c61 drop liveLootFlags gate; built+deployed offline md5 88d4549, NOT in-game verified — diag WAS in-game run).** User in-game: overlay census stuck "223/223" (Golden Runes never decrement) + collected loot not graying + per-category cluster checkbox "ne sert à rien" (only Visibility reaches overlay). User insight: census+cluster were built for the NATIVE map → read native data. **ROOT CAUSES (proven by [CENSUS-DIAG] one-shot log, overlay-only branch in refresh_category_census):** (1) loot collected = lot-backed; baked textDisableFlagId1 is STALE under ERR/Randomizer (loot remapped). Native resolves the live ItemLotParam getItemFlagId via refresh_loot_from_itemlot (gated config liveLootFlags) — but that only runs/populates g_lot_backed_rows in NATIVE mode. Added `goblin::resolve_loot_flag(lotId,lotType,baked)` (cached LotReader, flag@0x80 lot-wide / @0x60 single-item fallback) used by overlay census + map_entry_layer collected_flag. DIAG showed user's INI has `liveLootFlags=FALSE` → my resolver early-returned baked → still stale. FIX b8a9c61: REMOVED the liveLootFlags gate from resolve_loot_flag (that flag governs native row-REWRITE; overlay detection is read-only → must resolve live regardless). (2) cluster checkbox "dead" = DIAG showed `clustering_enabled=FALSE` at startup = GLOBAL clustering off by default (config enable_clustering=false) → whole cluster pass skipped → per-category gate moot. NOT a wiring bug: ckey_cov=100% every category (cluster_key perfect), cluster_optin reads live, render reads category_clustered correctly. → user must enable "Enable clustering" in F1; per-category then works (checked=pile, unchecked=real loc). OFFERED to flip enable_clustering default→true (pending user). **VERIFY: pick up Golden Rune → census 223→222 + grays; enable clustering in F1 → piles form + per-category uncheck pulls a category to real locations.** Diag insight: census collectible=0 (no badge) for QuestNPC/Graces/SpiritSprings/SpiritspringHawks/StakesOfMarika (no textDisableFlagId1; Graces excluded by design; Hawks use clearedEventFlagId which census doesn't check — matches native). [CENSUS-DIAG] one-shot log retained (harmless).
- **CLUSTER DEPLETION COUNT + CENSUS BADGES in overlay-only (commit 3e529d2, built+deployed offline md5 7f638a4, NOT in-game verified).** Two parity fixes. (1) **Cluster piles** showed static total member count (idxs.size()), ignoring collected progress. Now show UNCOLLECTED count (when collected_graying on) via the same marker_done predicate; fully-collected pile (remaining 0) = green-tinted disc (mirrors legacy CLUSTER_DONE icon), tooltip "<rem>/<tot> left". draw_clusters Pile struct += total; draw_cluster_glyph += depleted bool; pile_label += total (map_renderer.cpp). (2) **F1 census badges** (per-category "<rem>/<tot>" yellow/green next to each category checkbox, goblin_overlay.cpp:718) read `category_remaining/category_total` ← g_cat_total/g_cat_remaining atomics ← refresh_category_census. That fn ONLY swept g_section_rows, populated by inject_map_entries (NATIVE mode only) → read 0/-1 in overlay-only (= user's "déjà fait mais calibré pour l'ancienne map"). FIX: added overlay-only branch (g_section_rows empty) recomputing the SAME census from MAP_ENTRIES — same predicate (textDisableFlagId1 flag OR is_piece OR is_kindling collectible; taken via orp_flag_set/is_original_row_collected/kindling::is_row_collected), same is_piece classification (ReforgedRunePieces|ReforgedEmberPieces|LootMaterialNodes), is_kindling=WorldKindlingSpirits, ORIGINAL row ids (no remap without inject), WorldGraces EXCLUDED (deduped via discover_flag, not census-counted → matches native "graces total 0"). refresh_category_census ticks in dllmain safe_fragment_eviction_seh loop (runs always) + gated on note_menu_visible (F1 open within 2s) + 1s throttle + flag-6001 warm. **VERIFY in-game (overlay-only): F1 shows real rem/tot per category, decrements on pickup; cluster piles show uncollected count, turn green when fully looted.** ⚠️ overlay census still misses pieces/kindling that lack a textDisableFlag AND aren't yet in the collected/kindling tracking tables — but is_piece/is_kindling are counted by CATEGORY here so collectible total is right; only the "taken" side needs the refresh loops ticking (they do). Remaining parity ports: region labels, quest-NPC gating.
- **DISCOVERED-GRACE DEDUP (commit c312912, built+deployed offline md5 895ee87, NOT in-game verified).** Bug: discovered grace showed DOUBLE + its "undiscovered" overlay marker never cleaned up. ROOT CAUSE: vanilla WMP has NO grace rows — the GAME generates grace pins from BonfireWarpParam at runtime (= the DISCOVERED graces), ALWAYS, even in overlay-only mode (we only skip OUR injected category rows, not vanilla graces). Our overlay also drew ALL graces from the baked set → discovered ones doubled. FIX: each grace row carries its discovery flag in `textDisableFlagId1` (per-grace sequential: 71005,71006,…,71100,71101; ERR set it so the label hides once discovered). Captured into `LiveGrace.discoverFlag` (both live + baked paths in capture_live_graces), carried on grace `Marker.discover_flag` (new field, last in Marker after collected_flag), and the render loop (map_renderer.cpp, right after open_grp check) does `if (m.discover_flag && read_event_flag(m.discover_flag)) continue;` — UNCONDITIONAL (not tied to collected_graying toggle), read LIVE per-frame so it vanishes the instant the player rests. MapEntry markers leave discover_flag=0 → unaffected. Result: overlay shows ONLY undiscovered graces (helpers); discovered = native pin. User design: overlay-only + "remove overlay marker when discovered". **VERIFY in-game: undiscovered graces show as overlay markers; rest at one → its overlay marker disappears (only the native grace pin remains, no double).**
- **COLLECTED/CLEARED GRAYING ported to overlay (built+deployed offline md5 41dbeaa, NOT in-game verified).** First of the 4 open parity ports DONE. Legacy native map had NO actual graying — only BINARY hide (rune/ember/kindling collected→areaNo=99; loot textDisableFlagId→text hidden) + engine-drawn green checkmark on cleared bosses (clearedEventFlagId). Overlay does BETTER per user choice (Hybrid+F1 toggle): default = DIM+desaturate collected/cleared (~50% alpha, luma-blend) + green checkmark on cleared bosses; F1 "hide instead" → legacy hide. **Plumbing:** Marker += `row_id`(uint64, original MAP_ENTRIES id), `cleared_flag`(clearedEventFlagId), `collected_flag`(textDisableFlagId1) — appended AFTER fragment_flag (marker_layer.hpp); filled in MapEntryLayer build (map_entry_layer.cpp) ONLY (graces leave them 0 → never grayed). **Detection** (map_renderer.cpp `marker_done`): cleared_flag&&read_event_flag (boss→checkmark) ‖ collected_flag&&read_event_flag (loot) ‖ row_id&&(collected::is_original_row_collected‖kindling::is_row_collected) (rune/ember/kindling). **Render** in draw_marker: tinted AddImage / dim_color circle + draw_check. read_event_flag reads game directly (works any mode); collected/kindling::refresh tick in dllmain mod_thread loop UNCONDITIONALLY (not gated on injection) so they work overlay-only; is_original_row_collected falls back to original id when no remap (overlay-only). **Config:** `collected_graying`(default true)+`hide_collected`(default false), F1 checkboxes after Require-map-fragments. **VERIFY in-game (overlay-only mode): loot a marked item → icon dims; kill a marked boss → dims + green checkmark; toggle "hide instead" → collected vanish; rune/ember/kindling pickup → dims (needs refresh loop ticking).** ⚠️ NOT done: cluster-pile depletion graying (pile still shows full count even if all members collected — legacy had refresh_cluster_depletion; follow-up). Remaining parity ports: census badges, region labels, quest-NPC gating; then PRE-ENDGAME cull+ER-style.
- **O(1) CURSOR RESOLVE (commit 4be0602, built+deployed offline, NOT in-game verified) — supersedes the l2_tries scan.** RE brief docs/re/windows_worldmap_cursor_o1_re_prompt.md was ALREADY answered (static side: the CSMenuMan→WorldMapDialog offset is NOT statically resolvable — 471-ref hot global + factory-created dialog — must capture at runtime). Runtime capture now in hand (wmprobe.log `[MENU] cursor via CSMenuMan+0x80 → +0x10`). resolve_cursor_via_menu now SEEDS `c_off1=0x80,c_off2=0x10` → O(1) two-deref resolve from tick 1, no scan in normal op; reopens/grace-rests resolve O(1) too (field offsets stable, only pointers change); map-closed derefs fail → return 0, offsets untouched. L1/L2 scan = pure fallback, runs ONLY on a miss while map OPEN (= patch drift) → re-discovers + re-seeds; budget resets per map-open session (not lifetime). **This makes the f9c283b death-fix moot in normal op (no scan at all) while keeping a drift-safe fallback. VERIFY: map opens instantly + survives many grace-rest/reopen cycles; [MENU] re-seed lines should NOT appear on this build (only on a future ER patch).**
- **PROLOGUE GATE FINAL = player area m19 (commits 9f2c09a gate + 57b4c43 leak; built+deployed offline, NOT in-game verified). flag-120 approach ABANDONED.** In-game diag [PROLOGUE-DIAG]: past the prologue, flags 120/6010/21/22 ALL read false (f6001=true confirms the reader works) → the ERR EMEVD prologue flags are NEVER set on this build/save → flag-120 gate wrongly blanked everywhere. Switched to `get_player_raw_area()==19` (physically in Chapel m19), flag-independent, correct all saves, -1 fail-open. Re-added get_player_raw_area() (the 645d324 revert had removed it — earlier memory "still exists" was WRONG). USER CONFIRMED: gate now coherent on all maps before+after prologue. ⚠️ also blanks the rare late Four-Belfries m19 revisit (acceptable). **AREA-19 LEAK FIX (57b4c43):** the 5 area-19 Chapel rows (grace + 2 Grafted-Scion bosses + 2 items incl. White Shadow's Lure) have NO LegacyConv (area 18 does → West Limgrave) → project to map-space (~-6859,~17119) = far NW overworld corner → leaked onto every POST-prologue map (user saw them "in the Chapel"). = a real calc/classification bug, NOT prologue-only. FIX v1 dropped them (57b4c43), REPLACED by v2 (commit 97fe855, user design feedback "fix the calc + keep data, don't just disable"): (a) new config `chapel_gate` (default true) + F1 checkbox wraps the m19 blank so it's toggleable for the future baked→runtime work (ERR adds end-game Chapel content via Four Belfries → eventually want the Chapel SHOWN, not blanked); (b) area-19 rows reclassified to **GROUP_CHAPEL=4** (out of renderer open_grp 0..3) — KEPT in data, not drawn on overworld (no NW-corner leak), ready for a future m19-page handler. **PROPER calc fix (TODO/RE) = dump the m19 world→map-space converter (area 19 has NO LegacyConv, like the DLC-UG eyeball) → project area-19 markers onto a real m19 page; then chapel_gate=off shows them.** User's Q "does area==19 lose meaning with real data?": NO — its ROLE flips from "blank m19" to "the player is on the Chapel page → show its markers"; chapel_gate is the bridge (on now = blank; off later = show m19 page). Distinguishing prologue-blank from end-game-show still needs a working progression signal (flag 120 is dead here; maybe a different flag or first-visit detection). **STILL TO VERIFY (user's other 2 residuals, likely separate data-completeness, NOT the area-19 leak): "Table of Lost Grace" = Roundtable Hold = non-mappable hub → correctly absent by design; "Gilded Court graces from Leyndell" = needs a check whether they're in the baked area-11 grace data (possible gap) — deferred.**
- ~~PROLOGUE GATE RE-ADDED via flag 120 (commit faba77a~~ — SUPERSEDED by 9f2c09a, flag 120 doesn't work on this build (see above). Original note:** flag 120 built+deployed offline, NOT in-game verified).** RE findings 9d4544d5 (docs/re/windows_prologue_gate_re_findings.md): EMEVD m19 sets event flag **120** ON only on LEAVING the Chapel; every prologue event guarded by EndIf(EventFlag(120)) → `!read_event_flag(120)` == in prologue, NG+-safe, late Four-Belfries revisit (120 ON) NOT gated. IMPLEMENTED in hk_present (goblin_overlay.cpp): skip `draw_worldmap_markers` while in prologue. KEY: gated the MARKER DRAW, **not g_show** — markers render on `proto` (overlay-only / prototype), NOT the F1 menu toggle, so the findings' g_show suggestion would've only hidden the menu; F1 stays usable in the prologue. FAIL-OPEN: `past_prologue = read_event_flag(120) || !read_event_flag(6001)` — 6001 = codebase's "world ready" api-ok probe; an unresolved flag manager at early load reads false → draws rather than wrongly blank (avoids a startup blank regression). Supersedes the reverted get_player_raw_area()==19 approach (helper still exists, unused). **VERIFY: new game Chapel/Grafted-Scion prologue + open map → BLANK overlay; after leaving (Cave of Knowledge onward) → markers show; F1 menu works in prologue.**
- **🩹 "MAP DIES FOREVER" BUG = ROOT-CAUSED + FIXED (commit f9c283b, built+deployed offline, NOT in-game verified).** User: overlay map sometimes stops rendering on map reopen (esp. after a grace-rest), sometimes NEVER returns until game restart. DIAGNOSED FROM LOGS (dll/offline/logs/): wmprobe.log cursor logging STOPPED on a reopen while MapForGoblins.log [OVERLAY] render thread kept running → the PROBE thread stopped updating g_active_cursor → get_live_view() returns false → no overlay. ROOT CAUSE = `resolve_cursor_via_menu` `l2_tries` static counter: the cursor/dialog is found via the EXPENSIVE L2 (deep-deref) scan, capped at 6 — but the counter was STATIC FOR THE WHOLE SESSION, never reset. Each reopen whose dialog MOVED (grace-rest reallocates it) staled the cached chain → needed a fresh L2 search; after 6 lifetime stale events L2 was permanently disabled → cursor never re-resolved → map dead forever. FIX: reset `l2_tries=0` when the cached chain goes stale (the reopen path); never-found case still caps at 6 (no forever-scan regression). **VERIFY: grace-rest + reopen map repeatedly (>6×) → overlay markers keep showing every time.** NOTE this supersedes the earlier "user hypothesis = Chapel gate" (that was reverted and unrelated; real cause was the L2 budget).
- **FOG GATE CONFIRMED PERFECT + MERGED (commit f7cb7c7, built+deployed offline).** User in-game: fog gate exact, NO calibration needed (openTravelArea == our map-space, identity transform). Took a fragment → correct markers revealed. Wired marker_fogged() under the existing `require_map_fragments` toggle; RETIRED the experimental `fog_reveal_gate` flag + the [FOGCAL] per-piece calibration log (kept the per-layer summary). require_map_fragments now gates on the REAL fog (WorldMapPieceParam) — the MapList/marker_fragment_flag approximation is dead code (cleanup later: marker_fragment_flag, Marker.fragment_flag, map_fragment_flag, MapList can be removed). **Islands bug = FULLY FIXED.**
- **MID-SESSION RESOLUTION FIX = COMPLETE (commit fef62f6, built+deployed offline, NOT in-game verified) — supersedes the raw-poke PARTIAL.** RE agent answered the d699665 follow-up brief (their commit defda96d, docs/re/windows_midsession_resolution_swapchain_re_followup_findings.md): the raw-poke was the WRONG LAYER (only scalar dims; real corruption = stale GPU resources = swapchain back-buffers + per-output render-target arrays). Fix = call ER's COMPLETE re-apply **`FUN_1419ed440(renderMgr,W,H)`** (release all targets → uncond ResizeBuffers every output → refresh source dims from GetDesc → recreate targets) = fixes windowed+fullscreen+borderless in one call. IMPLEMENTED: `goblin::worldmap_probe::reapply_render_res()` replaces `fix_render_dims` — resolves fn by AOB **RENDER_REAPPLY_RES** (added to re_signatures.hpp registry + health check [[re-signatures-registry]]), edge-trigger (active dims vs backbuffer), render-thread guard ([mgr+0x6d0]+0x14==tid), re-entrancy guard, SEH-wrapped POD-only call helper (can't mix __try+C++EH per func). Fires from hk_present ONLY (removed the hk_resize_buffers call — re-apply calls ResizeBuffers internally → re-entrant). Still gated config `fix_midsession_resolution` (default off). **HYBRID v2 (commit e115e73): engine-call-alone was NOT sufficient in-game.** Two fixes: (1) the dims edge-trigger NEVER fires for fullscreen-doubling (findings §4: all scalar dims read the NEW value, only GPU resources stale) → added `note_resize_event()` (set from hk_resize_buffers) so hk_present fires on `pending_resize OR dims_mismatch`; guarded by g_reapply_in_progress against the nested-ResizeBuffers loop. (2) restored the scalar-dims POKE as a backstop running alongside the engine call (user's "call both" — also runs if AOB unresolved). reapply_render_res logs `engine=/poked=/pending=/mismatch=` per fire. **VERIFY in-game: enable fix_midsession_resolution → change res mid-session WINDOWED + FULLSCREEN + BORDERLESS → no zoom/doubling, no crash. DIAGNOSTIC: read MapForGoblins.log — [SIG] RENDER_REAPPLY_RES must = PASS (not MULTI/FAIL = AOB broken → engine=false always); [RENDIMS] reapply line shows what fired (engine=true means FUN_1419ed440 ran; pending/mismatch show which trigger). If still broken with engine=true → the re-apply doesn't fully cover that path (escalate: maybe needs the swapchain-recreate path or a SetFullscreenState re-toggle). Risk = VMP engine call; clang-cl may elide __except.**
  - **SCOPE SETTLED (user in-game 2026-06-21, commit b51b8bf = TODO in reapply_render_res): borderless + fullscreen-same-mode = FIXED/intended. Two WINDOWED transitions still residual (ENGINE bug, NOT our role — deferred):** (1) windowed 720p→1920x1080 = zoom still appears; (2) windowed 1920x1080→720p = DOUBLE buffer (stale 1080p buffer lingers behind the real 720p game). Both = window-resize paths where ER leaves stale GPU state a user-side hook can't cleanly own; complete cure would need a full swapchain RECREATE or forced SetFullscreenState toggle (heavy/risky, out of scope). Left as best-effort + TODO. **Mid-session resolution work = DONE for practical purposes.**
- **ISLANDS FIX (commit aafa7c2, built+deployed offline, NOT in-game verified): require_map_fragments leaked icons because we looked the fragment up on the ORIGINAL baked tile, legacy uses the PROJECTED tile.** USER measured: with 0 fragments, islands of icons still show (Divine Tower of Limgrave, Golden Rune Stormveil) while Divine Tower of LIURNIA correctly hidden → leak is by LOCATION, not item. ROOT CAUSE: legacy GetMapFragment runs AFTER inject_map_entries PROJECTED the row (dllmain order: inject@203 then apply_map_logic@204), so it keys off the PROJECTED overworld tile. A dungeon whose interior grid cell isn't enumerated in MapList (deep Stormveil m10 gridX 2-3 → MapTile(10,2) absent → 0 = "no fragment" = always shown) leaked; legacy projects it → (60,40,39) = IN MapList → WestLimgrave → gated. FIX: new `goblin::marker_fragment_flag(area,gx,gz,px,pz)` (goblin_inject.cpp) projects with conv_underground=FALSE (overworld dungeons→60; underground m12/40-43 stay native, MapList keys those separately) THEN map_fragment_flag on the result tile = replicates native GetMapFragment exactly (minus per-row ExceptionList). Both layers swapped from map_fragment_flag(original) → marker_fragment_flag(projected). **VERIFY: 0 fragments → Stormveil/Divine-Tower-Limgrave islands GONE; revealing a fragment still reveals correctly.** ⚠️ Still does NOT respect the real fog (tiles fully absent from MapList leak) — the WorldMapPieceParam fog-mask gate (FUN_1408882d0) remains the proper RE fix below.
- **PROLOGUE GATE NEEDS RE (confirmed 2026-06-21, user reverted the area==19 gate).** User measured via MapForGoblins_region.CT: prologue Chapel `dialog+0xA88`(open page id)=0 (= SAME as base overworld), view+0x370=0, view+0x374=1, dialog+0x98=1. And **prologue == late-game Chapel revisit = IDENTICAL values** → NO page/view field distinguishes them (both are m19 rendered as base page 0). So `get_player_raw_area()==19` (current approach) fires in BOTH prologue AND late-game revisit → user REVERTED it (map_renderer.cpp no longer has the gate; get_player_raw_area() helper kept). **The ONLY prologue-only discriminator = a PROGRESSION EVENT FLAG (was the prologue/Grafted-Scion done?). NEXT: RE/find that flag (read via read_event_flag), gate on `raw_area==19 && !prologueDoneFlag`.** Chapel(m19) is genuinely revisitable late-game per user.
- **SIDE BUG (under-investigation): overlay map markers + a top-left debug HUD sometimes STOP rendering mid-session (only F1 menu still works); user suspects a profile/character-change re-init bug.** Lifecycle traced so far: hk_present draws menu (draw_panel, pure ImGui) AND markers (draw_worldmap_markers→render_markers) in the SAME block gated `(g_show||proto)&&g_command_queue`; markers additionally need `icons_enabled()` (draw_worldmap_markers:507 early-returns if master off) + `get_live_view()` (probe) returning true. get_live_view keys off `g_active_cursor` (cached cursor ptr) + validates its vtable == base+CURSOR_VTABLE_RVA → stale/freed after a world reload → returns false = no markers (fail-safe). BUT the probe BG thread (100ms loop) re-runs resolve_cursor_via_menu EVERY tick (CSMenuMan→WorldMapDialog→cursor, caches OFFSETS not pointers) → SHOULD self-heal after a reload. Couldn't locate the "top-left debug HUD" in current code (only tooltip/cluster/loc-label AddText) → maybe a user-enabled diag or it IS the menu pill. **USER HYPOTHESIS 2026-06-21: the side bug WAS the Prologue Gate (`get_player_raw_area()==19`) firing in the REAL overworld (not just m19) → `return` blanked all markers.** Stays broken until restart (random trigger). User REVERTED the gate → observing if it recurs. If confirmed, the helper `get_player_raw_area()` intermittently reads area 19 while the player is in m60 (stale/transient MapId singleton read, or the resolved statics point at a wrong singleton) — a reliability bug in that helper that MUST be fixed before re-adding any prologue gate. NOTE the helper reads `*(mapid_slot)+0x2c` → area=(mid>>24)&0xff; a null/mid-update singleton would SEH→-1 (not 19), so a literal 19 = a real (wrong-context) m19 read. **ON HOLD pending recurrence.** Marker layer caches (GraceLayer.built_/MapEntryLayer g_built/s_layers) are build-ONCE statics, never invalidated on reload — secondary candidate (stale live_graces) but = stale data, not blank.
- **🌫️ FOG-OF-WAR = THE REAL require_map_fragments MECHANISM (diagnosed 2026-06-21 from user in-game test + image).** require_map_fragments works HALF: revealing a fragment reveals its icons correctly, BUT with ZERO fragments you still see ISLANDS of icons scattered in the black fog (image: markers outside the revealed rectangle). ROOT CAUSE: our `map_fragment_flag`=GetMapFlagFromTile(MapList) is an APPROXIMATION — tiles NOT in MapList return 0 → our gate (`frag && !read_event_flag(frag)`) treats 0 as "always show" → leak. Calc is IDENTICAL legacy↔overlay for the 0-cases (projection doesn't change it; MapList has both dungeon (30,gx) AND overworld (60,gx,gz) tiles but overworld coverage is SPARSE = corners/key tiles only, ~298 rows). The reason LEGACY doesn't leak: legacy sets the native row eventFlagId/textEnableFlag and lets the GAME render → the game's **FOG (CS::WorldMapPieceParam revealed-pieces mask)** hides icons on undiscovered tiles regardless of any flag. **Our overlay draws on the ImGui foreground OVER the fog → ignores it entirely.** So even a perfect MapList gate wouldn't fix it — the overlay must respect the real fog.
  - **THE REAL FIX (= user's "trouver le trigger via RE", NOT DLC-exclusive, also Limgrave start):** gate overlay markers on the GAME's actual revealed-pieces mask. RE lead ALREADY in docs/re/windows_re_live_refresh_grace_lead.md: **`FUN_1408882d0(toggleByte, areaIdx)`** walks CS::WorldMapPieceParam rows → returns the "revealed pieces" mask for an area (toggleByte≠0 → 0xffffffff=all revealed; else computes from flags/`_GetMapVariation`). So: read this mask (per area/tile) → marker on an UNrevealed piece → skip. This SUPERSEDES the MapList approximation (keep require_map_fragments as the toggle, back it by the real mask). Also resolves the DLC fog-of-war backlog item at the source. NOTE memory [[mapforgoblins-map-open-freeze]]: writing the reveal flag FAILED (ERR-remapped) but READING the computed mask is the right move.
  - **PATH A IMPLEMENTED 2026-06-21 (commit a9f2bc3, built+deployed offline, NOT in-game verified) — fog gate + CALIBRATION LOG.** RE agent answered (their commit 5542398, docs/re/windows_fog_reveal_mask_re_findings.md): **pure data, no VMP.** A WorldMapPieceParam piece is revealed ⟺ `IsEventFlag(openEventFlagId)` (`row+0x4`; 0=always shown); marker→piece = point-in-rect vs `openTravelArea L/R/T/B` (`row+0x8/0xc/0x10/0x14`, f32 map-space); rowId=areaIdx*100+pieceIdx, areaIdx∈{0=OW,1=UG,10=DLC}. IMPLEMENTED: minimal paramdef WORLD_MAP_PIECE_PARAM_ST; `goblin::marker_fogged(areaIdx,mx,my)` reads WorldMapPieceParam live (from::params, lazy build) + point-in-rect + read_event_flag; new config `fog_reveal_gate` (default false) → renderer gates on marker_fogged instead of fragment_flag (group bits→areaIdx: g&2?10:g&1). **THE OPEN UNKNOWN = coordinate calibration:** openTravelArea space vs our map-space (gU=worldX-7040, gV=-worldZ+16512) may differ in scale/offset (agent §3 said calibrate once vs a known piece). **CALIBRATION LOG [FOGCAL] shipped:** every piece {id,layer,rect,flag} + per-layer bbox + throttled sample-marker mapspace+verdict. **NEXT: user enables fog_reveal_gate+require_map_fragments in INI, opens map, reads [FOGCAL] log → compare piece-rect bbox vs marker-mapspace range per layer → derive the transform (likely affine) → apply in marker_fogged (or transform gU,gV before the test). Once calibrated+verified, make it the default backing for require_map_fragments + retire MapList/marker_fragment_flag.** Default off so it can't break the map pre-calibration.
  - **RE BRIEF WRITTEN 2026-06-21 (commit 59c4d49): docs/re/windows_fog_reveal_mask_re_prompt.md.** After the projected-tile islands fix (aafa7c2), user RE-TESTED: DIFFERENT objects still leak with 0 fragments → confirmed it's the tiles fully absent from MapList (fragment_flag 0) that need the real fog. User insight: "la native map gérait ça automatiquement" = the GAME consumes the piece-mask at render; our ImGui foreground bypasses it. Brief asks Windows agent for TWO paths: **A (preferred, maybe no function-RE)** = dump WORLD_MAP_PIECE_PARAM_ST paramdef (reveal flag + piece geometry) → read live via from::params → map tile→piece by geometry → gate on read_event_flag; **B (fallback)** = AOB FUN_1408882d0 + read the 32-bit mask at [manager+0x39c+idx*4]. **THE CRUX (both) = tile(gridX,gridZ)→piece mapping** (the one geometry we can't guess; row id = areaIdx*100+i, i 0..0x1f). NEXT: hand brief to RE agent → implement gate in map_renderer.cpp backing require_map_fragments. **STATUS: islands REDUCED by aafa7c2 (MapList-covered tiles now gated) but NOT eliminated (MapList-absent tiles still leak) — full fix needs this brief answered.**
  - GOOD: revealing a fragment reveals icons correctly (the in-MapList path works); Chapel prologue gate works (see below).
- **CHAPEL GATE SCOPE (user Q 2026-06-21):** gate = `get_player_raw_area()==19` → triggers ONLY when player is PHYSICALLY inside m19 (the Chapel map), NEVER in the overworld → overworld markers unaffected (good). BUT triggers in BOTH the prologue AND any late-game m19 revisit (same map id). User wants it NOT to apply on late-game revisit → needs a prologue-only discriminator (a progression event flag; or confirm m19 isn't normally revisitable). PENDING user decision; current = blanks m19 always.
- **CHAPEL-PROLOGUE BLANK + F1 FRAGMENT TOGGLE DONE 2026-06-21 (commit 2bf0ed3, built+deployed offline, NOT in-game verified).** User bug: at game START in the **Chapel of Anticipation** (m19, the Grafted Scion intro BEFORE the Cave of Knowledge m18 tutorial) the world map shows the whole Lands Between + our markers = spoiler. NOTE user distinguishes m19 Chapel-of-Anticipation (prologue/Grafted Scion) from m18 Cave-of-Knowledge (tutorial after) — target = m19 ONLY. **NO RE needed** — player area read via EXISTING `goblin::get_player_map_pos` infra (PLAYER_MAPID_SLOT AOB already resolved; MapId m-prefix = (mid>>24)&0xff).
  - get_player_map_pos PROJECTS dungeon→overworld (area 18 has a LEGACY_CONV {18,0,0→60,42,35} so it'd read 60, area 19 has NO conv so stays 19). Added `goblin::get_player_raw_area()` = RAW pr.area (pre-projection) to detect m19 reliably. render_markers: `if (get_player_raw_area()==19) return;` (blanks overlay; gated on player AREA not viewArea/open-page so it holds whatever page the prologue map shows; -1 unresolved=fail-open=show).
  - ALSO added F1 menu **"Require map fragments"** checkbox bound to existing config::requireMapFragments, persists via Save-to-INI (request_save serializes the B(...)-bound flag).
  - ⚠️ **VERIFY in-game:** (1) Chapel of Anticipation prologue (start new game / be at Grafted Scion) + open map → BLANK overlay (no markers). Confirm raw area==19 there; if markers still show, the prologue MapId m-prefix ≠ 19 → read actual value (add a one-shot spdlog of get_player_raw_area) and adjust the constant. (2) F1 fragment checkbox toggles live + survives Save→restart.
  - SIDE NOTE (not fixed, by-design): the 3 area-18 Chapel/Cave graces project via LEGACY_CONV onto West Limgrave overworld (grid 42,35) + area-19 grace has tab 11000 (late-game Leyndell access) — showing those on the overworld is CORRECT per user ("logique de l'afficher quand on est dans l'overworld"); only the prologue needed blanking.
- **require_map_fragments PORT DONE 2026-06-21 (commit 7816cc3, built+deployed offline slot, NOT in-game verified).** Implemented per the plan below. (1) NEW `goblin::map_fragment_flag(area,gx,gz)` in goblin_logic.{hpp,cpp} = thin wrap of static GetMapFlagFromTile(MapTile) [MapTile X=area,Y=gridX,Z=gridZ]. (2) `Marker.fragment_flag` (marker_layer.hpp, appended LAST → aggregate-init = 11th positional arg). (3) grace_layer.cpp + map_entry_layer.cpp compute `frag` at layer-build (same tile they pass to marker_cluster_key) → into Marker. (4) map_renderer.cpp gate inside the draw loop right after the open-group check: `if (config::requireMapFragments && m.fragment_flag && !goblin::ui::read_event_flag((uint32_t)m.fragment_flag)) continue;`. read_event_flag=orp_flag_set (returns false if event-mgr unresolved → markers hidden until game ready, fine on map screen).
  - ⚠️ **config::requireMapFragments DEFAULT = TRUE** (goblin_config_schema.cpp:16, ini key `require_map_fragments`) — memory's old "default false" note was WRONG. So overlay-only mode now HIDES markers for undiscovered fragments by default = native behavior; user without all fragments sees fewer markers (correct, not a bug). Set ini false to show all.
  - **NOT ported (follow-up):** per-paramId ExceptionList overrides (no rowId at marker layer; tile table covers the vast majority) + post-event SECONDARY story flags (textEnableFlag2: Erdtree-on-fire tile(11,5)/(19), CharmBroken tile21/22, SealingTreeBurnt tile(20,1)) → those areas show both pre/post icons in overlay until ported. Native GetMapFragment in goblin_logic.cpp:112 SetSecondaryFlags is the reference.
  - **VERIFY in-game:** overlay-only mode + require_map_fragments=true → a region's markers appear only after grabbing its map fragment; =false → all show. Cross-check vs native injection (native_map_injection=true) parity.
- Overlay map (src/worldmap/) = the future map; 5-step refactor DONE+VERIFIED (projection bake → MarkerLayer module → atlas → all categories → native_map_injection toggle).
- Marker polish DONE: per-category fallback colors, by-NEAREST-GRACE clustering (matches native), pile-AT-grace (no sea drift), zoom-stable clearance offset, resolution-relative + configurable scale (master/icon/cluster, F32 ini, F1 sliders, Save-to-INI), icon-vs-cluster scale decoupled.
- Bugfix DONE: seed_runtime_gates (overlay-only mode now honors category/section/cluster/master toggles + clustering opt-in).
- **Mid-session resolution zoom: raw-poke fix PARTIAL** (borderless same-aspect ✅; windowed/fullscreen ❌ = swapchain not cleaned). **WAITING ON RE AGENT** for follow-up brief docs/re/windows_midsession_resolution_swapchain_re_followup_prompt.md (commit d699665).
- **TOOLTIPS DONE 2026-06-21 (commit 83e2b10, built+deployed, NOT in-game verified):** hover map cursor over marker → its NAME; over cluster pile → location name + "N markers". `goblin::lookup_text_utf8(id)` (lookup_text+UTF8); Marker.name_id (textId1/grace textId); map_renderer hit-tests cursor (client px from overlay) vs drawn markers/piles, closest-within-radius wins, draws dark tooltip box near cursor. Names via expanded PlaceName FMG (setup_messages runs in overlay-only too). **VERIFY in-game: hover markers/piles → names/counts show; check cursor-px aligns with marker-px (DisplaySize vs client).**
  - DONE 52217b4: marker tooltip = item name + LOCATION (loc_pname) two lines (native-style). DONE b5f854a: cluster pile = location NAME label under the glyph (shadowed, centred).
  - NEXT parity (user picks): COLLECTED/cleared graying (dim looted/cleared via live event-flag reads — highest value; needs marker eventFlagId), per-category CENSUS badges (F1 0 in overlay-only → recompute from MAP_ENTRIES + loot flags), REGION labels (major-region names on map, goblin_major_regions anchors), quest-NPC gating, **require_map_fragments port (see below)**. Plus WAIT on RE agent (swapchain follow-up d699665).
**`require_map_fragments` PORT PLAN (explained to user 2026-06-21, NOT yet implemented):** legacy mechanism = each TILE(area,gx,gz)→map-fragment region→its discovery EVENT FLAG via `MapList` (goblin_map_tiles.hpp) + static `GetMapFlagFromTile(MapTile)` (goblin_logic.cpp). Player finds fragment→game sets flag. Legacy rides the GAME's per-icon gate: sets `row.eventFlagId = fragment flag` → native icon shows only when its eventFlagId set (+ secondary story flags textEnableFlag2 for post-event areas: Erdtree-on-fire @ tile(11,5)/(19), CharmBroken @ tile21/22, SealingTreeBurnt @ tile(20,1)). OVERLAY PORT (small, all infra exists): (1) expose `goblin::map_fragment_flag(area,gx,gz)` wrapping GetMapFlagFromTile; (2) Marker += `int fragment_flag` (compute at layer-build like loc_pname, from srcArea+gridX+gridZ); (3) renderer gate: `if (config::requireMapFragments && m.fragment_flag && !goblin::ui::read_event_flag(m.fragment_flag)) skip;`. config::requireMapFragments already exists (default false=show all). read_event_flag=orp_flag_set (goblin_inject.cpp:3300).

## 🔜 NEXT TASKS (handoff 2026-06-20 #8 — do in this order)
**REFACTOR STEP 1 DONE 2026-06-21 (commit 23d9b4a on docs/worldmap-projection-re-brief, built+deployed dll/offline, backup .bak-pre-bake-refactor, NOT yet in-game verified).** Baked the projection + motion-sync, stripped ~1000 lines of RE/diag scaffolding from goblin_overlay.cpp (affine solver, calib UI, grid/reticle-dots/LAGCSV/MAPFPS/LEAD diags, solo/area hotkeys, "Marker proto" tune window, project_uv/os_mouse_px/AffineFit/g_calib all GONE). NEW baked API in goblin_projection.hpp: `View`/`Calib` structs, `view_center`, `project_screen` (canvas factor + res-relative bias), `ViewDelay<>` ring buffer (delay baked 1.0). draw_markers_proto now ~90-line clean grace renderer (region gating + exact converter for base-OW/UG + DLC-OW, baked `dlc_ug_eyeball` helper for the unverified DLC-UG group-3). Legacy RE-era header funcs kept so off-target tests build; added T5 test for project_screen (9 pass + 6 lag pass). **STEP 1 IN-GAME VERIFIED by user 2026-06-21 (works).**
**REFACTOR STEP 2 DONE 2026-06-21 (commit bba3907, built+deployed, NOT yet in-game verified).** Extracted the world-map drawing into a NEW module `src/worldmap/` (subfolder per user's note, kept separate from legacy goblin_inject native-injection):
  - `worldmap/marker_layer.hpp` — generic `MarkerLayer` interface + `Marker` struct {worldX/Z, group 0..3, srcArea, packed-ABGR color}. ImGui-free, decouples DATA↔DRAW.
  - `worldmap/grace_layer.{hpp,cpp}` — `GraceLayer` = 1st MarkerLayer impl; builds its Marker cache once from `live_graces()` with group classification.
  - `worldmap/map_renderer.{hpp,cpp}` — `render_markers(layers)` owns the common path: live view + baked ViewDelay + open-group gating + world→map-space (exact converter / DLC-UG eyeball) + project_screen + cull + draw. Holds the static g_view_delay + g_centroid + dlc_ug_eyeball (moved out of overlay).
  - `goblin_overlay.cpp` draw_markers_proto = thin shim driving a static GraceLayer through render_markers (−132/+8). CMake: registered new sources + added `src/` to MapForGoblins include path (so worldmap/*.cpp reach bare-named headers).
  - **Adding a category = just a new MarkerLayer.** **STEP 2 IN-GAME VERIFIED by user 2026-06-21 (works).**
**REFACTOR STEP 3 (ICON ATLAS) DONE 2026-06-21 (commit 875e5e0, built+deployed, NOT yet in-game verified).** Markers now draw as REAL textured icons (circle fallback), reusing the UPSTREAM (VirusAlex) atlas — checked all 3 remotes (upstream=VirusAlex orig, gacsam, origin=<user>); upstream had it.
  - **VENDORED from upstream/master:** `src/generated_shared/goblin_overlay_icons.{hpp,cpp}` (generated RAW RGBA atlas 400×280, 40px CELL, ~60 cells keyed by `show_*` strings incl. `show_graces`, bosses, smithing, sorceries, items… = matches our category plan; NO runtime decode) + `tools/generate_overlay_icons.py`. Same project lineage (our origin's upstream) → licence fine.
  - **DX12 upload** ported into goblin_overlay.cpp from upstream: SRV heap bumped 1→2 ([0]=imgui font, [1]=atlas), `upload_rgba()` (CreateCommittedResource default+upload buf, 256B row align, CopyTextureRegion, fence-wait, CreateShaderResourceView) + `try_upload_atlas()` (one-time, self-gating, called in hk_present once g_command_queue captured; circle-fallback on fail). Released in cleanup_imgui_device. Dropped upstream's __try (clang-cl elides it, see [[clang-cl-seh-noinline]]).
  - **Renderer:** `Marker.icon_key` (GraceLayer = "show_graces"); `render_markers(layers, atlas_texture)` → `AddImage(cell UV)` when atlas+key resolve else circle. UV math pure (from generated header) → renderer has NO DX12 dep. ImGui 1.90.9 ImTextureID=void*. Icon ≈26px.
  - **STEP 3 IN-GAME VERIFIED by user 2026-06-21 (works).**
**REFACTOR STEP 4 (ALL CATEGORIES) DONE 2026-06-21 (commit 0bf68a1, built+deployed, NOT yet in-game verified).** Overlay map now draws EVERY marker category, each a MarkerLayer, gated live by the F1 menu.
  - `worldmap/map_entry_layer.{hpp,cpp}` — `MapEntryLayer` over baked `MAP_ENTRIES` (9296 rows, MAP_ENTRY_COUNT), one per category; ALL categories share ONE bucketed cache built in a single pass (same marker_world_pos + group classify as graces). `visible()` = `goblin::ui::category_visible((int)cat)`.
  - `worldmap/category_meta.{hpp,cpp}` — `category_icon_key(int)` = 63-entry Category→atlas-key table, `static_assert`'d vs `Category::WorldInteractables`+1. nullptr→circle fallback (~6 cats: talismans/whetblades/utilities/throwables/world_maps/quest_npc have no atlas icon). 5 Reforged/Kindling use atlas names (show_ember_pieces/items_and_changes/rune_pieces/fortunes/kindling_spirits) NOT the schema show_* key.
  - `GraceLayer::visible()` now reads live `show_graces` (was hardcoded true). Shim = GraceLayer + one MapEntryLayer per other cat (reserve→stable ptrs), gated on master `icons_enabled()`.
  - ⚠️ **BEHAVIOR CHANGE:** overlay markers now follow F1 master+per-category toggles (graces included) — default per-cat = OFF, so **user must ENABLE categories in F1 to see overlay markers** (graces no longer auto-on). Master defaults ON. ⚠️ **DOUBLE-DRAW:** legacy injection ALSO reads these toggles → enabling a cat shows BOTH native injected icon + overlay icon until injection decommissioned.
  - **STEP 4 IN-GAME VERIFIED by user 2026-06-21 (works + live toggle update).**
**REFACTOR STEP 5 (OVERLAY-ONLY TOGGLE) DONE 2026-06-21 (commit 7bb7ec2, built+deployed, NOT yet in-game verified).** User chose the REVERSIBLE TOGGLE (not full rip-out) because overlay isn't yet at feature-parity (no clustering/census/collected/region-labels).
  - New config bool **`native_map_injection`** (INI [section as overlay flags], default TRUE). FALSE → dllmain SKIPS `inject_map_entries` + `apply_map_logic` (gated at dllmain.cpp ~194) → nothing written to game WorldMapPointParam = NO native page-build (no map-open freeze) + NO native icons (no double-draw). `capture_live_graces` still runs first (overlay grace data intact).
  - Overlay marker layers + cursor probe AUTO-ENABLE when `!nativeMapInjection` (hk_present `proto = overlayMarkersProto || !nativeMapInjection`; probe-start in dllmain ~248 same OR) → no need to also set overlay_markers_proto. config field in goblin_config_schema.cpp (namespace goblin::config) + extern in goblin_config.hpp.
  - TRUE = classic native map + all legacy features. Flip anytime. Legacy code UNTOUCHED.
  - **STEP 5 IN-GAME VERIFIED by user 2026-06-21 (works) — overlay-only mode functional. THE 5-STEP REFACTOR IS COMPLETE (projection bake → MarkerLayer module src/worldmap/ → icon atlas → all categories → overlay-only toggle).**
**POLISH 2026-06-21 (commit 74ebc68, built+deployed, NOT in-game verified):** per-category fallback COLORS done + renamed draw_markers_proto→draw_worldmap_markers. category_meta CAT[] table now carries a Group (Equip/Key/Loot/Magic/World/Quest/Reforged) → category_color() per-group circle colour. NOTE atlas icons are ALREADY individually coloured by the generator, so category_color only affects the ~6 iconless cats (talismans/whetblades/utilities/throwables/world_maps/quest_npc) + icon-load failures (not most markers).
**CLUSTERING ported to overlay 2026-06-21 (commit 78bb035, built+deployed, NOT in-game verified).** Live render PASS (no pool rows/FMG/injection) in map_renderer — the whole reason overlay clustering is trivial vs the legacy pool-row nightmare ([[cluster-runtime-queue]]/[[cluster-redesign-bugs]] all moot now).
  - Marker gained `category` field (set by each layer). Pass: categories opted-in (`ui::category_clustered(cat)`) bin into a SCREEN-SPACE 44px grid; cells ≥ `ui::global_threshold()` draw ONE mixed-pile glyph (disc + member count at centroid), sparse cells draw members normally; non-opted cats always normal. Gated on `ui::clustering_enabled()`. Re-binned EVERY FRAME → live toggles + zoom-aware (clusters dissolve zooming in), no rebuild. Matches user's ask (ONE mixed pile per dense spot, per-cat checkbox=join/normal, threshold, live).
  - Reuses existing F1 cluster controls (live atomic reads). SCREEN-cell binning (not legacy by-location PlaceName) → no loc_id plumbing, no wrong-location bugs. **Location-NAME labels (vs count-only) = later pass** (needs loc_id from MapEntry textId). **VERIFY: overlay-only mode + enable clustering + opt some cats in F1 → dense spots collapse to numbered piles, dissolve on zoom-in, live.**
**BUGFIX 2026-06-21 (commit b8e1df7, built+deployed): overlay-only mode sub-toggles had no effect.** ROOT CAUSE: per-category visible/cluster-opt-in/threshold + master + per-section visible atomics were seeded ONLY inside inject_map_entries → SKIPPED in overlay-only mode → all zero-default → categories read hidden + cluster opt-in all-false (so clustering pass NEVER binned → "enable clustering does nothing"). FIX: extracted `goblin::seed_runtime_gates()` (pure config→atomic seed, no park/replan) called ALWAYS from dllmain (inject still seeds too in native mode). ALSO added SECTION gating to overlay layers (category_visible AND section_visible) for parity. This also fixes the "enable cluster wrong calculation" backlog item (was: no opted-in category).
**TOOLTIPS (user Q 2026-06-21): EASY, NO atlas needed.** Overlay knows each marker's SCREEN pos → hover hit-test + ImGui tooltip. count=trivial (clusters). Marker NAME/location = resolve textId via MsgRepository/FMG live (graces already carry textId; MapEntry.data has textId fields + object_name) — data-plumbing, moderate, not atlas. Atlas is only for ICONS (already have it). "Replicate vanilla LOOK" = ImGui styling, still no atlas.
**CLUSTERING REWRITE 2026-06-21 (commit 441f40e, built+deployed): by-NEAREST-GRACE, not screen cells.** USER BUG: overlay-only+clustering-on, some objects clustered NATIVELY but NOT in overlay. CAUSE: overlay binned by 44px SCREEN cells; native (replan_clusters grp_key) groups by nearest Site-of-Grace within source area → markers at same location but >44px apart on screen didn't pile. FIX: exposed `goblin::marker_cluster_key(area,gridX,gridZ,posX,posZ,&pname)` = `find_nearest_grace` (same as legacy grp_key) → Marker.cluster_key + loc_pname (both layers populate). map_renderer now groups clustered markers by cluster_key: group size > clusterThreshold → ONE glyph at member screen-centroid; cluster_key<0 → always exact; clustered markers NOT culled pre-group (off-screen members still count). Threshold uses `>` (config "> this many"). Matches native by-location piles.
**MARKER POLISH 2026-06-21 (commits ef57979/0c6085b/c05d2ae): pile-offset + zoom-stable + scaling config.** (1) pile offset OFF its grace icon (~28px) in the cardinal dir AWAY from nearest neighbour grace — ANGLE is zoom-invariant so stable across zoom (earlier max-clearance pick flipped at high zoom). (2) icon/glyph sizes now RESOLUTION-RELATIVE (×realH/1080, base kIconHalfBase=10→20px, kGlyphRBase=12) to match native canvas-scaled icons (were fixed 26px = too big). (3) NEW `IniType::F32` float config type (load std::stof clamp 0.1-5, save std::to_string) + 3 floats overlay_master_scale/overlay_icon_scale/overlay_cluster_scale (default 1.0) → final size = res-base×master×type; F1 "Marker scale" sliders (live) persisted via Save-to-INI. F32 is reusable for any future float setting. (4) commit 29e2509: cluster pile OFFSET now `2*glyphR` (cluster-scale only) — was iconHalf+glyphR so overlay_icon_scale wrongly moved piles; now icon scale = icons only, cluster scale = piles only. **ENDGAME CHECKLIST at top of file (parity F1↔init, port remaining features, remove native map, baked→runtime getters).**
**PILE-AT-GRACE 2026-06-21 (commit 371bbfa, built+deployed): fixed "25 in the overworld SEA" + mislocated underground piles.** CAUSE: pile glyph drawn at member SCREEN-CENTROID → drifts into sea when a grace-group spans water or has a transitive-conv mis-projected member. GROUPING (nearest-grace) was CORRECT; only the pile POSITION was wrong (NOT a tile-matching bug — user's 2nd hypothesis ruled out; 1st hypothesis "reuse grace locations" = the fix). FIX (user's idea): place pile AT its grace. Added raw grid+pos (gridX/gridZ/posX/posZ) to GraceAnchor struct + regen build_grace_anchors.py (444 anchors); `goblin::grace_anchor_world(key)` projects an anchor via the SAME marker_world_pos pipeline (handles underground conv) → world_to_mapspace_xy → project_screen → glyph at the grace (fallback centroid if lookup fails). 256 convention CONFIRMED correct (grid→world always *256, matches anchors; the "no 256 underground" rule is for the INVERSE world→grid split, never done here). **VERIFY: no piles in the sea; underground piles on their grace; pile sits where the grace renders.**
  - NOTE issue-1 "some cats don't honor visible" = mixed-pile perception (toggling 1 cat shrinks a multi-cat grace pile but it persists for others) = CORRECT/native behavior, not a bug (gating verified equivalent to legacy is_section_hidden). Entrance-fallback for graceless dungeons (legacy entrance_cluster_key) NOT replicated → those stay exact (minor; backlog).
  - **NEXT (remaining parity): cluster location-NAME labels (loc_pname now carried → resolve via FMG), TOOLTIPS (count/name/location on hover), per-category CENSUS badges, COLLECTED/cleared graying, REGION labels, quest-NPC gating → THEN flip default to overlay-only.**
1. **REFACTOR FIRST (user wants this before adding categories — `goblin_overlay.cpp` WAS a "fourre-tout": projection + calib + ~20 hotkeys + affine solver + diags + grace loop + F1 menu mixed; step 1 stripped the diag half).** Target separation:
   - (a) **Projection** = pure/testable in `goblin_projection.hpp`: world→render→screen + the −1.0-frame ring-buffer view delay + canvas factor (realW/1920) + res-relative bias. No UI state.
   - (b) **Generic MarkerLayer** interface `{ markers(), category, color/icon, visible_pred, page }` — each data type is a plugin; **graces = 1st impl**; decouple data↔draw.
   - (c) **Renderer** = one category-agnostic loop: visible layers → page-cull → project → draw (atlas/circle).
   - (d) **Diags quarantined** behind ONE debug flag (or stripped once baked): grid=I, reticle dots (red/cyan/orange), LAGCSV=V, LEAD slider/F/H, MAPFPS=T/Z, calib C/X, affine P/M/U/Del/solver.
   - (e) **F1 settings panel** separated.
   - STEP ORDER: (1) BAKE the tuned constants (−1.0 delay, canvas factor, projection) → strip the diag scaffolding; (2) extract MarkerLayer, port graces; (3) generic renderer + icon atlas (currently coloured `AddCircleFilled`, no atlas); (4) plug other categories = just new layers.
2. **Finish Grace MVP polish:** icon atlas (vs circles); optional discovered/undiscovered gate.
3. **Then categories + the FEATURE BACKLOG below** (live grace data, require_map_fragments, discovery gates, DLC fog-of-war RE).
Once (1)+(2) done, the map is READY for all other data — `project_uv` is already generic, adding a category = feeding a new MarkerLayer, no projection re-RE.

## ⏩ SESSION HANDOFF (2026-06-20 #8, read this first)
**PROJECTION SOLVED — the "décalage worse at low zoom" was a MISSING CANVAS FACTOR, not temporal.** Branch docs/worldmap-projection-re-brief; commit **be1bd9c** (+ CT fixes 248cf17/83545e0/e99cb92). The #7 handoff mis-framed it as ease residual; it was a static scale bug. MEASURED via Cheat Engine (`MapForGoblins_mapspace.CT`, NUMPAD 0 on the SAME grace at 3 zooms, now also logs mouse-pixel + canvas), NOT guessed — burned a whole session guessing (ease/lead/multiply-vs-divide all WRONG) before the user insisted on measuring.
- ✅ **THE FIX:** `project_uv` was missing the canvas scale. Engine renders the map in a FIXED virtual **1920×1080** GFx canvas (hardcoded, confirmed in decompiled FUN_1409bdc50 / FUN_1409ce190 = literal 1920.0), then scales to the backbuffer. project_uv did `(mU−centerU)·zoom·scaleX + realW/2` — NO `realW/1920` factor → at 1280×720, scaleX=1.0 over-scaled markers by 1/0.667=1.5× → error ∝ distance-from-centre = worst at low zoom (more map visible), perceived during pan, whole grid. **Fix = multiply by `(realW/1920)` and `(realH/1080)`** (scaleX/scaleY stay ≈1.0 residual). project_uv line ~441.
- ✅ **MEASUREMENT that nailed it:** same grace, 3 zooms → `screen_local = render104·zoom − pan`; paired with the grace's screen pixel (mouse hovered on it). Linear fit `pixel = 0.665·screen_local + offset` held across ALL zooms (residual <1px). **scale 0.665 ≈ 1280/1920 = canvasW/1920, CONSTANT** → proves static canvas factor, not zoom-varying, not temporal, not ×-vs-÷.
- ✅ **RULED OUT by the same data:** (1) ×zoom is EXACT (`reticleFC·zoom − pan` = constant 692.7 across 11× zoom range); `/zoom` is garbage — so the engine stage-1 `(marker+pan)/zoom` "DIVIDE" in the RE doc is NOT the render→screen form, MULTIPLY is. (2) **+0xFC = the VIEW CENTRE** (`reticleFC·zoom−pan` = snapMid exactly) → our `centerU=(pan+snapMid)/zoom` is confirmed correct; the CT-locked cursor is the centre mirror, not a mouse-tracker. The canvas fix ALSO killed a gamepad/no-mouse drift (map now stays fixed without touching the mouse).
- ✅ **"dash" SOLVED** (commits 1ec95be/34093b0/dda8322) — was a CONSTANT temporal phase after all (the earlier ease/lead "failures" were contaminated by the scale bug). The native GFx map layer is composited **exactly 1 frame behind** our Present sample, so map-bound markers need a **−1.0 frame view DELAY** to sync (user-tuned, perfect). Implemented as a frame-history RING BUFFER (sample |lead| frames back, fractional lerp) — NOT extrapolation `cur+lead·(cur−prev)`, which overshoots/flips the points on sharp reversals (keyboard/gamepad ZQSD pan = instant velocity). g_lead applied to the view CENTRE+zoom, written back to pan; F1 "delay (frames)" slider + F/H (0.125 step). MAPFPS throttle (T/Z) was tested = best at 0, NOT the fix. CORRECTION (user, image-confirmed): it's NOT "markers on a delayed layer but cursor fresh" — the ENTIRE game frame (map AND its cursor) is composited ~1 frame behind the OS input, so the ER cursor itself LAGS the raw OS mouse. Markers (delayed view) already match the map; the magenta diag cross was drawn at the RAW OS mouse = AHEAD of the ER cursor → also delay the drawn cross by |g_lead| frames (mouse ring buffer) to sit on the ER cursor. So 1-frame delay is GLOBAL (whole render behind input), applied to both markers (via view) and the diag cursor (via mouse).
- ✅ **Resolution-relative bias** (commit 5f755f5): g_calib.biasX/biasY now stored in 1920×1080-reference px, scaled by realW/1920 in project_uv + C-key calib → calibration is resolution-independent (constant-px bias used to mis-place at a different res). The projection scale (realW/1920) was already res-independent.
- ⚠️ **ER-SIDE GOTCHA (Linux/Proton):** changing the game resolution MID-SESSION (e.g. 1920→1280) corrupts the world-map zoom AND breaks imgui/minimap inputs — it's an Elden Ring swapchain/canvas staleness bug, NOT ours. **Workaround: restart ER** after a resolution change. So test each resolution from a FRESH game launch. **RE BRIEF WRITTEN 5b8c26b → ANSWERED 2026-06-21 (commit 3ce2b18, docs/re/windows_midsession_resolution_swapchain_re_findings.md).** RE agent SOLVED it (static Ghidra). My hypothesis-1 RIGHT (stale render dims), refined by user clue (3D world ALSO zooms ⇒ shared field, not GFx-only). ROOT CAUSE: active render-target dims `[DAT_1447ef360+0x128 + outIdx*0x170] +0x118(Wf)/+0x11c(Hf)` read by BOTH 3D viewport AND map zoom-fit FUN_140d84990; mid-session resize only refreshes the PRIMARY swapchain output entry, leaves 3D/UI-map output entries' dirty bit (+0x140) unset → stale old-res → ~1.5x zoom (oldW/newW). Canvas math FUN_1409bdc50/ce190 read CONSTANTS 1920/1080 (clean); our realW/1920 reads backbuffer live (mod side fine). FIX (call from hk_resize_buffers, gated default-OFF): (1 safest) `FUN_1419ebb40(renderMgr=*(base+0x47ef360), 0xffffffff)` = recompute active dims ALL outputs, no swapchain side-effects [AOB 48 8B C4 55 41 54 41 55 41 56 41 57 48 8D A8 B8]; (2 heavier) `FUN_1419ed440(mgr,w,h)` full re-apply. RUNTIME TEST: probe entry+0x118/11c before/after resize (stay 1920 while GetDesc=1280 ⇒ confirmed), then call fix(1). injection point = hk_resize_buffers (goblin_overlay.cpp:2091). **STEP 1 (probe) IMPLEMENTED 2026-06-21 (commit a9cd187, built+deployed):** `worldmap_probe::dump_render_dims(bbW,bbH)` walks render-output list (DAT_1447ef360+0x128..+0x130 stride 0x170), logs each entry active dims +0x118/+0x11c + dirty +0x140 vs backbuffer as `[RENDIMS]`; config `debug_render_dims` (default false); hk_present calls ~every 2s w/ swapchain GetDesc dims. RPM-guarded, self-contained (no probe-init needed). **TEST: enable debug_render_dims → change res in-game → read MapForGoblins.log [RENDIMS] → the entry whose activeWH stays OLD-res = the stale one (confirms target). STEP 2 (gated fix calling FUN_1419ebb40) = after step-1 confirms; needs AOB-resolve + crash-risk in-game verify.**
**STEP 1 CONFIRMED IN-GAME 2026-06-21 ([RENDIMS] logs): after 1080→720, backbuffer=1280x720 but entry#0 activeWH=1920x1080 AND srcWH=1920x1080 (BOTH stale, dirty=0, single output, persistent) → resize never touched the render-output entry. So fix(1) recompute-from-source useless (source stale too). NOTE crash @ eldenring.exe+0x1EB9999 during test = the SetEventFlag detour (debug_event_flags=true in user INI) — KNOWN unrelated crasher, NOT our probe (RPM-safe) NOR the res change; tell user to set debug_event_flags=false.**
**STEP 2 DONE 2026-06-21 (commit f6661d7, built+deployed): RAW-POKE fix (agent fix-3, NOT the engine call).** `worldmap_probe::fix_render_dims(w,h)` + seh_write_i32/f32 (WPM-guarded): on swapchain resize, write each render-output entry's SOURCE (src+0x3c8/0x3cc int) + ACTIVE (entry+0x108/0x10c int, +0x118/0x11c float) dims to new W/H, offsets +0x110/0x114=0 (no letterbox, same-aspect only). NO engine call (avoids FUN_1419ebb40 VMP crash-risk). config `fix_midsession_resolution` (default false); hk_resize_buffers calls it w/ live GetDesc dims. **VERIFY: enable fix_midsession_resolution + debug_render_dims=false-event-flags, change res in-game → 3D+map NOT zoomed; [RENDIMS] shows activeWH=srcWH=new res. If persists/misbehaves → fall back (restart ER), or escalate to engine recompute call.**
**FIX PROVEN WORKING IN-GAME 2026-06-21 (user + [RENDIMS] logs): after resize, fix_render_dims patches entry → activeWH=srcWH=new res, NO zoom.** BUT coverage gap found: some res changes resize the backbuffer WITHOUT firing ResizeBuffers (swapchain recreate / different path) → hook missed → stale again. FIXED (commit f867bd3): fix_render_dims now IDEMPOTENT (skips if first entry active already == w/h, cheap read) → called EVERY FRAME from hk_present (path-independent enforcer) when fix_midsession_resolution on; resize-hook call kept for immediacy. **RAW-POKE FIX = PARTIAL (not complete).** ✅ Fixes BORDERLESS same-mode same-aspect zoom (1080⇄720). ❌ NEW failures (user 2026-06-21): WINDOWED mid-session still zooms; FULLSCREEN→720 = DOUBLED "two games" render. KEY: at the doubling, ALL dims read CONSISTENT (backbuffer=active=src=720, dirty=0) → it's the SWAPCHAIN BUFFERS/present geometry, NOT dims → raw-poke (dims-only) fundamentally CAN'T fix mode/fullscreen changes. **FOLLOW-UP RE BRIEF WRITTEN (commit d699665): docs/re/windows_midsession_resolution_swapchain_re_followup_prompt.md** — asks agent: (Q1 PREFERRED) hand-stage res entry+0x144 + set dirty entry+0x140=1/mgr+0xeb8=1 so ER's OWN consumer FUN_1419ed870 re-derives incl swapchain (no engine call; needs staged-descriptor layout); (Q2) full re-apply FUN_1419ed440 + safe invocation/thread; (Q3) windowed-specific stale field; (Q4) present/buffer field behind doubling. **NEXT: agent answers → implement the complete swapchain re-derive. Raw-poke stays as the borderless-zoom partial (flag default off). Also need a WINDOWED-mode [RENDIMS] log to diagnose Q3.**
- **NEXT (backlog):** strip the remaining diags (grid=I, LEAD slider/F/H, MAPFPS=T/Z, reticle dots), then per-category colors + icon atlas + decommission the WorldMapPointParam injection. Projection + motion-sync are DONE.
- **FEATURE BACKLOG (user-requested 2026-06-20, for the ImGui overlay map):**
  1. **RE LIVE grace data** — read graces live from the game (BonfireWarpParam/MSB) instead of baked snapshots, to avoid per-mod baked-data drift / breakage. See [[live-param-vs-baked-data]] (params live, MSB/EMEVD baked hybrid); the live_graces() seam exists, needs a live BonfireWarpParam path.
  2. **`require_map_fragments = false`** (new INI flag) — gate: require the player to have DISCOVERED the map fragment of an area before the overlay shows that area's icons. Default off (show all).
  3. **Grace discovered/undiscovered gates** — per-grace visibility gate by the discovery event flag (the overlay can show ALL incl. undiscovered = the mod's point, but offer a toggle to only show discovered, or style undiscovered differently). The discovered-grace gate was disabled when injection-based; re-do it overlay-side (we read flags via SetEventFlag/EventFlag_C1, see [[thread7-coverage-detector]]).
  4. **RE the DLC map FOG OF WAR** — the DLC (Land of Shadow, page 61) map renders BLACK/fogged for the whole map EXCEPT one subregion. Find WHY (which flag/param controls the per-region DLC map reveal — likely WorldMapPieceParam.openEventFlagId remapped by ERR, see [[mapforgoblins-map-open-freeze]]) so the overlay/reveal works on the DLC map.
- LESSON (reinforced): MEASURE before coding. The user was right — without engine ground truth we guessed wrong 5×. The CT hover-snapshot at multiple zooms IS the oracle. See [[disambiguate-bug-symptoms-first]].

## ⏩ SESSION HANDOFF (2026-06-20 #7, read this first)
**Both marker-drift bugs SOLVED.** Branch docs/worldmap-projection-re-brief; latest commits f8cd992 / 1bceb7e / 3739563 / fd0e455 / eb78154 / 8a42082 / e7b9156.
- ✅ **BUG 1 (cursor drift, camera frozen + mouse-to-edge)** = projection anchored to the cursor (+0xFC). Fix: default g_pan_center=true → centre = (pan+snapMid)/zoom, snapMid=(snap-rect +0x340..0x34c mid), cursor-INDEPENDENT (commit fd0e455). Y toggles raw-reticle for A/B; underground was fine, the apparent UG drift was just the Y toggle.
- ✅ **BUG 2 (drift during scroll/zoom, settles at rest)** = we projected the engine's INSTANT target pan/zoom (+0x378/+0x380) but ER DRAWS the map from an EASED view (lerps toward target each frame). Proven with a projected terrain-grid diag (key I): grid + graces lead the map exactly together. Decisive RE: the game draws its reticle from the eased +0x10C (raw[4/5]), NOT target +0x104 — confirmed by a 3-dot diag (red +0xFC / cyan +0x104 / orange +0x10C; orange sits on the white game reticle on fast moves). NOTE +0x10C/+0x104 is snap-contaminated (locked-on-icon → +0x104 snaps, +0x10C stays) so don't derive the centre from it. Fix = low-pass the shared VIEW (pan+zoom) toward target: **a = dt/(dt+tau)** (NOT exp form — exp left a per-frame ramp lag → still fps-dependent; dt/(dt+tau) gives steady lag = v·tau exactly, fps-independent), **g_easeTau≈0.02s** (commit f8cd992). Easing the shared view covers EVERY project_uv element at once. Re-seed on map close (!live→sInit=false) else markers freeze off-screen on reopen ("no icons" bug). F/H dial tau, R=0.02. zoom MUST be eased too (else zoom drifts). ⚠️ STATUS: ~95% fixed, a SMALL residual grid decalage remains during scroll even eased (tau dialing didn't fully kill it). Leads to chase next: (a) the engine ease may not be a pure 1st-order lerp (try matching its exact curve / the snap-anim FUN_1409bc8c0 ease(elapsed/duration)), (b) pan and zoom may need DIFFERENT taus, (c) a sub-frame phase between our Present sample and the engine render that no view-ease can remove (then accept it — this is the inherent overlay-vs-native-layer limit, why modders use the laggy native map). tau=0 (press F to 0) = target/instant for A/B (graces lead the map); tau=0.02 = eased (small residual).
- ✅ **PERF: read_wgm escalation fixed** (108→1089ms over a session). Cache the resolved per-tile structure (geom_ins/name/pos), re-read ONLY the alive flags on cache hit, keyed+validated by the tile's geom_ins vector ptrs; prune unloaded tiles (commit eb78154). Now ~0.01ms on hits.
- ✅ **Input-stuck fix**: hk_wndproc swallowed KEYUP/BUTTONUP while overlay open → keys held before open never released → camera frozen "à vie". Now releases always pass to the game (commit e7b9156).
- ✅ Disabled the all-instance gamepad-cursor scan (slow map-open regression, commit 8a42082).
- DIAG knobs still in (clean up later): grid (I), 3 reticle dots (red/cyan/orange), EASETAU (F/H), MAPFPS throttle (T/Z, unused — not the cause), [LAGCSV] logger (V). TODO: bake final tau, strip diags.
- LESSON saved [[disambiguate-bug-symptoms-first]] — vague "décalage" = 2 different bugs; ask static/dynamic/which-input/direction/settles BEFORE coding (burned many build cycles).

## ⏩ SESSION HANDOFF (2026-06-20 #6, read this first)
**View-centre marker drift = TWO separate bugs. Bug 1 FIXED, bug 2 open.** Branch docs/worldmap-projection-re-brief; latest commit **3eb952d**.
- ✅ **BUG 1 FIXED — "scaling cursor-dependent" drift.** Camera-frozen + mouse-to-screen-edge shifted every marker ~10px permanently. Cause: project_uv subtracted the CURSOR (+0xFC, v.raw[0]) as the projection centre → it scaled about a point that moves with the mouse. Fix (3eb952d): centre = CURSOR-INDEPENDENT view centre from pan+snap-rect (RE §2): `centreX=(panX+(snapMinX+snapMaxX)/2)/zoom`, snap-rect = view+0x340..0x34c (added to LiveView + probe read). Reverted the failed g_pan_center=true default (it dropped canvas/2 centring → placement regression). User-confirmed bug 1 dead. Proven on a real capture: reticle-coupled drifts 1482px over a 149-frame pan-frozen run, cursor-indep = 0px (tests/test_lag_capture.cpp over tests/data/lag_capture.csv).
- 🔴 **BUG 2 OPEN — real-pan lag.** When the map ACTUALLY pans, markers trail ~10px then settle on stop (edge-scroll never stops → looks permanent). NOT cursor, NOT zoom, NOT update-cadence (pan read live every frame; ImGui rebuilt every Present, ~50fps steady from [LAGCSV] timestamps). = the pan read at Present is phase-shifted from the pan the engine rendered the map with this frame (engine eases displayed view toward target; 0.1f lerp — cursor+0x130 / snap-anim FUN_1409bc8c0). WHY ~all modders use ER's native laggy map (user). DIAGNOSTIC SHIPPED: **g_lead** knob ([ −0.1 / ] +0.1 / \ reset, HUD LEAD=) extrapolates pan: `used = raw + lead*(raw−prev)`. NEXT: tune lead in-game during a real pan until markers stick; lead≈1.0 → exactly 1 frame → bake.
- Tooling: [LAGCSV] per-frame logger (toggle V), src/goblin_projection.hpp (pure testable projection), tests/test_projection.cpp + tests/test_lag_capture.cpp (native g++: `cd tests && g++ -std=c++17 -I../src test_lag_capture.cpp -o t && ./t`).

## ⏩ SESSION HANDOFF (2026-06-20 #5, read this first)
Branch **docs/worldmap-projection-re-brief** (off master; NOT merged). Overlay-markers prototype is FAR along. Latest commits: 03c68c7.

**#5 STATE (most important first):**
- ✅ **POSITION SOLVED for all 3 real map pages (60 overworld, 12 underground, 61 DLC overworld)** — exact game converter `mapX=wx−7040, mapZ=−wz+16512` (scale 1, Z-flip; agent dump, areaNo 60≡61 constants). Underground unified via `marker_world_pos(...,conv_underground=true)` (area-12 local→overworld coords) + gate UG by ORIGINAL areaNo. User-confirmed graces land on native icons MOUSE-STILL. Commits d804f78, b65ce2a.
- 🔴 **VIEW-CENTRE STILL OPEN — the agent's fix (839b07b) BROKE calibration in-game (user, 2026-06-20).** Reverted default back to the reticle centre (commit 23af9ea); the pan path stays behind `Y`. The agent's DIRECTION is right (pan is device-independent) but the formula `screen=(marker·zoom−pan)·(realW/1920)` produces WRONG positions — likely an axis transpose (try `G`), a canvas-scale mismatch (the 1920×1080 assumption vs actual backbuffer/widget rect), or panX/panZ↔U/V pairing. Hand back to the agent with "calibration broken, positions wrong even mouse-still" + have the user run a 1-point `C` calib and report the residual. Original analysis below:
- ~~VIEW-CENTRE SOLVED by the Windows agent (commit 839b07b)~~ — superseded, see above. RE'd map-space→screen: `screen_local = marker*zoom − pan` (pan = WorldMapArea +0x378/+0x37C, already in 1920×1080-canvas px, DEVICE-INDEPENDENT — the cursor tick FUN_1409bd4b0 never writes pan, only the pan/zoom setters do; cursor +0xFC/+0x104/+0x10C are ALL the input-driven reticle = root of markers-follow-cursor). My earlier pan attempt was wrong only because it multiplied pan by zoom (`pan+(screen/2)/zoom`); correct = subtract pan DIRECTLY. project_uv rewritten, pan path default ON, Y toggles the old reticle centre. Findings: docs/windows_worldmap_viewcenter_re_findings.md. **RUNTIME-VERIFY PENDING:** grace-lock while panning with BOTH mouse and gamepad; `C` pins a const bias if there's a fixed offset, `G` swaps axis if transposed. If this verifies, the overlay marker projection is COMPLETE for mouse+gamepad.
- ⏳ DLC UNDERGROUND (areas 40-43, page 10) still on eyeball — needs its converter dumped (NUMPAD 3 on the Realm of Shadows map via MapForGoblins_converter_dump.CT).
- ✅ Graces now from `goblin::live_graces()` (overlay refactor, commit e7618ec) BUT sourced from BAKED (commit 03c68c7) — live WorldMapPointParam has NO grace pins (they're BonfireWarpParam+MSB, authored offline; see [[live-param-vs-baked-data]]). The live_graces() seam is ready for a future BonfireWarpParam path.
- ✅ Infra shipped this session: AOB signatures registry + uniqueness health-check ([[re-signatures-registry]], commits b80bd7e/bf75aef/b837bcd), event-flag writer (a19f2bc), per-session zip log lifecycle (f0d1bb5), 100% save swap to test DLC ([[err-save-file-format]]).
- ⚠️ DEPLOY GOTCHA: Bash is sandboxed → builds/`cp` must use `dangerouslyDisableSandbox:true` or the game loads a STALE dll (burned a session on this; see [[mapforgoblins-linux-build]]).
- Polish backlog (bottom of file): warp-teleport flash, Deeproot offset, missing graces.

## ⏩ SESSION HANDOFF (2026-06-20 #4)
Branch **docs/worldmap-projection-re-brief** (off master; NOT merged). Overlay-markers prototype is FAR along.

**#4 PROGRESS (2026-06-20):**
- ✅ **OPEN-MAP REGION GATING = SOLVED + WIRED** (filter #3 DONE). Windows agent solved it (commit 3f4ba42); wired into the overlay (commit **0437909**). `get_live_view` reads two O(1) fields off WorldMapDialog (=cursor−0x2DB0): `page=*(int*)(dialog+0xA88)` (0=base,10=DLC), `layer=*(u8*)(*(void**)(dialog+0x2B68)+0xB8)` (0=surface,1=UG) → `LiveView.openDlc/.underground`. Overlay `open_grp=(openDlc?2:0)|(underground?1:0)` replaces the dead player_in_dlc()+DAT_143d6cfc3 guess. LIVE-CONFIRMED: OW/UG/DLC each draw only their page's markers; UG↔surface toggles live.
- ✅ **EVENT-FLAG WRITER shipped** (commit a19f2bc): `goblin::markers::set_event_flag` via EventFlag_C1 @0x1405d2240 — see [[mapforgoblins-map-open-freeze]]. (Map-reveal-by-flag FAILED — reveal is per-tile WorldMapPieceParam.openEventFlagId, ERR-remapped; used a 100% save swap instead, see [[err-save-file-format]].) DLC now TESTABLE in-game.
- ✅✅ **ALL 3 MAP PAGES CONFIRMED CORRECT (user, 2026-06-20): overworld 60 + underground 12 + DLC overworld 61** all place graces on the native icons (mouse-still). The world→map-space conversion is DONE for every real page. (commit d804f78). Only DLC underground 40-43 left on eyeball.
- ✅ **WORLD→MAP-SPACE = SOLVED (agent commit 0a30738, no affine M/T needed).** The transform is a simple diagonal+Z-flip with EXACT game constants: `mapX = worldX − 7040 ; mapZ = −worldZ + 16512` (origin 7168/16384, bias 128, scale 1.0). Agent dumped the live `CS::WorldMapViewModel` converter array (+0xF8). **areaNo 60 AND 61 have IDENTICAL constants** ⇒ base overworld (60), DLC overworld (61), and base underground (12) ALL use this ONE formula. Wired into goblin_overlay.cpp (replaces the eyeball g_aff for these pages). Underground (12) also needs the legacy conv applied first (local→unified) — done via `marker_world_pos(..., conv_underground=true)`; gate UG by ORIGINAL areaNo (commit pending). LIVE-CONFIRMED mouse-still: overworld + underground graces land on the native yellow icons. Only **DLC underground (40-43, page 10)** still on eyeball (its converter not dumped — NUMPAD 3 on the Realm of Shadows map).
- ⏳ **THE REMAINING BLOCKER = map-space→SCREEN device-independent view centre.** project_uv used cursor+0xFC as centre = RETICLE-COUPLED → markers FOLLOW the mouse/gamepad (correct only mouse-still). Pan-based centre tried + wrong-for-all. Needs RE: `docs/windows_worldmap_viewcenter_re_prompt.md` (find the stable view-centre that pans under mouse+gamepad, OR the engine world→screen projection). Y hotkey A/B-toggles centre candidates. See [[overlay-gamepad-cursor-bugs]]. Polish backlog (warp-teleport flash, Deeproot offset, missing graces) at the bottom of this file.

**PAGE→REGION (verified 2026-06-20 from grace_position_index.json + WorldMapLegacyConvParam). THERE ARE ONLY 3 WORLD-MAP SCREENS: 60=base overworld (tab 61000-65000); 12=base underground (Nokron/Deeproot/Ainsel/Siofra/Lake of Rot/Mohgwyn, tab 12000-12002); 61=DLC, a SINGLE map (Land of Shadow/Shadow Keep, tab 6800-6940+21000).** Base game = 2 separate toggleable screens (60⇄12). **DLC = ONE map (61), NO separate underground screen** (user was right). ⚠️ CORRECTIONS to earlier notes: (1) 61 is DLC, NOT base — "base-OW{60,61}" shorthand is WRONG (tab_id classifier still routes 61 correctly via DLC tabs, but region label was off). (2) **areas 40-43 are NOT "underground DLC maps" — they are DLC dungeon interiors (Stone Coffin Fissure etc.) that legacy-conv ONTO page 61** (verified: 40-43 src-only in conv param, all resolve→61; terminal pages={60,61,12}, 19/70=tiny edge). So markers there render on 61, NOT separate pages → CAPTURE/calibrate only pages 60, 12, 61 (the getter-capture prompt was fixed to list these 3, drop 40-43). (3) The capture anchor called "Dragonbarrow page61" is actually a SHADOW KEEP grace (src area 21, tabId 21000); Dragonbarrow = base Caelid = page 60. Page 61 right, NAME wrong. Getter-capture prompt = docs/windows_marker_offset_getter_capture_re_prompt.md (has the corrected 3-page table).

### STATE @ session #3 end (most recent first):
- **PROBE cursor resolve = O(KB) + crash-free.** seh_read* now use **ReadProcessMemory** (clang-cl elided __try even on noinline → 0xC0000005; see [[clang-cl-seh-noinline]]). Whole-RAM scan DELETED. Cursor resolved via CSMenuMan(slot base+0x3d6b7b0)→WorldMapDialog→+0x2DB0, **2-level bounded walk** (L1 flat + L2 deref, cached → O(1)); dialog was NOT a flat field (L2 found it). Commits 1fc4b01, f0512ef. Sticky single canonical cursor (b158e5f) — no more teleport/mirror-switch.
- **EYEBALL transform (overworld) defaults shipped:** M=swap@1.0 ([[0,1],[1,0]]), render-space rotation **−90°**, FIXED per-group pivot, pan(gtx,gty). Overworld pan default (−1086,−341) (b8194ef). F1 shows final **M'=R(rot)·M, T'=RC+pan−M'·pivot** per layer (ef1f5d1) — read these to bake `render=M'·world+T'`. Pivot is FIXED per group (d470c0a) = stable pan. Scale=1.0 (user-confirmed, NOT 0.5). Rotation MUST be render-space not screen (zoom tracking, 14ab523). All controls in F1 "Marker proto": orientation toggles(swap/negX/negZ), rotation field, pan, UG pan/rot.
- **REGION GATING by tab_id shipped (bb623ab) but BLOCKED:** open-map detection signals all DEAD (sublayer DAT_143d6cfc3=0 always, fullRect const 10496, player≠open-map). region_diag delta-scan (0b204e1) → candidates view+0x370/0x374, dialog+0x98/0x14/0x5c. **2 RE prompts pending for the agent:** (1) docs/windows_open_map_region_re_prompt.md (43bb6b5, +view-driven caveat) = the open-region field; (2) docs/windows_marker_affine_capture_run_re_prompt.md = MEASURE exact M/T (transform is VMP-locked, can't static-RE; eyeball is approximate).
- **NEXT:** hand both RE prompts to the agent. Region field → fix open_grp gating (currently stuck base-OW). Exact M/T → bake invariant pan. Then: per-category + atlas + decommission injection. Known issue: a few graces land in the overworld SEA (conv outliers, area-19 etc.).

**DONE/PROVEN:** screen projection solved (screen=(render−viewCentre)·zoom+canvas/2, sub-pixel, [[overlay-rendered-markers]] body). Probe `get_live_view()` reads live render coord (cursor +0x104/+0x108). DX12/ImGui overlay draws graces. Data via baked MAP_ENTRIES (no new data; live-param = future, see [[live-param-vs-baked-data]]). Tooling: `overlay_markers_proto`/`debug_worldmap_probe` INI flags; in-game keys C(calib reticle) O(solo)+./, N/B(area) K(calib origin) J(dump) L(log) + NEW P/M/U/Del (affine, below). diff tool tools/diff_map_data.py.
**SHIPPED+COMMITTED:** step-1 conv translation+src_gz (f960f81), map-open crash guard + robust load_delay poll (4066b2a, ed347d9), version.rc antivirus-FP (8b3dd2a), overlay proto wip (418e29e), CT recipe to recover M/T via Cheat Engine (ffe7a61). **THIS session (all deployed dll/offline):** in-DLL affine solver (6355a17, backup .bak-pre-affine); reticle screen scale 0.987→1.0 = cursor pixel-perfect (8bdf05c); placement-hook RE brief docs/windows_marker_affine_hook_re_prompt.md (5ef4875); #2 conv nearest-entrance fallback + audit (00c8a0b); grace_placement_followups.md (3e7b28c). Conv-removal Q answered → [[live-param-vs-baked-data]] (NO, only baked snapshot removable).
**KEY MOVE (6355a17):** the world→render step is affine `render = M·world + T` (M=scale·ROTATION shared, T per-page). Instead of hooking the VMP placement thunk with Cheat Engine (CT recipe ffe7a61), we solve M/T **IN-DLL** — the overlay already reads live cursor render (+0x104/+0x108), so calibration pairs are collected + least-squares-fit in-process (solve3x3/solve_affine in goblin_overlay.cpp). Seed M = 90° axis-swap @0.5 (renderX≈0.5·worldZ, renderZ≈0.5·worldX); diagonal baseline kept as fallback. Hotkeys (menu-CLOSED, reticle live): **P**=push (world→render) pair for solo'd grace, **M**=solve+apply, **U**=toggle affine/diagonal, **Del**=clear. F1 window mirrors Solve/Clear + shows M + pair count. Logs `[AFFINE]` lines are bakeable (M once + per-page T table).
**THE BLOCKER (where to resume):** M/T values still UNCONFIRMED — needs a live in-game calibration pass. Per page: solo a grace (O, ./,), hover its real icon, P ×3+ (≥3 pairs/page), then M. Watch `[AFFINE]` log: shared M should match across pages, mean residual sub-pixel. Confirms/corrects the axis-swap signs.
**WEB-SEARCH VERDICT (2026-06-20): no public shortcut for the world→map-render projection.** Souls Modding Wiki (soulsmodding.com er-refmat:map-overview) confirms our WORLD model (small tile=256 units, tile center=local(0,0,0), world=grid·256+pos) — externally validated. But NO mod/tool has solved the world→map-RENDER (Scaleform) transform: the "map overlay" mods are external-browser windows or regulation.bin marker edits (game does the rendering), NOT projected in-game Scaleform overlays. Our approach is novel; M/T RE is the only path; re_v61 closed it (cursor-snap only). DATA (grace list + discovery flags) IS live-readable (no bake, user's instinct right) but POSITION still needs M/T. "Gate discovered to show pre-rest" = valid product/visibility feature, orthogonal to the position math.
**SCALE = 1.0 (user-confirmed, NOT 0.5).** Despite the structural 10496=82·128 (which suggested 0.5), the user tested: 0.5 = graces too close together, **1.0 = perfect**. Overworld default M=swap@1.0 stays. (My 0.5 suggestion was wrong for this pivot/centroid pipeline.)
**PROBE CURSOR RESOLVE NOW O(KB) (98e5e63) + STICKY (b158e5f) — teleport + latency fixed.** Agent Ghidra (c843cc3): cursor = WorldMapDialog + 0x2DB0; CSMenuMan static slot = base+0x3d6b7b0; the dialog ptr is in CSMenuMan's first few KB but the exact offset isn't statically liftable (471 readers). Shipped the agent's reco = BOUNDED WALK of CSMenuMan+[0..0x2000 step 8] checking *(p+0x2DB0)==cursor vtable → O(KB) not O(GB) whole-RAM scan (scan kept as fallback). Returns the CANONICAL menu-embedded cursor (no mirror ambiguity → stable view-centre). Logs winning offset once → hardcode O(1) next patch. STILL TODO live-confirm: reconfirm CSMenuMan+0xCD==7 (log showed it oscillating 3/7/0 around open — may be transient), capture the winning CSMenuMan offset.
**PROBE LATENCY (partial fix ec76ce1, BACKLOG):** "MARKER PROTO: no live view" hung ~5s sometimes = the cursor probe ran a whole-address-space vtable scan every 400ms EVEN map-closed; a map open mid-scan waited it out. Fixed by gating the scan on world_map_open() (CSMenuMan+0xCD==7) + RW-only regions → no closed thrash, faster first find. BUT the first-open scan is still O(committed memory) (multi-GB qword walk for the CURSOR_VTABLE_RVA value). **TODO optimize later: RE a direct O(1) menu→cursor pointer chain** (CSMenuMan → world-map screen obj → cursor = menu+0x2DB0, see [[mapforgoblins-map-open-freeze]] PROBE notes) so get_live_view resolves the cursor with no memory scan at all. **Prompt WRITTEN for the agent: docs/windows_worldmap_cursor_o1_re_prompt.md (c15bcc0)** — carries CSMenuMan AOB + 0xCD==7 gate, cursor vtable 0x2b29a90, cursor=menu+0x2DB0, view@cursor+0xF0, ground-truth check *(menu+0x2DB0)==vtable. Unblocks 3 things at once: instant map-open view, lag-free per-frame read (= fixes the pan "DASH" #4), delete the scan. Also possible fallback: cache the cursor's MEM region across opens (heap reuse) and scan it first.
**SCREEN PROJECTION NOW PIXEL-EXACT (8bdf05c):** scale 0.987→1.0 killed the micro reticle offset (cyan==game reticle confirmed in-game). So render→screen is DONE; remaining grace error is purely world→render (M/T).
**#1 HOOK-RE DELIVERED (re_v56, b885e215, docs/marker_affine_hook_re_findings.md):** hook site PINNED + NON-VMP for world_in: CALL thunk_FUN_1457cd3df in reconcile FUN_140a832a0 @RVA 0xa839a6 (AOB `66 48 0F 7E C9 48 85 C9 74 08 49 8B D5 E8`, call@found+13), RCX=CSWorldMapPointIns* point, RDX=ctx, param row @point+0x80. render_out NOT a point field (+0xa0/+0xe0=color/UV LUT) — it's on the icon Scaleform Matrix2x4 written inside the VMP thunk (entry 0xa805e0). **KEY CORRECTION: cursor-snap IS EXACT (not noisy) — the reticle snap target == icon render pos, so cursor +0x104/+0x108 on a SNAPPED grace = render_out exactly; "noise" only if un-snapped.** → the in-DLL solver (6355a17, P/M keys) IS the viable path, no MinHook of the VMP matrix needed.
**USER FLAGGED 2026-06-20 (base mode, NOT solo):** overlay still visibly ROTATED/inverted — e.g. graces that should be vertical/low render horizontal/upper-right. = the SEED M (90° swap guess) is wrong; by-eye orientation guessing is PROVEN to fail (history: neither diagonal nor swap → genuine rotation of arbitrary angle, needs ≥3 pairs/page). NOTE (user-corrected 2026-06-20): green overlay graces are NOT redundant with the native yellow ones — the game only shows graces the player has DISCOVERED; the overlay shows ALL graces incl. UNDISCOVERED (the mod's whole point = show everything regardless of flags). So graces ARE a real overlay feature, not just a calib reference. Native yellow icons serve only as the calibration ground-truth (their render pos = the transform). Once M/T correct, undiscovered graces + all extra markers land right.
**USER WON'T MANUAL-CALIB → DECISION: the Windows agent EXECUTES the capture + delivers the M/T NUMBERS (not just mechanism).** re_v56 pinned the site but deferred the run. Wrote follow-up **docs/windows_marker_affine_capture_run_re_prompt.md** (8268d6c): bp 0xa839a6 (world_in=point+0x80, areaNo=post-conv dst page) + read render_out from the icon Scaleform Matrix2x4 (VMP thunk 0xa805e0, OR via point→icon + matrix offset = post-call plain read) → capture ≥3 pairs/page from the GAME's own correctly-placed native markers (60,61,12,40-43) → lstsq render=M·world+T → return M(shared)+per-page T constants. Transform is version-STATIC → one-time measured M/T baked = done, zero user calib.
**ARCHITECTURE FORK RESOLVED (agent re_v58/re_v59, 2f2ca20/752d7a4):**
- **re_v59: math (M·world+T) IS REQUIRED.** Undiscovered markers have NO readable render pos — visibility is build-time + event-flag(discovery) gated (show-pred vt[1] FUN_140a81450 registers conditionally, NOT per-frame; per-row FUN_140d58470 reads flag via VerifyEnable/DisableEventFlag); the +0x398 icon-update only positions already-built points. The mod must INJECT rows to show undiscovered at all = proof. (Live confirm: count +0x398 vs on-screen icons.) NO read-live escape.
- **re_v58: NO static auto-read of render_out, NO in-DLL self-cal.** thunk 0xa805e0 = bare JMP into VMP; point→icon + icon→Matrix2x4 offsets live in the VMP gap (ctor 0xa82d09 un-analyzed) → not liftable statically. render_out only via (a) live in-VMP bp 0xa805e0, or (b) SNAPPED CURSOR +0x104/+0x108 (exact on icon). Param-row layout @point+0x80: from areaNo byte gridXNo=+1,gridZNo=+2,posX=+4,posZ=+12.
- **⇒ Capture = snapped-cursor method = EXACTLY the shipped in-DLL P/M solver (6355a17)** (world from baked data, render from snapped cursor; the world_in hook is redundant — we already know each grace's world). Either USER runs P/M, or the AGENT runs the capture (snap ~3/page, or in-VMP bp 0xa805e0 to auto-grab all on-page pairs) → delivers M+T → bake. User won't manual-calib → AGENT does it.
**REGION GATING (2026-06-20) — tab_id classifier shipped, open-map detection BLOCKED on RE.** Gating by GRACE_ANCHORS.tab_id shipped (bb623ab): 4 groups isDLC*2|isUG (base-OW{60,61}, base-UG{12}, DLC-OW, DLC-UG{40-43}); DLC graces conv to 60/61 but carry tab 6800-6999 → tab_id separates them (the scatter fix). goblin::grace_tab_id + player_in_dlc added. BUT the OPEN-MAP detection is DEAD: live-confirmed **sublayer flag DAT_143d6cfc3 = 0 on ALL maps** (dead), **fullRect const 10496** (all maps share the WorldMapArea canvas), and **player can open ANY discovered map** (player_in_dlc ≠ open map). → must read the open-region from the WorldMapDialog. Delta-scan (region_diag in probe, 0b204e1) flagged candidates flipping on map switch: **view+0x370/0x374 (0,1↔10,11), dialog+0x98 (1↔2), dialog+0x14/0x5c (0↔3)** — no clean value→region by eye. **Prompt WRITTEN: docs/windows_open_map_region_re_prompt.md (7800f3e)** → agent finds the authoritative open-region/TAB field on CS::WorldMapDialog (=cursor−0x2DB0, now resolved) + decodes value→region. Until then open_grp is stuck at base-OW (shows only 60/61 on all maps).
**EYEBALL = NOT static-RE-able (VMP, re_v58/v61) → MEASURE.** User asked "can we RE the eyeball too?" — the world→render transform is VMP-locked, no readable field; the EXACT M/T = the snapped-cursor calibration (capture pairs → lstsq), which the agent can EXECUTE (docs/windows_marker_affine_capture_run_re_prompt.md). Eyeball pan is inherently approximate/drifts; the calibration gives the exact invariant. ALSO: each pivot/grouping change I make shifts the eyeball pan reference one-time (not per-session variance) — freeze the grouping before final tuning.
**OVERWORLD EYEBALL FIT LANDED + DEFAULTED (afd1d11, 2026-06-20).** User hand-aligned 60/61: **M=[[0,1],[1,0]] (swap@1.0), render rotation −90°, pan(gtx=−2170, gty=850), centroid pivot.** centroid world≈(10115,10420), equivalent bake T≈(−7342,−4017). Now the AffineFit defaults → boots aligned. APPROXIMATE (scale 1.0 not the exact value → green dot not perfectly centred, de-centering grows when zoomed far → precise per-page lstsq bake still the real finish). OPEN ISSUES user flagged (backlog, post-overworld):
1. not pixel-perfect / zoom-far de-centering = scale imperfect (eyeball; lstsq fixes).
2. NO zone gating yet (page filter #3, needs page-id RE) — all graces drawn at once; discovered-grace gate DISABLED for now.
3. underground (page 12) + DLC (40-43) need their OWN pan/T (likely duplicate the per-page logic); untested.
4. **pan "DASH" lag**: when panning with the mouse the dots aren't updated live — they "dash"/catch up to position, whereas vanilla ER map has no dash. = the live-view (probe pan/zoom) lags the game by a frame/tick → markers chase. Backlog (smooth/live the view read).
5. study how vanilla MapForGoblins gates by zone/discovery, mirror it.

**BREAKTHROUGH — transform ≈ M=diag(0.5) + ROTATION −90° + pan (2026-06-20).** User found the orientation is a clean −90° (=270° CW) rotation, NOT the swap/mirror M. KEY FIX (14ab523): the rotation MUST be applied in RENDER space (rotate M·(world−centroid) by θ BEFORE project_uv), not screen space — screen-space rotation (after project_uv) also rotated the pan/zoom response → "n'importe quoi" on zoom. Now: render = R(−90)·(0.5·(world−centroid)) + RC + pan, then project_uv(live view) → zoom/pan track like the cursor. So the real transform = scale 0.5 (diag M) + a −90° render-space rotation + per-page pan(T). F1 rotation field (g_aff.screen_rot, render-space, zoom-stable) + presets 0/90/180/270 + slider; eyeball flow: diag preset + rotation −90 + pan. NEXT: find per-page pan(T), bake effective M' = R(−90)·diag(0.5) + T.
**USER FOUND THE PIVOT BUG + CENTROID-ROTATE SHIPPED (bfde02c):** "rotate doesn't rotate, it MOVES the map" — because render=M·world rotates about world-origin (0,0) and markers sit ~10000 units away → any M change flings them (looks like translation). FIX: eyeball pivot mode render=M·(world−centroid)+RC(5248)+pan → M spins the cloud IN PLACE about its centroid; pan positions it. F1 shows centroid + bakeable T=RC+pan−M·centroid (eyeball fit → render=M·world+T directly). Pivot ON by default; plain M·world+T[page] path kept for lstsq Solve. USAGE: F1→ pivot ON → preset 90 CW (now rotates in place) → drag pan onto page-60 icons → read a,b,c,d + "bake T" → give me → bake.
**USER READS IT AS 90° CW + EYEBALL-DIAL SHIPPED (abcd0b0):** the seed M=[[0,.5],[.5,0]] is a REFLECTION (det<0), NOT a rotation — on one anchor mirror≈rotation, but the spread looks rotated/flipped (= user's "90° CW"). The true map frame is likely a 90° ROTATION (det>0) = one off-diagonal sign flipped. F1 "Marker proto" window now: shows det + MIRROR/rotation label, editable a/b/c/d (DragFloat4), global pan gtx/gty (added to every page), presets (90 CW / 90 CCW / mirror / diag). TEST: pick a preset, drag pan until page-60 graces sit on game icons → aligns by rotate+pan alone = clean 90° (read a/b/c/d + pan, bake); won't align = T per-page differs = use lstsq Solve. (Projection adds gtx/gty; no change at 0.)
**PIVOT = T (clarified 5189233, docs/windows_marker_affine_pivot_clarification_re_prompt.md):** user's "just rotate 90° but it flies off-screen" = the overlay runs M(seed)+T=0; a rotation about world-origin sends large world coords off-screen → the per-page T is the missing "pivot/re-centering". T is NOT a separate unknown — it falls out of the same per-page lstsq (the '1' column) once render_out is captured. DELIVERABLE MUST be M(shared) AND T=(e,f) for ALL 7 pages (M alone=T=0=exact off-screen bug); on-rect [0,0,10496,10496] sanity per page.
**CAPTURE TOOLING BUILT (agent 387d31b, tools/cheat_engine/MapForGoblins_render_finder.CT v6, NUMPAD 3):** auto-finds render_out by SCANNING the point object's float fields for a pair tracking world linearly (NO debugger / NO VMP step — if the render field lives on the point, skips the VMP bp); groups by page, fits shared M on dominant page, emits T[page]+(on-rect YES/NO) for all pages. Fallback print if no field tracks → snapped-cursor. NOT YET RUN (tool, not numbers). Caveat it flags: a page needs ≥4 BUILT markers to fit → may need temporary calibration-time INJECT mode to have enough built markers per page (one-shot, not runtime; consistent with re_v59 only-built-have-pos).
**LIVE-CONFIRMED + capture method (agent ce94d2b v5 / 387d31b v6):** render_finder vtable-scans 3 point classes (Point 0x2b487a8, Discovery 0x2b48440, Reentry 0x2b48d28), no debugger. On an open overworld page it found only **4 built instances** (Point=2 Discovery=1 Reentry=1) → EMPIRICALLY confirms undiscovered=not built=no render pos → M·world+T mandatory (re_v59 was right). CORRECTION: **point+0x80 is a POINTER to the WorldMapPointParam row** (read [point+0x80] then fields), not inline.
**ANOMALY RESOLVED → RE CLOSED (agent re_v61, d870160).** render_finder (C++ instance scan) = DEAD END: only ~4 CSWorldMapPointIns exist. The visible marker icons are **Scaleform display objects under 9 CS::WorldMapItemControl lists** (MarkerList/AreaList/ItemList/WarpList/MemoList…, built FUN_1409be5e0 / 9× FUN_1409ca380), positioned by the GFx movie — NOT per-marker C++ objects. Hover = generic FD4 GUI hit-test FUN_140d7ff40 (0x74xxxx layer), same Scaleform/VMP wall. "Font Baker" = dead end (bakes tooltip TEXT, not icon pos). **FINAL: NO C++ per-marker render field exists; the ONLY C++-readable render pos is the cursor +0x104/+0x108. No further RE will surface one.** ⇒ snapped-cursor calibration is THE path, definitively.
**CAPTURE METHOD (corrected) = SNAPPED CURSOR.** render_out = cursor +0x104/+0x108 (exact on an icon). Hover 3-4 KNOWN graces per page, pair with their known world → solve M + per-page T. "A handful of exact pairs beats hundreds of unavailable." Tools: (a) the in-DLL P/M solver (6355a17 — solo grace=known world, snap, P; auto-pairs; BEST), or (b) MapForGoblins_mapspace.CT NUMPAD 0 (log render+pan/zoom, note grace, offline solve). show_all=true (set in dll/offline/MapForGoblins.ini) = more graces visible to hover, helpful not required.
**NEXT ACTION:** capture 3-4 known-grace pairs/page (60,61,12,40-43) via in-DLL P/M or mapspace.CT NUMPAD 0 → M + T[pages] → hand me numbers → bake into goblin_overlay.cpp (replace g_aff runtime solve). When it returns M+T → bake as constants in goblin_overlay.cpp (replace g_aff runtime solve) → base mode correct → per-category + atlas (upstream goblin_overlay_icons) + decommission injection. Anchors: Dragonbarrow(61) world(12338,11985)→render(6018.75,6187.28); Academy(60) world(9217,13617)→render(1826,?). (In-DLL manual solver P/M still exists as fallback; agent confirmed cursor-snap IS exact.) (Approx cursor-hover variant: docs/marker_mapspace_CT_recipe.md.) FULL followup list = docs/grace_placement_followups.md. **#2 legacy conv = DONE (00c8a0b):** "transitive" was a non-issue (pipeline resolve() flattens all chains to 60/61 at bake → runtime loop=dead code); audit of 153 legacy graces = 148 exact, 5 degrade-to-entrance (area19 Haligtree has NO overworld projection at all; (10,1)+(31,8)×3 match no conv entry); shipped nearest-base-point fallback (was first-entry) + src_gz in exact test. REMAINING after #1 = #3 open-page filter (read-only RE: page-id field, WorldMapArea+0x6e=garbage, leads DAT_143d6cfc3/menu+0x151).
**Detailed findings below** (RE docs: marker_to_mapspace_re_findings.md, world_map_projection_re_findings.md).

---

**BIG ARCHITECTURE IDEA (user, 2026-06-19) — render markers in the ImGui overlay instead of injecting WorldMapPointParam rows.** This sidesteps EVERY RE wall this project keeps hitting, because all three hard problems are artifacts of using the GAME's native marker pipeline:
- **Freeze** = the game's per-displayed-page row-build (cost ∝ # on-page WorldMapPointParam rows; confirmed: master-off=0lag, parked areaNo=99=free, strip-labels=still-laggy). If we inject ZERO rows and draw our own → no page-build → no freeze.
- **Live-refresh** = the game caches its drawn icon list until a menu transition (build-fn replay, per-frame reconcile re-invoke, AND SetEventFlag(6001) ALL failed to repaint live, see [[thread8-mapopen-bottleneck-re]]). If WE draw every frame → live refresh is FREE (toggles/clustering/proximity instant).
- **Phantom dup/oversized icons** = the replan pool-row seed race ([[cluster-redesign-bugs]]). Gone if we don't use pool rows at all.

So: inject minimal/zero WorldMapPointParam, render ALL goblin markers via the existing ImGui+DX12 overlay (Present hook, F1 menu — [[thread6-overlay]]), projected onto the open map. Clustering/filtering/tooltips/search become trivial in our own code.

**RE LANDED 2026-06-19 (docs/world_map_projection_re_findings.md) — transform FOUND, awaiting runtime confirm.** No flat view-matrix (map = Scaleform/GFx), BUT pan/zoom are plain floats behind cursor+0xF0 → CS::WorldMapArea: **pan vec2 @+0x378 (x@378,z@37C), zoom f32 @+0x380**; static full-map rect @+0x350=[0,0,10496,10496]. Affine collapses to `canvasX=(markerX+panX)/zoom`, `px=canvas*(realW/1920)*scale+bias` — screen = virtual 1920×1080 (scale to backbuffer). Option A (recommended) = analytic affine from live pan/zoom + ONE-TIME {scale,bias} residual calib (cursor itself is the calibrator: marker coord +0xFC/+0x104 ↔ its drawn pixel); calib MUST hold across zoom (part of make-or-break). Option B fallback = call engine projection FUN_140d82770/FUN_14117d180 (pixel-exact but render-thread-only, fragile). Per-page: current page @menu+0x151, underground flag DAT_143d6cfc3, area@WorldMapArea+0x6e — draw only markers of open page. Reach everything from the existing cursor vtable-scan (no new AOB for read path). Probe rewritten to log pan/zoom/rect/canvas under debug_worldmap_probe (the old debug_wm_transform_scan delta-scanner REPLACED → flag now ORPHANED in schema/INI/hpp, needs cleanup). **My fix 2026-06-19: VIEW log was gated on reticle-move → would miss ZOOM (centers on reticle, coord static) + edge-scroll PAN (reticle clamped at edge); now logs on coord-move OR pan/zoom-change. Rebuilt+deployed, log cleared.** §6 RUN DONE 2026-06-19 (13:13) — PART 1 PASSED DECISIVELY. pan@+0x378 LIVE+tracks (at const zoom 0.7485, pan.x swept 2252→3283 horizontal, pan.z 4686→3940 vertical, independent), zoom@+0x380 LIVE+tracks (0.1988→0.6→0.7485→1.06→2.25→back), +0x350 static [0,0,10496,10496] throughout, CANVAS origin(0,0) size(1920,1080) confirmed. → Option A inputs confirmed; architecture VIABLE. **CAVEAT — exact affine NOT yet solvable from the log: probe logs reticle MARKER coord, NOT its SCREEN pixel, so no ground-truth (marker↔px) pair to fit {scale,bias} or settle mul-vs-div.** Evidence says doc's `/zoom` is WRONG: zoomed-out zoom=0.1988 ≈ 1920/10496=0.183 = px-per-marker-unit → zoom MULTIPLIES → true form ≈ `px=(marker−panMarker)·zoom + bias` (NOT (marker+pan)/zoom). mul-vs-div matters (can't be absorbed by a const scale/bias across zoom) → MUST resolve with one screen-px ground truth. THREE ways to get it: (a) overlay prototype + visual tune of form+scale/bias on reticle+a known grace [FASTEST, recommended]; (b) cheap+safe: log GetCursorPos in the probe IF reticle==mouse (no engine calls) → solve affine offline from next log; (c) Option B Scaleform oracle (cursor+0x90 node, FUN_14117d180/FUN_140d82070 localToGlobal) = exact reticle px but render-thread-only+fragile. NOTE user's "grep Matrix in exe" = route (c) — ALREADY mapped in the doc, no grep needed. User chose (a).

**PROTOTYPE v0 SHIPPED 2026-06-19 (route a, self-test).** New config `overlay_markers_proto` (INI, default false; set true + debug_worldmap_probe). probe exposes `get_live_view(LiveView{cursorX,cursorZ,panX,panZ,zoom})` — reads fresh from a published g_active_cursor (atomic, set by probe loop when zoom!=0), SEH-guarded → safe on render thread. Overlay (goblin_overlay.cpp draw_markers_proto) projects the RETICLE'S OWN marker coord via the affine → draws cyan ring+crosshair on the foreground draw list every frame the map is open (frame now built when g_show OR proto). F1 menu shows a "Marker proto (affine tune)" window: live reticle/pan/zoom/projected-px readout + sliders {scaleX,scaleY,biasX,biasY} + a MULTIPLY-vs-DIVIDE checkbox (default multiply: px=(m-pan)*zoom*(realW/1920)*scale+bias). SELF-TEST: tune until cyan ring sits ON the game reticle through pan+zoom → affine solved (then hardcode + add graces filtered by open-page area). Built clang-cl, deployed dll/offline (backup .bak-pre-markerproto), both flags true. dllmain starts probe on either flag. AWAITING in-game tune.

**AFFINE SOLVED 2026-06-19 (cross-capture fit).** Doc's `/zoom` + (m-pan) form was WRONG, and the reticle's screen coords are NOT +0xFC/+0x104. Cross-pattern capture (horizontal then vertical mouse sweep, logging cursor+0xFC..+0x118 + mouse + pan/zoom) revealed: **reticle screen-axis coords = +0x104 (drives screenX) and +0x108 (drives screenY)** — +0xFC is frozen (~4824, a target/snap field, ignore); +0x10C mirrors +0x104, +0x110 mirrors +0x108. Fitted model (pan coeff = exactly −1, coord coeff = zoom):
  **screenX = (+0x104)·zoom·scaleX − panX + biasX**
  **screenY = (+0x108)·zoom·scaleZ − panZ + biasY**   (panX=+0x378, panZ=+0x37C; scale≈0.99≈1)
Verified on a panned point: 0.1968·7105.85 − 546.84 + 160 = 1012 px ✓. LINEAR in bias → ONE capture calibrates. Proto rewritten: project_uv(U=+0x104,V=+0x108), hotkey **C = 1-point calib** (solves biasX/Y so reticle projects onto mouse), L = log row, X = reset; sliders scaleX/scaleY (~1.0) for residual + biasX/Y; live err readout. confirmed reticle==mouse. Built+deployed. NEXT in-game: press C once → cyan ring should snap to reticle → PAN+ZOOM verify it stays locked (nudge scale if drifts). THEN: for GRACES, U/V = gridZ*256+posZ / gridX*256+posX (axis order to confirm on first grace draw); filter by open-page area (WorldMapArea+0x6e).

**PHASE 1 GRACES — COORDINATE UNIFICATION (key finding 2026-06-20).** First grace draw: all 84 on-page graces piled in a LEFT COLUMN, not on game icons. Cause: baked graces have areaNo=10/11/… (legacy sub-area, e.g. Stormveil=10) with posX/posZ PAGE-LOCAL (±400) + gridXNo coarse(0/1) — NOT area-60 unified space the reticle/view uses (~thousands). So grid*256+pos on area-10 = tiny → cluster left. FIX: project every row to area-60 overworld FIRST via the project's existing legacy-conv. Exposed `goblin::marker_world_pos(areaNo,gx,gz,px,pz → out_area,world_x,world_z)` (goblin_inject.hpp/.cpp, mirrors get_player_map_pos: calls static project_dungeon_row_to_overworld then world=grid*256+local; no-op for area 60/61 native + area 12/40-43 underground). Overlay grace loop now: marker_world_pos → gU=world_z(+0x104/Z axis), gV=world_x(+0x108/X axis) → project_uv. Legacy dungeons w/o exact conv entry cluster at entrance (region-correct, no intra-spread) — fine for Phase 1. NOTE viewArea (+0x6e) read = WRONG (returned a float bit-pattern 0x49F40000) → page filter auto-disabled (draw all, cull off-screen); real areaNo offset still TBD. scale default set 0.987 (the 1.4% residual shows far-from-centre/zoomed-out). Built+deployed, AWAITING: do graces now land on game grace icons (region-correct)?

**✅ DATA→MARKER MAPPING SOLVED 2026-06-20 (anchor pair: Dragonbarrow Cave, area 21).** Solo #102 area=21 world=(wx=12400,wz=11985); L-hover on the real grace → U(+0x104)=6018.75, V(+0x108)=6187.28. → **worldScale = 0.5 EXACT** (wz·0.5=5992 vs U 6018; wx·0.5=6200 vs V 6187; ratios 0.502/0.499), **axes gU=wz gV=wx (NO swap)**, residual ~0.4% (conv rounding) = "red dot right next to real grace". So the FINAL grace projection = project_uv(v, wz*0.5, wx*0.5) where (wx,wz)=marker_world_pos(...). px changing wildly with zoom = NORMAL (screen pos is view-dependent; world is stable — was a non-bug). REMAINING BUG = TRANSITIVE-CONV areas: legacy dungeons reached multi-hop (area 10 Stormveil, 11 Leyndell, 16 Raya Lucaria interior, +Ashen 35) — project_dungeon_row_to_overworld does ONE hop so they get wrong overworld coords → scatter (that's why "no graces in Liurnia": Raya Lucaria=area16 transitive, misplaced to Caelid/east). Well-mapped areas (21, 60-native, single-hop conv) are CORRECT. FIX = handle transitive conv (m35→area11→area60 chain, see tools/generate_data.py:623 note) OR those show on their sub-page not overworld. NEXT: hardcode worldScale 0.5, fix transitive conv, then per-category + atlas.

**MAPPING IS PER-PAGE + OFFSET-BEARING → RE IT (2026-06-20).** Anchor-guessing FAILED to generalize: native area-60 ALSO piles in Caelid (gridXNo starts ~40 not 0 → pure scale can't align, needs an OFFSET; "dialing slides but never sticks"). Dragonbarrow area-21 (conv) worked at ×0.5 by luck; Academy area-16 (transitive) computes ~13500 should be ~3650; native area-60 offset-broken. So the render-space transform is PER-PAGE (overworld tile/region origin, legacy-dungeon transitive LegacyConv, underground 12, DLC 40-43 each differ) = the game's internal placement logic, NOT guessable from baked data. DECISION: RE it. **WROTE docs/windows_marker_to_mapspace_re_prompt.md** — Windows/Ghidra brief to find the param-row→render-marker-space conversion the icon-build uses (FUN_140a832a0/_DiscoverMapPoint/CSWorldMapPointIns position field; WorldMapLegacyConvParam transitive chain; WorldMapPlaceNameParam region origins). Pre-loaded with REAL measured anchors to verify against: Dragonbarrow(area21)→6018.75/6187.28, Academy Gate(area16)→U≈1826, native area-60 gridX≈40 offset. Deliverable = formula per page-class (or read CSWorldMapPointIns field). This is the proper fix vs per-area anchor calibration.

**✅ RE LANDED 2026-06-20 (docs/marker_to_mapspace_re_findings.md).** Formula: **render = (gridXZ·256 + pos − origin[dstPage]) · 0.5**. SCALE=0.5 STRUCTURALLY confirmed (fullRect 10496 = 82·128, world-tile 256 → render-tile 128). **origin[dstPage] = per-page offset (world units), DIFFERENT per page (page60≈5632, page61≈300)** — THE missing piece (pure scale piled all into one region). Verified exact: Dragonbarrow page61 (12338.25−300)·0.5=6019.1 vs measured 6018.75 ✓; Academy page60 (9217.7−5632)·0.5≈1793 vs ~1826. origin NOT liftable statically (placement in Scaleform/VMP thunk_FUN_1457cd3df) → CALIBRATE at runtime (1-2 graces/page, ~6-7 pages: 60,61,12,40-43); origin = world − 2·render. Z-flip possible per page (renderZ maybe (originZ−world)·0.5; player→UI transform FUN_140876140 has 0x80000000 flip). **BUG in our project_dungeon_row_to_overworld: SUBSTITUTES dst_gx·256+dst_pos + keys only (src_area,src_gx) → drops offset-from-base + srcGridZ = the area-16 wrong-region bug. FIX = base-point TRANSLATION: dstWorld = dstBaseWorld + ((gridXZ·256+pos) − srcBaseWorld), keep real dstPage (60 OR 61, e.g. Dragonbarrow→61).** Native pages (60/61/12/40-43): skip conv, dstPage=areaNo, dstWorld=grid·256+pos. Draw only markers whose dstPage==open page (menu+0x151/DAT_143d6cfc3). Handles: CSWorldMapPointMan static 0x143d6e9b0, build FUN_140a82a80 (AOB in doc), CSWorldMapPointIns vtable 0x142b487a8 (param row @inst+0x80, NO render-pos vmethod). IMPL PLAN: (1) fix conv=translation+dstPage, (2) per-page origin table + render=(world−origin)·0.5 +Zflip, (3) calibrate origins in-game via solo/L (origin=world−2·render).

**2026-06-20 PROGRESS + BLOCKER.** Step 1 (conv translation + src_gz) SHIPPED + committed (f960f81); a regression crash (game icon-build FUN_141eb9ed0, out-of-range tile from the translation on weird rows like area-11 posX=-4695) FIXED with a range guard ([0,16128]) — UNCOMMITTED. load_delay made robust (poll world_map_param_ready, num_rows>0) — committed ed347d9 + a num_rows strengthen UNCOMMITTED. Step 2 (per-page origin) tried in the overlay (origin model render=(world−origin)·scale, seeded 60≈5565/61≈301,-390, K-calibrates) BUT **by-hand calibration FAILED: the grace constellation comes out ROTATED vs the real map**, and a manual 0/90/180/270+mirror search couldn't recover it (rotating around the wrong pivot threw markers off-screen; the R·world·scale+offset model was reverted at user request). So the true transform is an AFFINE with a NON-IDENTITY 2×2 matrix: **render = M·world + T** (M=scale·rotation/reflection, T per-page) — NOT liftable statically (Scaleform thunk_FUN_1457cd3df in VMP). Overlay reverted to the simple origin baseline (visible-but-rotated). **NEW RE PLAN: docs/windows_marker_mapspace_CT_re_prompt.md — deliver a CHEAT ENGINE table (.CT) that hooks the placement thunk + logs (world_in→render_out) pairs live, so the user reads off M + per-page T at runtime instead of blind calibration.** Hand to Windows RE agent (CE-capable). Proto tools shipped meanwhile: worldScale dial (PgUp/PgDn, +Shift fine), area filter (N/B/A), solo (O + ./,), J=grace dump, L=hover log; crash fixed (clang-cl SEH noinline, see [[clang-cl-seh-noinline]]).

**MODEL CORRECTED 2026-06-19 (zoom drift fixed) — CENTER-RELATIVE, pan NOT used.** First pan-based form drifted with zoom (at zoom 2.25 err=(-267,-152)). Re-fit: the view CENTER is +0xFC/+0x100 (raw[0]/raw[1]) — NOT frozen, just moved little; +0x104/+0x108 = a point's marker coords; screen pivot = (realW/2, realH/2):
  **screenX = (markerU − centerU)·zoom·scaleX + realW/2 + biasX**   (centerU=+0xFC, markerU=+0x104)
  **screenY = (markerV − centerV)·zoom·scaleY + realH/2 + biasY**   (centerV=+0x100, markerV=+0x108)
Verified 3 pts: (3390−4824)·0.196+960=679✓, (38.7−4824)·0.196+960=22✓, (4105−2717)·0.196+540=812✓. scale≈0.986 (S=zoom*0.986; ~1.4% real, tune scaleX/Y once → holds all zooms since center-relative). bias≈0. So +0x378/+0x37C pan = IRRELEVANT for projection. WorldMapArea/zoom (+0x380) still needed for S. Proto uses project_uv(markerU,markerV,realW,realH) reading center from raw[0/1]. Deployed. AWAITING zoom-invariance confirm: cyan should track reticle across pan+zoom with err~0 (nudge scaleX/Y to ~0.986 if constant drift).

**✅ AFFINE PROVEN END-TO-END 2026-06-19 — err=(0.0,−0.1)px at zoom=2.25, scale=(1.0,1.0), NO calib.** Center-relative model is EXACT and zoom-invariant. scaleX=scaleY=1.0 (the earlier 0.986 was mouse-capture noise; true S=zoom exactly). bias≈0 (screen pivot = realW/2,realH/2 is exact). So FINAL projection (read-only, render-thread, cheap):
  screenX = ((+0x104) − (+0xFC)) · zoom + realW/2
  screenY = ((+0x108) − (+0x100)) · zoom + realH/2
where +0xFC/+0x100 = view centre, +0x104/+0x108 = marker coords, zoom = WorldMapArea+0x380. **Overlay-markers architecture VIABILITY CONFIRMED** — the one blocker is GONE. Probe latency cut (100ms tick, eager rescan ≤0.4s while no active cursor, force-rescan on cursor death) to fix the ~0.5s first-open lag. NEXT: draw real GRACES — markerU/markerV = a grace's (gridZ*256+posZ, gridX*256+posX) [confirm axis order on first draw], filter by open page (WorldMapArea+0x6e areaNo). Then per-category, atlas (data already solved via MAP_ENTRIES + diff tool). Proto code uncommitted (probe + overlay + config + dllmain) — commit when graces land.

**THE ONE BLOCKER = world→screen projection (READ-only RE).** To draw our icons ON the game map at correct spots we need, each frame, the map's VIEW TRANSFORM: the map widget's screen rect + world-center-at-screen-center + zoom (px per marker-unit). Then projecting marker (area, gridX*256+posX, gridZ*256+posZ) → screen is a trivial affine. This lives in the CSWorldMapMenu object — BUT it's READ-ONLY (we never touch the game's render, just draw on top), far more tractable than the rebuild/live-refresh RE that's blocked. FOOTHOLD: the cursor probe ALREADY located that menu object (CS::WorldMapCursorControl vtable RVA 0x2b29a90, embedded at menu+0x2DB0; cursor coords in MARKER space at +0xFC, see [[mapforgoblins-map-open-freeze]] PROBE notes + docs/re_findings_questbrowser_cursor.md). The pan/zoom/center/screen-rect fields are almost certainly adjacent in that object → extend the wmprobe to dump menu+0x2DB0..+0x400 while panning/zooming, correlate which floats change → derive the transform. Caveat: underground/DLC pages + the area→page mapping must be handled (only draw markers whose area == current page; the cursor probe is per-page so the transform is per-page too).

**ICON TEXTURES:** can't cleanly copy the game's live glyphs (Scaleform VECTOR shapes in the worldmap .gfx + VRAM atlas; grabbing the SRV + per-frame UVs = fragile RE). RE-BAKE instead: extract/pack our own icon atlas (PNG/DDS) → one DX12 texture uploaded at overlay init → ImGui::Image(atlas, uv0, uv1) per marker. Sources on hand: the FFDEC-baked icon frames the gfx pipeline already makes (export to PNG), or original ER menu DDS via WitchyBND/UXM. SPIKE SHORTCUT: no textures needed first — AddCircleFilled + category color/letter proves the projection; add the atlas after.

**SPIKE PLAN (Linux-doable, make-or-break = step 2):**
1. Extend wmprobe: dump menu+0x2DB0..+0x400 floats; pan + zoom the map; log which change → identify center(x,z), zoom, screen-rect. Derive world→screen affine.
2. ImGui-draw 3-4 dots at KNOWN marker world-pos (e.g. graces from grace_position_index.json) using that transform → must land ON the game's own grace icons + track through pan/zoom. If yes → VIABLE.
3. Migrate: ImDrawList over the map (cull off-screen, ~thousands cheap), clip to the map screen-rect, per-category colors/atlas, our clustering/filter. Drop (or minimize) WorldMapPointParam injection.

**SPIKE STEP 1 SHIPPED 2026-06-19 (awaiting in-game log).** Extended wmprobe (goblin_worldmap_probe.cpp) with a per-frame float-DELTA detector: new config flag `debug_wm_transform_scan` (requires debug_worldmap_probe). Chunked SEH bulk-read snapshots the active cursor object (0x400 win) + its parent menu object (cursor-0x2DB0, 0x3200 win) each tick, logs ONLY changed floats (old->new, plausibility+0.05 jitter filtered) to MapForGoblins_wmprobe.log. Built clang-cl+ninja, deployed dll/offline (backup .bak-pre-tscan), both INI flags set true. RUN PROTOCOL: open map, move cursor (locks active), then discrete ~1-2s steps: pan L/R/U/D, zoom in/out, idle → paste log. DERIVE: menu offset(s) moving on ZOOM=scale, on PAN=center, static 4-tuple=screen rect → affine. (cursor coord fields +0xFC/+0x104/+0x10C will spam in [cur] = known reticle, ignore.) NEXT after log = spike step 2 (draw test dots at known grace pos, verify land+track) → then PRD.

**RUN 1 (12:28) BUG FOUND+FIXED 2026-06-19.** First log: 0 TS deltas. Cause = engine keeps ~3 MIRROR cursor objects moving in lockstep (live coords confirmed: +0xFC X 3203→4338, +0x104 Z; addrs 0x1971bae30 / 0x50145350 / 0x503b8620, menus = those-0x2DB0). My single-lock ts_active thrashed between mirrors each tick → prev cleared every frame → never diffed. FIX (rebuilt+redeployed, same .bak-pre-tscan): drop single-lock, delta-scan EVERY live cursor (sane nonzero coords) keyed by address, each delta line tagged [cur@addr]/[menu@addr] so the mirror carrying the transform is identifiable; dead-cursor prev-buffers pruned per tick. NOTE +0x104==+0x10C in every line (both = Z; +0x10C is NOT Y as doc claimed). Run-1 log archived as MapForGoblins_wmprobe.log.run1-nobug.

**RUN 2 (12:33) RESULT — float-dump CANNOT reach the view transform (NEGATIVE, firm).** 171 TS ticks captured clean. Cursor object (0x400 win): ONLY the 5 coord floats (+0xFC/+0x100/+0x104/+0x108/+0x10C) change — nothing else, bounds rect is behind the +0xF0 pointer (indirect, not in flat window). Parent menu object (cursor-0x2DB0, 0x3200 win): ONLY a 0x750..0x910 band changes, and it's GARBAGE SCRATCH (+0x07ac jumps 0.079→86→-199→217371→365742→1.000 frame-to-frame = reused heap spill, not a transform). NO center, NO zoom, NO screen-rect inline anywhere. CONFIRMS RE doc §3: object at cursor-0x2DB0 is NOT CSWorldMapMenu; the real view/camera singleton is the unresolved chain (Windows-RE-blocked). So the Linux float-dump spike route is EXHAUSTED for the transform.

**PIVOT (the unblock) — self-calibrate the transform in the overlay, no engine field, no Windows RE.** The map reticle follows the MOUSE; overlay already has io.MousePos (screen px). Manual 2-point calibration: user hovers reticle onto a KNOWN grace (data/grace_position_index.json has areaNo/gridX/gridZ/x/z → marker = grid*256+pos), presses hotkey → overlay records (io.MousePos ↔ known marker coord). 2 points → solve axis-aligned affine (scale_x,scale_z,tx,tz; map not rotated). Draw ALL graces with it → must land on game's grace icons = VIABLE. Self-tracks zoom/pan/res by re-solving from live samples; sample only when settled (reject edge-clamp/gamepad/ease outliers). Converts the blocker from Windows-RE to overlay-only Linux task. Per-page: only draw markers whose areaNo==current page (page detector still TBD). DECISION 2026-06-19: user chose Windows-RE route. **WROTE docs/windows_worldmap_projection_re_prompt.md** — Windows/Ghidra brief for the world→screen view transform. Pre-loads all run-2 negatives (transform NOT inline near cursor/container) so agent skips that; strongest lead = static-RE FUN_1409bdc50 (edge-scroll pan, AOB in doc) which reads cursor-coord vs on-screen rect → screen-rect + scale fields, plus follow cursor+0xF0→bounds rect [+0x340..0x34c] (visible marker extent; check if it shrinks on zoom = direct affine). Deliverables: singleton→center/zoom/screen-rect chain (+AOB), C++ world_to_screen snippet, current-page/area field (overworld/underground/DLC pages), runtime evidence projecting a known grace onto game's icon through pan+zoom, matrix-form fallback. Overlay self-cal (B, mouse↔reticle) kept as fallback if RE stalls. NEXT: hand prompt to Windows RE agent. (data/grace_position_index.json confirmed: areaNo/gridX/gridZ/x/z, marker=grid*256+pos.)

**DATA = NO new acquisition.** Overlay draws from the EXISTING baked set `generated::MAP_ENTRIES` (src/generated/goblin_map_data.cpp, 9296 rows: row_id, WORLD_MAP_POINT_PARAM_ST data {iconId,areaNo,gridXNo,gridZNo,posX/Y/Z,textId...}, Category, object_name AEG id, lotId). Today inject builds `entries` from it → swaps into WorldMapPointParam. Migration = fork that builder output to a shared list both inject AND overlay read (single source of truth); visibility = live flag + section/category state (already computed). Marker world = grid*256+pos.

**VERSION-DRIFT DIFF TOOL SHIPPED 2026-06-19: tools/diff_map_data.py** (user chose this over in-game profiler). Parses goblin_map_data.cpp (designated-init regex; validated 9296/9296 vs MAP_ENTRY_COUNT), snapshots to JSON, diffs two bakes (.cpp or .json) by stable key (object_name→row_id): added/removed/moved/recategorized/re-iconed grouped by area+category, exit!=0 on drift (CI). BASELINE SAVED: data/map_snapshots/err-2.2.9.6.json. WORKFLOW: after each ERR update re-bakes goblin_map_data.cpp, run `python tools/diff_map_data.py diff data/map_snapshots/err-2.2.9.6.json src/generated/goblin_map_data.cpp` → catches coverage drift + the pending vanilla-DLC anomaly. Validated on vanilla-vs-ERR (+2894 added = ReforgedRunePieces/EmberPieces/QuestNPC as expected). NOT committed yet. Render-path parity (overlay-set vs inject-set) deferred = structural once builder is forked; reuse F9 dump if needed.

**ATLAS PIPELINE PHASE (noted 2026-06-19, after RE-transform unblocks position).** Match native look = NOT ImGui theme tuning (theme = window chrome only). Markers = textured quads, ImGui::Image(atlas, uv0, uv1) per marker. Because we inject ZERO WorldMapPointParam, the game draws 0 icons → only ours show → no double-icon/clash. Same source art → pixel-match.
- SOURCES on hand: real ER icon DDS (extract via UXM/WitchyBND) OR the FFDEC-baked PNG frames our gfx pipeline already produces (see tools/add_gfx_icon.py, add_gfx_icons_batch.py — they already manipulate icon frames into the .gfx). Pick whichever gives clean per-category PNGs.
- BUILD: extract per-category icon PNGs → pack into ONE atlas (PNG/DDS) + a uv-rect table (category→uv0/uv1) → upload ONE DX12 texture at overlay init (imgui_impl_dx12 single-font-SRV pattern already in goblin_overlay.cpp; add a 2nd SRV for the atlas) → ImGui::Image per marker.
- TUNING to match game: icon screen-size FIXED (do NOT scale with zoom — ER icons stay ~constant screen px; confirm in-game); subtle outline/shadow (2nd quad or AddRect); alpha blend standard; CLIP draw to the map widget screen-rect (from RE transform); gate on map-open (Present hook draws over everything incl. menus otherwise); hover-highlight + tooltip labels (current ImGui font ASCII-only → load a glyph font for FR/accents, see [[thread6-overlay]] language-switch backlog).
- FIDELITY TRAP: ER icons are VECTOR (crisp any zoom); our atlas is RASTER (fixed res) → bake HIGH-res (~128px/icon) so it stays crisp at normal sizes.
- SPIKE SHORTCUT: AddCircleFilled + category color/letter first (no atlas) → proves projection; atlas is a SEPARATE later phase. Effort = extraction + packing + size/outline/clip tuning.
- **UPSTREAM ATLAS TO REUSE (found 2026-06-20, VirusAlex commit d98ba95):** upstream already baked a category-icon RGBA atlas for THEIR config overlay: `src/generated_shared/goblin_overlay_icons.{hpp,cpp}` (ATLAS_RGBA[] raw RGBA + ICON_CELLS[]{key,col,row} + CELL size + LOGO_RGBA), generator `tools/generate_overlay_icons.py` + `tools/render_map_icons.py`. Raw RGBA embedded (no runtime decode) → upload 1 DX12 texture, ImGui::Image(uv from col/row*CELL). This is EXACTLY our Phase 3 marker atlas — REUSE the generator + baked atlas instead of building from scratch (it's per-CATEGORY icons = what marker-by-category needs). Pull via `git show d98ba95:src/generated_shared/goblin_overlay_icons.hpp` etc (upstream remote fetched).

**LEAD — overlay-markers v2 = LIVE-PARAM-DRIVEN (user-noted 2026-06-20).** MFG data = OFFLINE-baked snapshots (tools/extract_*.py read regulation.bin params + FMG + MSB + EMEVD → generated*/*.cpp, per-profile) → DRIFTS between mods (why the diff tool exists). Ambitious coherent path: read the game's LIVE param tables at runtime via SoloParamRepository — and THE PROJECT ALREADY HAS THE KEYS: `from::params::get_param<T>(L"ParamName")` (generic live param-by-name reader, used for ItemLotParam_map), `find_world_map_point_param_res_cap()` / `find_param_res_cap_by_name(L"TutorialParam")` (walk the ParamResCap list), `MsgRepositoryImp` (live FMG, used for names + PlaceName inject), live-loot reads ItemLotParam.getItemFlagId live. So driving markers from LIVE WorldMapPointParam + WorldMapLegacyConvParam (both params → live-readable → exact conv, zero drift, mod-agnostic incl randomizer) is INCREMENTAL. HYBRID is the realistic target: live params for what's in regulation (markers/conv/lots/names), stay BAKED for what's NOT a param — AEG099 gathering-node MSB geometry, EMEVD event-flag↔item correlations, MFG's iconId→category curation. Caveat: live-reading gives the SAME area-local coords + same conv need → it fixes COHERENCE/drift, not the projection math. Deferred (user: "on note ce lead").

**WHY THIS IS THE TOP LEAD:** it's the only path that solves freeze + live-refresh + phantom together, and the remaining RE is READ-only (we have a foothold), not the blocked render-rebuild. Risk: getting the transform exactly right across pages/zoom; effort: significant rearchitecture. But it's the clean ceiling-breaker for "fast + ALL markers + live". Existing overlay infra (DX12 Present hook, ImGui, world_map_open state) already in place.

## Overlay polish backlog (2026-06-20, user-flagged, underground now working)
- **Warp-teleport flash**: using the grace dialog to fast-travel shows the green overlay
  markers for ~0.5s DURING the warp/teleport transition. Gate the overlay draw OFF during
  warp. Hexinton CT already RE'd the warp-teleport state for free → reuse that flag/field to
  gate.
- **Deeproot Depths graces OFFSET** while other underground areas (Siofra etc.) align — a
  specific legacy-conv entry for the Deeproot sub-area is wrong (per-sub-area conv issue, not
  the global formula).
- **Missing native graces / extra green dots**: some green markers have NO yellow native
  grace under them, and some real graces (e.g. "Ambition's Rest") have NO green dot. Mix of
  (a) our baked data gaps and (b) Hexinton "unlock all" not unlocking every grace flag. Audit
  our grace data vs a complete grace list.
- **Off-screen culling**: the overlay already CPU-culls off-screen markers (ImGui does NOT
  cull primitives outside the clip rect — it builds their vertices, GPU clips — so our cull
  saves real work). [GRACE-DIAG] log now reports `culled-offscreen=N`.

## Grace data anomalies — TRACED (2026-06-20)
Why some graces have no green dot (user audit, e.g. "Ambition's Rest"). Source: graces come
from `data/massedit/World - Graces.MASSEDIT` (414, gen'd from BonfireWarpParam by
generate_graces.py); the BAKED `goblin_map_data.cpp` has 427 grace textIds but **11 MASSEDIT
graces are MISSING from the baked** → no overlay dot. Two distinct causes:
1. **Conv-drop (9 graces, areas 10/11/22/28 = legacy dungeons).** Includes Ambition's Rest
   (textId 100001, massedit id 12602). The bake (extract_markers.py:297-306 / generate_data)
   drops a dungeon grace when `(area, gridX)` has no LEGACY_CONV entry (`skipped_dungeon:
   continue`) — it lacks the NEAREST-base-point fallback the DLL's
   project_dungeon_row_to_overworld HAS (commit 00c8a0b). Fix: port the nearest-fallback into
   the offline bake + regenerate.
2. **Native-drop (2 graces, areas 60/61 = NATIVE, NOT converted).** textId 640028 (id 12875,
   area60, has unkC0..DC=6000 extra fields) and 690009 (id 13000, area61, uses `pad2_0=1`
   instead of `dispMask00`). Dropped by a generate_data parse/filter edge (likely the
   dispMask00 check missing the DLC `pad2_0` variant). Fix: handle pad2_0/extra fields in
   generate_data.
GREEN-dot-without-yellow-native = NOT a bug: the overlay shows ALL graces (BonfireWarp =
authoritative); yellow shows only DISCOVERED ones (Hexinton "unlock all" is incomplete). A
true live BonfireWarpParam path would fix the missing-green class (reads authoritative + DLL
nearest-fallback conv). See [[live-param-vs-baked-data]].
