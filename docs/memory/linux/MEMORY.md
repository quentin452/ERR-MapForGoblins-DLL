## Reconciliation 2026-06-28

Imported into a checkout at `master` = `931438d`. Memories that say `HEAD 4a7716d` are older than the
current local tip; keep their no-bake conclusions, but do not treat their commit pointers as current.

- [MSBE parser supersedes bake](msbe-parser-supersedes-bake.md) — SHIPPED + NO-BAKE PHASE 2 DONE (ERR; import checkout now `master@931438d`, older note said `HEAD 4a7716d`): parser is the live DiskMSB source (8381 markers); MAP_ENTRIES empty, baked=0, 61/61 off-bake (disk 8381/live 469/live-cls 74=8850); vanilla/ERTE/Convergence still baked. Killed §8 MapIns chase + FieldIns cache
- [MapForGoblins Linux build](mapforgoblins-linux-build.md) — cross-compile the MSVC DLL on Linux via clang-cl + xwin
- [Collected GEOF brute-force scan](collected-geof-bruteforce-scan.md) — RESOLVED 2026-06-27 (merge 83a723f): collected-refresh Wine lag dead. read_geof bulk-read (×550) THEN ALL collected reads dropped ReadProcessMemory → direct in-process deref (clang-cl-safe __try-around-noinline-CALL, see [[clang-cl-seh-noinline]]); wineserver IPC flood gone, ~20fps Proton stutter dead, read_wgm max 581→4ms. Also: field_probe @geom raw-addr mode for the native-getter RE (now optional)
- [Worldmap projection RE SOLVED](worldmap-projection-re-solved.md) — world→map-UV = FUN_140876140 + live WorldMapViewModel converter array (binary-verified objdump); kills baked LEGACY_CONV/affine/eyeball/group; IMPLEMENTATION pending
- [Worldmap tile fog RE](worldmap-tile-fog-re.md) — DLC "require-fragment broken" = mislabel not bug: WorldMapPieceParam = fragment reveal (gate redundant), REAL per-tile walk-fog in CS::WorldMapTiledLayer/WorldMapTile; RE brief written, Ghidra hunt pending
- [RE signatures registry](re-signatures-registry.md) — all AOBs+RVAs in src/re_signatures.hpp + [SIG] startup PASS/FAIL log = first place to look after ER update breaks resolution
- [Page-transition flicker](page-transition-flicker.md) — UG↔OW + OW↔DLC BOTH FIXED (commit 3616a35). OW↔DLC was NOT position-jump/RE: our own view-delay 1-frame lag flashed old map; reset() on group change fixed it
- [ERR save file format](err-save-file-format.md) — ERR reads ER0000.err NOT .sl2 (same BND4 format); SteamID@0x19003B4 + USER_DATA_010 MD5@0x19003A0 for save swap/patch; user SteamID64=<SteamID64>
- [Overlay gamepad cursor bugs](overlay-gamepad-cursor-bugs.md) — RESOLVED (be1bd9c): map-drift-on-cursor-move = projection centre cursor-coupled + missing canvas factor; now (pan+snapMid)/zoom + realW/1920
- [MapForGoblins work state](mapforgoblins-map-freeze.md) — fixes landed (Bug A, transitive conv/Ashen Capital, gates), coverage audit, Equipment gap drilled (by-design), pending vanilla-DLC anomaly + conv/erte + backlog
- [MapForGoblins map-open freeze](mapforgoblins-map-open-freeze.md) — ~6s freeze game-side (per-page row count, superlinear); areaNo=99 eviction proven; fragment-eviction shipped (c4445c1); NEXT=clustering (needs WorldChrMan player-pos RE); unlock-all flag-writer AOB now FOUND (EventFlag_C1, see [Thread 7]); crash-dumper works under Wine
- [Thread 3 grace toggle](thread3-grace-toggle.md) — ERR rewrote grace menu → DLL-native per-section toggle shipped (feat/section-toggle 2cbcddd, F8/F7); live-refresh RE briefed/blocked on Linux
- [QuestLog flag data](questlog-flag-data-reference.md) — MIT open-source quest→event-flag DB (34 NPCs) to make Thread 1 markers quest-aware without RE
- [Grace-menu ESD spike](grace-menu-esd-spike.md) — ERR grace settings menu = talk-ESD (not DLL); esd_dump toolchain on Linux; AddTalkListData=1:19; merge GO-but-fragile (ERR re-ships file each update)
- [Thread 6 overlay](thread6-overlay.md) — in-game ImGui+DX12 settings overlay (feat/overlay); P1-P3c shipped (F1 menu: master/section/category/cluster toggles + Save-to-INI; legacy F6-F11 removed); needs Proton verify
- [Quest Browser](quest-browser.md) — in-overlay quest-step list + per-step progress checkmarks (config::questProgress); HAND-AUTHORED original text (QuestLog data removed); 34/50 done, 16 left (Gideon/Vyke + 14 DLC need wiki); chosen over unreliable per-NPC map gating
- [Cluster runtime QUEUE](cluster-runtime-queue.md) — HANDOFF: runtime clustering shipped but buggy; user wants ONE mixed pile (checked=clustered, unchecked=normal), NOT per-category; priority bugs (disable-deletes-section, threshold-0 crash, wrong locations) + backlog (region labels, underground)
- [Cluster redesign bugs](cluster-redesign-bugs.md) — live-test bugs in by-location clustering: caves in sea, count stale permanently, projection/map-page
- [Thread 7 coverage detector](thread7-coverage-detector.md) — runtime coverage-gap detector (feat/thread7-debug-unknown): SetEventFlag + AddItemFunc hooks (AOBs from Hexinton CT) → flag↔grant correlation → per-category "unmapped item" toast; hooks verified, real-gap toast unverified; EventFlag_C1 = the in-game flag WRITER (unblocks unlock-all)
- [Thread 8 map-open bottleneck RE](thread8-mapopen-bottleneck-re.md) — freeze CONFIRMED = # on-page WorldMapPointParam rows (master-off=0lag, parked/areaNo99=free, strip-labels=still-laggy → not fonts/draw/table-size); lever=parking; 3 OPEN PROBLEMS recorded (live-refresh=NO [build-replay/reconcile/SetEventFlag all failed], lag=on-page-count, pre-existing phantom); all experiments reverted to clean master
- [Overlay-rendered markers](overlay-rendered-markers.md) — THE active project (overlay IS the map). Import checkout is `master@931438d`; older body references `HEAD 4a7716d`. LATEST (2026-06-27): NO-BAKE PHASE 2 DONE (ERR baked=0, 61/61 off-bake via DiskMSB+live, see [[msbe-parser-supersedes-bake]]); F1 ITEM/OBJECT SEARCH (results+locate+cross-page auto-switch, English alias index + token match + accent-fold); LIVE PARAM-OFFSET source-of-truth (offsets read from exe via find-what-accesses); LIVE TAXONOMY CLASSIFIER (goodsType+sortGroupId). Also: unresolved-RE-sig F1 error, EN error windows, font arrow/math glyphs, discovered-grace fragment-bypass. Backlog: region-name banners, circle-fallback icons (FFDEC), migrate vanilla/ERTE/Conv to no-bake.
- [Overlay pending RE](overlay-pending-re.md) — both resolved/superseded. Loot-flag CLOSED (no bug: ≥2³⁰ fix; 0/N residual = vanilla-save-ported-to-ERR regulation mismatch). Distance-adaptive REWIRED onto the native map CURSOR (commit 65580d9: LiveView.cursorX/Z, all pages, no physics) — underground player-pos RE (ccb2227 reverted, broke it) now DEFERRED, only a future minimap needs it
- [clang-cl SEH noinline](clang-cl-seh-noinline.md) — clang-cl ELIDES __try around a raw LOAD (even noinline) → 0xC0000005. TWO safe fixes: ReadProcessMemory (kernel, can't elide, but Wine-IPC slow) OR (better, 2026-06-27) __try around a noinline CALL (raw_copy/raw_store8) which clang-cl PRESERVES → drop RPM for free in-process reads (killed the Proton collected-stutter, see [[collected-geof-bruteforce-scan]])
- [Live-param vs baked data](live-param-vs-baked-data.md) — drop generated/* snapshots, read params/FMG live (SoloParamRepository/MsgRepository) to kill per-mod drift; hybrid (params live, MSB/EMEVD baked)
- [Disambiguate bug symptoms first](disambiguate-bug-symptoms-first.md) — ask sharp clarifying questions to pin a vague symptom BEFORE coding/building/dialing; ambiguity burned many build-test cycles
- [Overlay icon atlas](overlay-icon-atlas.md) — PNG-embedded + runtime stb decode (commit 5db8995); blue dots = 6 BY-DESIGN circle-fallback categories (talismans/whetblades/utilities/throwables/world-maps/quest-NPC), NOT a bug; regen needs FFDEC
- [Minimap future feature](minimap-future-feature.md) — FOUNDATION SHIPPED (2026-06-21 #3, commit 52288bf): overworld north-up corner HUD, opt-in; doubles as the map-CLOSED player-pos test; heading + underground RE still needed. (was BACKLOG idea): in-game corner minimap HUD reusing worldmap overlay infra + the now-live player world-pos; prereq = reliable player pos (phys read showed overworld local=0,0) + heading RE
- [ERR underground grace gate](err-underground-grace-gate.md) — ERR's real cave-grace gate = BonfireWarpParam.iconId (1=normal, 44=underground, 93 graces), NOT areaNo. SHIPPED + VERIFIED all maps (commit cbbec2e 2026-06-23): DLL reads graces LIVE from BonfireWarpParam (new BONFIRE_WARP_PARAM_ST.hpp), gate=iconId==44, old areaNo==31 + baked grace MASSEDIT/generate_graces.py DELETED; also fixed pre-existing grace anomalies. ERR regul decrypts on Linux (AES managed, not Oodle-blocked)
- [Linux RPM walk danger](linux-rpm-walk-danger.md) — per-node ReadProcessMemory walks = Wine FREEZE cliff (free on Windows); bulk-read whole struct (1 RPM/node not ~6), throttle re-walks, wrap game-thread walks in GOBLIN_BENCH_QUIET (else invisible to bench); don't gate production paths behind dev dumpIconTextures
- [Param struct offset verification](param-struct-offset-verification.md) — hand-writing a from::paramdef struct: derive offsets from LIVE in-memory row bytes (one-shot reinterpret_cast dump), NOT the XML/SoulsFormats (both mis-anchor ±1); pack(1), partial head OK; ER rows byte-packed (unaligned floats); packed bitfields share a byte (dispMask00/01/02 = bits 0/1/2 → use &0x7 not &0x3). UPDATE 2026-06-27: for single FIELDS, prefer reading the offset LIVE from the exe via find-what-accesses (*_ACCESS AOBs in re_signatures.hpp, resolve_field_offset) — patch-resilient; shipped for goodsType/sortGroupId/pickUpItemLotParamId/isEnableRepick/BonfireWarp textId1
- [Native icon render pipeline](native-icon-render-pipeline.md) — real ER icons on markers: pipeline SOLVED, Boss+Grace wired. CEILING REACHED (2026-06-23 [MAPICON-AVAIL] dump): per-category map-point iconIds (369-443) exist in sheet layout but vanilla never DRAWS item map-pins → never harvested → map_icon_rect fails; only iconId 1-89+208-261 resident (graces/dungeons/regions/DLC/MP); rects irregular-packed; SB_MapCursor.layout asset-blocked on Linux. Mod shows what vanilla map doesn't → most categories have NO ER symbol. Items=inventory icons (real); rest=atlas. Next-best = inventory-icon coverage, NOT map symbols
- [Extra graces in Siofra](extra-graces-siofra.md) — ROOT-CAUSED + PARKED (non-priority, 2026-06-23): phantom graces = BonfireWarpParam rows for unplaced/cut bonfires. 2 classes: nameless (Siofra, fix=lookup_text null gate, watch dllmain order) + named cut ("Underground's End"@Lake-of-Rot, param-indistinguishable from real, needs MSB-entity allowlist = Oodle-blocked on Linux, interim=curated blocklist). NOT a regression. Diagnostic reverted, master clean
- [Loot identity live / ERR additive](loot-identity-stable-err-additive.md) — SUPERSEDED 2026-06-27: ERR loot now FULLY off-bake (no-bake Phase 2, baked=0) — position came off-bake too via the DiskMSB parser + live recovery passes, NOT the deferred runtime chase below. ERR ADDS items (not swap-randomizer); loot identity doesn't drift but is now read LIVE anyway (runtime-by-design: resolve_loot_item_textid drives label+FMG-name-preload, baked textId1 stripped from 4316 rows, shipped; needed encode_live_item +100M fix first); POSITION = (was baked, RE validated 0/30305 + in-process [LOOTPOS] probe byte-exact (commit 4818d43). REMOVE-BAKED killed: global all-pos structure REFUTED (e1b502b, per-tile streaming no registry), census = 34% asset/49% lot-needs-join/16% geom-blind → live-only loses 16% + 49% anonymous; baked FUSES MSB-Events+EMEVD runtime lacks. Asset→ItemLotID runtime link EXISTS (4a809cc, FieldIns pool lotId@+0x50) → enables COVERAGE SUPPLEMENT/explore-cache (loaded-only), NOT replacement. Follow-on RE: STEP1 SOLVED + STEP2 lead (be1b018 Ghidra, supersedes 600c60c no-link verdict): FD4Singleton RB-tree of all loaded field-instances iterable from static slot er+0x3d7b0c0 (reg→[+0x10]→+0x720 map, node+0x28=instance); STEP2 join path A = embedded CSGrowableNodePool@instance+0x3A8→child FieldIns lotId@+0x50 (earlier RPM missed it), path B = walk registry. in-process probe (295fb1a..160a01a) EXHAUSTED both: path A REFUTED (embedded asset+0x3A8 pool EMPTY at tile-load → loot FieldIns open-time-only), path B INCONCLUSIVE (documented er+0x3d7b0c0 chain reaches only ~2 nodes, target lot not found, my layout reading off). Escalated to GHIDRA (windows_fieldins_registry_layout_and_preopen_re_prompt.md). Ghidra ANSWERED (da19285 find_fieldins7/8): path-B fixed = deref container=*[sub+0x720], self-reg map@container+0x18 (head=*[+0x20] size=*[+0x28]), instance=node+0x30 (node+0x28=callback ptr, the old misread). NO treasure class — lotId carrier=geom-item; RESIDENCY=sealed chests OPEN-time → premium limited to PLACED/DROPPED world loot, sealed stays baked. REVERSED (da513922): link ALIVE for LOADED loot — resident node {lotId@+0,flag,FieldIns*@+8} w/ self-validating sig *(u32)(FieldIns+0x50)==lotId, + co-resident MapId@node−0xD8 + local pos@node−0xD4 (→abs world pos), owner CS::MapIns (lot is INLINE field not pool→path-A red herring). §8 brute-scan EXHAUSTED (a275bde..bb63808): incl MEM_MAPPED, target chest lot still only in ItemLotParam (sigNodes=0, not resident this session); generic node scan too slow under Wine; アイテム-name too common (garbage +0x50). ENUM ANCHOR RESOLVED (188d977, offline-validated): WorldMapManImp (vt 0x2a8f918, slot er+0x485cbb8, 796 guard-refs) → +0x5d0 WorldBlockMap[] → +0x110 pool<MapIns*> → MapIns (vt 0x2a8d6d8); all RTTI confirmed on-disk. STEP0 ship-ready = vtable-scan MapIns = 343 (live-proven). Per-record = inline, found by FieldIns+0x50==lotId sig, MapId@node−0xD8 pos@node−0xD4. Scope = placed/spawned world loot (sealed=baked, §8). WIRING ATTEMPTED (fee828c..b01f551 [MAPINS] walker): ENUM SOLVED in-process (vtable-scan er+0x2a8d6d8=342, crash-safe) BUT static chain slot er+0x485cbb8 WRONG (vtable≠WorldMapManImp) + RECORD NOT in MapIns body (0 records at full 342, even post-chest-open → not timing/spawn) → node in a CHILD ALLOC (≥1 ptr hop), da51392's +0x460 was a session offset. ESCALATED → ★ AGENT SOLVED (ed8fa0c, live RPM): reach = MapIns+0x460 node {lotId@+0,flag@+4,FieldIns*@+8}, SOLE validator *(FieldIns+0x50)==lotId (my 0-records was the アイテム name gate too strict — dropped). MapId@node-0xD8, pos@node-0xD4, absPos=gridXZ·256+local. WIRED in-process (106d988, +0x460 + hedge window). SCOPE = loaded spawned/opened/placed loot only (1/343 MapIns item-bearing = opened chest; sealed=baked §8). NEXT = verify in-game → wire resolve_loot_item_textid → explore-cache. Ships now = baked-partName-join. diag off. STRATEGY LESSON: brute mem-scan = DISCOVERY only, never production; LOTSCAN "failed" in-process = uncontrolled state + coverage gaps not a tooling law. Standard = (1) ANCHOR CHAIN from static base (MapIns anchor-walk, vtable-scan STEP0=343) or (2) HOOK creator/consumer (like grace suppression); retire diag_lot_memscan for the anchor-walk. ICON-DOUBLING (separate): only native-drawn cats double → Grace+Boss suppressed (boss = dispMask clear on WorldMapPointParam 5100, eadc1b4 default ON verified); loot/items have no native map point → overlay-only, no double. Exe-scan: slot 0x3d7b0c0 CONFIRMED + RTTI from disk (vt 0x2a86860=CSWorldGeomStaticIns). Diags dormant off. Future-proof = re-diff per ERR bump
- [Engine direction decisions](engine-direction-decisions.md) — STANDBY strategy (2026-06-23): pick ONE primary of ER-mod (finish/polish) / EvoCraft (craft, keep PURE) / new 2.5D engine (ship a product). LOCKED: a 2.5D+ImGui engine = SEPARATE repo not in EvoCraft; combat-skill Souls feel → 2.5D enough (Hades), no 3D; reuse only within a genre family. New-engine plan = 2.5D + continuous plane sim, "Combat Room" first slice, reuses MFG DX12/atlas/ImGui bricks
- [One-DLL externalize mapdata](one-dll-externalize-mapdata.md) — PARTLY MOOTED 2026-06-27: ERR has NO bake anymore (no-bake Phase 2 done) → reads markers live from its own map/MapStudio/*.msb.dcx + game memory, already one-build per-mod. Real path to one-DLL-for-all = migrate vanilla/ERTE/Convergence onto the same DiskMSB+live pipeline (they still ship baked MAP_ENTRIES), NOT externalize the bake. Original parked externalize design retained for context
- [Entity-radar foundation](entity-radar-foundation.md) — future real-time player-bubble radar: loaded-only RE already done fits it perfectly; ASSET/chest radar = ZERO new RE (reuse geom walk + FieldIns→lotId + transforms + player pos); ENEMY radar's only gap = the abandoned WorldChrMan ChrIns enum. Radar=live bubble, world map=baked — don't mix
