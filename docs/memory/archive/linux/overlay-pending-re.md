---
name: overlay-pending-re
description: Overlay-map RE blockers both resolved/superseded. Loot-flag CLOSED (no bug; regulation-mismatch on ported saves; ≥2³⁰ fix). Distance-adaptive now keyed on the native CURSOR (all pages, no physics) → underground player-pos RE deferred (only a future minimap/you-are-here needs it)
metadata: 
  node_type: memory
  type: reference
---

🆕 PLAN-B SUPPRESSION RE COMPLETE (commit 253c5c9, `windows_suppress_native_pins_runtime_re_findings.md`): the static mgr WAS right — `R15 == [er_base+0x3D6E9B0]` (caller FUN_140623410 @0x624296 `MOV RCX,[0x143d6e9b0]`). std::map _Tree at **mgr+0x390**: `_Myhead@+0x398` (a POINTER, not size — the [PINSET] bug), `_Mysize@+0x3A0`; node key@+0x20 / value(CSWorldMapPointIns*)@+0x28; DFS walk. Sibling mgr `[er+0x3D6F558]` (FUN_140aba1a0) = likely the player-marker/objective system (separate → safe). Suppression approach = the show-predicate areaNo→99 filter (reuses parking) OR clear the tree. **NEXT for B: re-add a SAFE +0x398 walk (ReadProcessMemory, NOT __try) to confirm contents, then implement suppression.**
⚠️ When the native-pin-suppression runtime RE returns (docs/re/windows_suppress_native_pins_runtime_re_prompt.md), the new +0x398 walk MUST use ReadProcessMemory / rpm<T>, NOT `__try` — the [PINSET] diag (commits 7f3d641/0c8f532) crashed with 0xC0000005 (clang-cl elided the __try walking wrong offsets) and was REMOVED (commit 02caf53). [[clang-cl-seh-noinline]].

✅ DONE — RE TASK (user 2026-06-22): KILL the baked LegacyConv → engine runtime world→map-space projection. **RESOLVED + IMPLEMENTED + IN-GAME VERIFIED 2026-06-22** (`config::liveProjection` default true; worldmap_probe::project + goblin_legacy_fold.cpp live fold + DLC eyeball deleted; "fixed hundreds of misplaced objects in a blink"). Baked LEGACY_CONV/affine kept ONLY as map-closed fallback; only leftover = delete baked table after rigorous live-fold cross-check. Full detail [[worldmap-projection-re-solved]]. (was:) Brief: `docs/re/windows_world_to_mapspace_projection_re_prompt.md` (commit e69d0a0). Replace LEGACY_CONV + the `mapU=worldX-7040/mapV=-worldZ+16512` affine + the DLC-UG eyeball + marker_group_from with the game's own per-icon projection (leads: FUN_140a82a80/FUN_140a832a0/FUN_140d82770/affine site er+0xa8397e/CSWorldMapPointMan=[er+0x3D6E9B0]). CONTEXT: WorldMapLegacyConvParam (game param, base+DLC) has ZERO area-19 rows → the Chapel of Anticipation (m19, prologue) has NO overworld position; our baked projection falls back to RAW → a mis-placed "Chapel of Anticipation" cluster right of Stormveil at zoom ~2.25 (the reported bug 3). The simple gate (skip srcArea==19) was REJECTED by the user in favour of this RE. Clustering position is NOT zoom-dependent by design (grouping by grace, world-anchor/centroid placement) — the apparent zoom-drift = a fixed WRONG geographic spot from the bad area-19 projection.

Two RE tasks block overlay-map features (both have written RE prompt docs in the DLL repo `<repo>/docs/re/`). Hand these to the Windows RE agent.

## 1. Player position — ✅✅✅ DONE + VERIFIED IN-GAME (2026-06-21 #3, doc 94df64d + fix a2e3c27; un-gate c176b0f). Player overlay dot sits EXACTLY on the vanilla yellow pin OW+DLC+UG; minimap + distance-adaptive confirmed working on every page. Saga CLOSED. Player-pos HUNT debug viz STRIPPED (commit 09ed8ac): removed the magenta PLAYER dot + cyan grace dots + [PLR] HUD + the [PLAYER] map-pos debug log. KEPT the distance-zone near/far rings + centre dot (cluster_debug_radius) = distance-adaptive tuning aid.
**FRAME SOLVED: `LocalPlayer+0x6C0`(X)/`+0x6C8`(Z) IS the exact `WorldMapPointParam posX/posZ` frame on EVERY page.** RE validated standing on the Bois-des-Fidèles grace (area 12 UG): live +0x6C0/+0x6C8 = 1437.8/1519.0 == the grace's baked posX/posZ (1438/1519); worldX = MapId_gridX*256 + [+0x6C0] = 1949.8 ≈ wx 1950. So the player feeds the SAME bridge+projection as a marker. **FINAL FIX (commit a2e3c27):** get_player_map_pos sets `tmp.posX=[lp+0x6C0]; tmp.posZ=[lp+0x6C8]` then `project_dungeon_row_to_overworld(conv_underground=true)` + `gridX*256+posX` — identical to marker_world_pos → correct overworld AND underground, map-open AND map-closed. **Root cause of the old UG bug (confirmed): the local was read from geomMgr+0x70/+0x74 (physics-block frame, off by the block origin +1432/-816/+1544) — geomLocal is NOT used now (user-confirmed unreliable).** WorldChrMan from er_base+0x3D65F88 (== WCM_FINDER AOB; both `0x143d65f88`). ChrIns reads (0,0) only during a load → get_player_map_pos returns false then. Float-window diagnostic removed. **VERIFY in-game: minimap overworld → player-relative markers align + track; (underground now also valid — minimap still overworld-gated in draw_minimap, can un-gate later); distance-adaptive underground also now unblockable (still gated overworld-only, separate).** This unblocks the minimap, the worldmap "you are here" dot, and underground distance-adaptive.

## 1z. (history) Player position chain discovery (commit cc53594 doc + 29c0dde first fix)
**CHAIN (runtime-confirmed by the Windows RE agent — CE find-what-accesses + live RPM walk, NOT a static guess):** `LocalPlayer = [WorldChrMan + 0x1E508]`; player **X/Y/Z = float [LocalPlayer + 0x6C0 / +0x6C4 / +0x6C8]** (Y=height; 2nd render copy at +0x6D4..). WorldChrMan = the existing `WCM_FINDER` static (`er_base+0x3D65F88`, already PASS). **Correct on EVERY page incl. underground, AND updates map-CLOSED** (the geom-mgr block-local read did not → that was the minimap "shows objects not here" bug). Doc: `docs/re/windows_player_pos_RESOLVED_re_findings.md`. Writer RVAs 0x659F3D/0x65A3BD (only if a hook ever wanted). The old 0x10EF8→+0x6B0 chain was dead (offset drifted, field moved +0x6B0→+0x6C0).
**FIX SHIPPED (commit 29c0dde, built+deployed md5 31e8c2, NOT in-game verified):** `get_player_world_pos` + `get_player_map_pos` (goblin_inject.cpp) now read the ChrIns vector via `probe_player_seh` (`[wcm+0x1E508]+0x6C0`). get_player_map_pos: position from ChrIns, area/tile still from the MapId singleton. **⚠️ OPEN: §4 FRAME (world vs map-space).** Assumed OVERWORLD ChrIns X/Z == marker frame (tile*256+local absolute world) → fed in directly. **`[PLR2]` log (on area change) prints chrIns vs MapId tile*256 vs old geomLocal → reads the log to confirm:** chrIns≈tile*256+geomLocal → frame right (done); chrIns≈geomLocal(0..256) → block-local → world=tile*256+chrIns; else constant offset → apply. **Underground/DLC ChrIns ≠ unified-overworld marker projection** → NOT converted (minimap + distance-adaptive are overworld-only). **IN-GAME PROGRESS (2026-06-21 #3, log-confirmed on this Linux/Proton box — read `<ERR_ROOT>/dll/offline/logs/MapForGoblins.log` directly):** the chain WORKS in-process + tracks movement + updates map-CLOSED. Reading at Smoldering Church (overworld tile 46,40): `[LocalPlayer+0x6C0]` X≈**-69.7**, Y≈151.3(height), Z≈**3.8** (2nd copy at +0x6D4 identical). **These are SMALL → tile-LOCAL, NOT absolute world (~11700).** Note: chrIns reads **0,0,0 during a load/fast-travel** (player not yet positioned) then goes live. The `0x143d65f88` slot (= er_base+0x3D65F88, == the WCM_FINDER AOB result; both same) is correct — earlier 0,0,0 was the load-timing, not a wrong slot. **⚠️ geomLocal (CSWorldGeomMan +0x70/+0x78) is NOT reliable (user-confirmed wrong) — do NOT use it as a fallback.** A float-window dump of LocalPlayer (0x600..0x73C) found NO absolute-world field there (max ~78) → only tile-local pos at +0x6C0/+0x6D4.
**INTERIM CODE (uncommitted, NOT built/deployed):** get_player_map_pos = `world = MapId_tile*256 + chrIns_local` (chrIns-only, no geom fallback, return false when chrIns=0). This is a BEST-GUESS frame.
**OPEN = the exact FRAME/TRANSFORM → handed to Ghidra/RE.** Brief written: `docs/re/windows_player_pos_mapframe_re_prompt.md` — asks the agent to decompile the native yellow "you are here" dot's world→map-UV function (leads: FUN_140d82770, cursor+0x90, ctor FUN_1409bc5b0/setup FUN_1409be5e0) → report which field it reads, tile-local vs absolute, and the exact transform constants (to compare vs our marker transform mapU=worldX-7040 / mapV=-worldZ+16512). Goal = one player→map-space value that serves BOTH the open worldmap AND the minimap. **NEXT: user pastes the RE findings → finalize get_player_map_pos frame, build/deploy, verify minimap tracks + worldmap "you are here" dot.** Add the chain to [[re-signatures-registry]] if a new AOB ever needed (currently reuses WCM_FINDER).

## 1b. (superseded history) Underground player position — was SUPERSEDED by the cursor approach
**Prompt: `docs/re/windows_underground_player_pos_re_prompt.md`; FINDINGS: `docs/re/windows_underground_player_pos_re_findings.md` (commit ec5586d).**
ROOT: the LocalPlayer field DRIFTED `+0x10EF8`→**`+0x1e508`** (the old one is dead → null underground = the NaN). The engine builds player map-pos the SAME way everywhere via the live `*(WorldChrMan+0x1e508)` → `+0x58`→`+0x10`→`+0x190`→`+0x68` physics Vec (X@+0, **Z@+0x8**, +0x4=height). The old `get_player_map_pos` read CSWorldGeomMan 2D block-local X@+0x70/Z@+0x74 (correct overworld, block-anchor ORIGIN underground = the garbage). **FIX (ccb2227):** `get_player_world_pos` base 0x10EF8→0x1e508; `get_player_map_pos` now reads the live physics Vec (Candidate A, valid every page), falls back to the CSWorldGeomMan 2D read (X@0x70/Z@0x74) when the chain fails; bridges with MapId tile (gridX*256+localX) as before. **`[PLAYER-MAPPOS]` one-shot log** reports area/tile/local + `src=phys+0x1e508` vs `mgr-fallback`.
⚠️ NOTE the resolved `WORLD_GEOM_MAN_SLOT` = CSWorldGeomMan (2D, Z@+0x74), NOT the 3D map-point manager `DAT_143d69ba8` (Z@+0x78) — RE's "Candidate B +0x74→+0x78" does NOT apply to our manager; that's why the fallback stays +0x74.
**FIX ccb2227 REVERTED (commit 64605ac) — it broke underground + read overworld `local=(0,0)` (the +0x1e508→+0x58→+0x10→+0x190→+0x68 Vec read 0,0 on overworld; chain resolves `src=phys` but the sub-offset/node is wrong on this build). Overworld clusters were CORRECT before it (they use grace anchors, not player pos); underground broke because get_player_map_pos feeds region/projection. Reverted to the CSWorldGeomMan read (overworld-correct, underground-origin) = prior working state.**
**CURSOR APPROACH TRIED + REVERTED (commit 65580d9 → reverted c706144).** Keyed distance-adaptive on `LiveView.cursorX/cursorZ` (cursor +0xFC/+0x104). User rejected: that's the MOVABLE RETICULE, NOT what we need. **What we need = the native YELLOW PLAYER-DOT** (the "you are here" arrow the game draws at the player's exact map position — see the user's screenshot). cursorX/Z ≠ player.
**STILL HUNTING (2026-06-21 PM, commit 394264a md5 47ed…): CSWorldGeomMan reads ORIGIN underground → need the real DAT_143d69ba8.** Visual diag proved: overworld dot ≈ on yellow dot (manager = player pos, micro precision); underground the candidate dot was off (Ancestral Woods/Siofra: raw tile=(2,0) local=(1.5,-5.3) ≈ ORIGIN → conv projected to bogus overworld (42,32)). = the RE-warned 2D-CSWorldGeomMan-reads-origin-underground. So our WORLD_GEOM_MAN_SLOT is NOT the map-point manager. **AXIS-BUG REFRAME + FIX (RE commit 2e8304d `windows_yellowdot_player_pos_re_findings.md`; fix commit ecc14dd, md5 c161…, NOT in-game verified).** The Vec getter FUN_14045e390 PROVES the layout: **X@+0x70, Y(HEIGHT)@+0x74, Z@+0x78**. `get_player_map_pos` read **+0x74 as Z = HEIGHT** → masked overworld (flat→Y small→tiny error) but broken underground (deep→Y swings→dot thrown far). The underground values were NEVER origin — they're sub-area-local (small = local frame); +0x74-as-Z was the bug. FIX: probe reads **lz from +0x78** (was +0x74); conv_underground=true bridge already present (55bc9cc). Also: the earlier [YELLOWDOT2] phys (0,0,0) was a SHORT READ — real leaf is `[[[[*(WCM+0x1e508)+0x58]+0x10]+0x190]+0x68]+0x70` (one more deref; c+0x68 is a ptr, Vec at +0x70). Re-added a lean PLAYER dot (cluster_debug_radius, every page) to validate RE §1: does the dot sit on the yellow dot + TRACK MOVEMENT underground. **IN-GAME RESULT (2026-06-21): axis fix INSUFFICIENT underground — dot still far off (Ancestral Woods).** Decisive: drew EVERY grace anchor as cyan dots (page-gated via new `marker_group_from` helper, commit 69527c1) → **the cyan graces land ON their real graces underground** = the grace/marker projection pipeline is CORRECT. So the bug is the **player INPUT frame, NOT projection/axis**: we reconstruct the player from the MapId tile (singleton+0x2c, coarse underground block id e.g. area12 tile (2,0)) + manager local (+0x70/+0x78), which underground is a DIFFERENT frame than the WorldMapPointParam grid the graces use → marker conv picks the wrong cell/entrance. Overworld works (MapId tile == the 60-grid). **→ pursue RE §3 = the CURSOR POSITION PROVIDER (`cursor+0x90` provider + `FUN_140d82770` world→map-UV) that draws the yellow dot = authoritative same-frame source on every page. Prompt narrowed (commit 0164acb).** Debug viz (magenta PLAYER dot + cyan page-gated graces + [PLR] HUD) still in, gated cluster_debug_radius. Overworld player-pos ≈ correct (micro). NEXT path: cursor-provider RE (map-open; minimap map-closed = separate). (was: dot on yellow dot + walk-track? YES → drop gate (`cursor+0x90` provider + `FUN_140d82770` world→map-UV, ctor FUN_1409bc5b0/setup FUN_1409be5e0 — that's literally what draws the dot, map-open only).** Supersedes the prior findings (their +0x74=Z + the +0x68 direct read were both wrong).
**(earlier) handed to a RUNTIME-CT RE PROMPT (commit 6a68c1e, `docs/re/windows_player_pos_runtime_ct_re_prompt.md`).** Ran [YELLOWDOT2] underground (Ancestral Woods): **DAT_143d69ba8 = the SAME slot as CSWorldGeomMan** (both er_base+0x3d69ba8; MAPPOINT_MGR_BUILDER AOB ambiguous=2 matches, resolved ok=false) → reads ORIGIN underground at +0x70..+0x88; **physics chain WCM+0x1e508→+0x58→+0x10→+0x190→+0x68 derefs OK but reads (0,0,0)** on this build (wrong leaf offset, also 0,0 overworld). So the prior findings doc offsets are WRONG for app 2.6.2.0/ERR 2.2.9.6 runtime. The crash (cfe09c1, 0xC0000005) was the `__try` elision ([[clang-cl-seh-noinline]]) → redone with `rpm<T>` ReadProcessMemory (crash-safe). **DECISION (user): stop guessing offsets on Linux; the new prompt asks the Windows agent for a Cheat-Engine .CT + the AOB/deref/offset recipe, validated live vs the yellow dot, for overworld + m12 + DLC m40-43, map-open AND map-closed (minimap needs map-closed).** Ground truth = the native yellow "you are here" dot. **MEANWHILE: overworld distance-adaptive WORKS + stays (gate `!(open_grp&1)` kept); underground = normal clustering. Debug scaffolding (YELLOWDOT*/HUD/dots/MAPPOINT sig/rpm probe) still in the code, gated off (cluster_debug_radius=false) — strip when the CT lands or before shipping. [SIG] MULTI MAPPOINT_MGR_BUILDER warning is harmless noise.** get_player_map_pos conv_underground=true fix KEEP (helps overworld dungeons).
**(earlier) BREAKTHROUGH (visual diag, commits 0b3c407 HUD + 55bc9cc fix): manager IS the player pos overworld; underground PROJECTION FRAME bug — get_player_map_pos now passes conv_underground=true (matches markers).** The candidate dot (tile*256 + mgr local) landed ~ON the native yellow dot OVERWORLD (micro precision only — +74≈+78≈right, manager confirmed = the player position) but grossly OFF-SCREEN UNDERGROUND. ROOT: `get_player_map_pos` projected with `conv_underground=FALSE` while the markers (`marker_world_pos`/`grace_anchor_world`) use `conv_underground=TRUE`, which unifies base underground (area 12) into the same overworld map-space. Without it the underground player landed in the raw area-12 frame = wrong page corner. **FIX (55bc9cc): get_player_map_pos passes conv_underground=true.** Also dropped the misleading debug centre-line (it swung on pan, read as "wrong place"; the crosshair itself glues to the map like a marker). **VERIFY (md5 60ac…, cluster_debug_radius on): magenta PLAYER crosshair sits on the native yellow dot UNDERGROUND too now (HUD shows player_ok/proj_area/px/ON). If yes on both pages → remove debug + drop the overworld-only cluster gate → distance-adaptive on the real player pos, all pages.** Z offset moot (manager local correct as-is via +0x70/+0x74 → marker_world_pos; the +78-vs-74 question doesn't matter, error sub-pixel).
**(earlier) HUNT (commit 730b330): the yellow-dot lives in the map-point manager.** RE findings (ec5586d) §2 say the engine builds the player map-pos into the map-point manager at **+0x70(X)/+0x78(Z)** (Z is +0x78; +0x74 = HEIGHT) — that's the yellow dot. AMBIGUITY: our resolved `WORLD_GEOM_MAN_SLOT` (≈er_base+0x3d69ba8) may be that 3D map-point manager (Z@+0x78) OR the 2D CSWorldGeomMan (Z@+0x74, reads origin underground). To disambiguate WITHOUT guessing (per [[disambiguate-bug-symptoms-first]]): **`[YELLOWDOT]` one-shot-per-page diag** logs mgr `+0x70/+0x74/+0x78/+0x80/+0x84/+0x88`, triggered from the render loop on EVERY open page (incl. underground) when `cluster_debug_radius=true` (set true in the user INI). **NEXT (needs user run): open map OVERWORLD + UNDERGROUND at a KNOWN spot, share `[YELLOWDOT]` lines.** The offset triple that matches the real location on BOTH pages = the yellow dot → wire get_player_map_pos to read it (likely just +0x74→+0x78 if it's the 3D mgr; or resolve DAT_143d69ba8 separately if not), then drop the overworld-only gate. The +0x1e508 physics chain (ccb2227, reverted) read 0,0 overworld → abandoned for this manager-read path. Helpers player_map_tab / grace_anchor_tab / grace_anchor_tile kept. Related: [[mapforgoblins-map-open-freeze]], [[minimap-future-feature]] (same yellow-dot pos).

## 2. Collected/uncollected loot flag (CLOSED 2026-06-21 — NO BUG; ≥2³⁰ fix shipped, residual = regulation-mismatch on ported save)
**Prompt: `docs/re/windows_collected_loot_flag_re_prompt.md`; FINDINGS: `docs/re/windows_collected_loot_flag_re_findings.md` (commit ca7f4e9).**
RE answer (static Ghidra, app 2.6.2.0/ERR 2.2.9.6): TWO causes of the 100%-save over-report.
**#1 (the big one) FIXED (commit 204b62b, md5 06f2…, NOT in-game verified):** ER temp/event-local flags (≥2³⁰) and `-1` ("always re-droppable") have NO save-backed bit — `IsEventFlag`/`FUN_1405f9400` maps id→group=id/[mgr+0x1c]; a group absent from the persistent manager returns false with NO high-id guard, and temp groups are never persistent → those flags read false FOREVER. Their lots are REPEATABLE loot (golden runes/crafting/consumables = the worst "broken" cohorts). FIX: `resolve_loot_flag` (src/goblin_inject.cpp) now returns **0** when the resolved flag `== 0xFFFFFFFF` or `>= 0x40000000` → caller treats marker as NOT collectible (census `if(!flag)continue` excludes from total; `marker_done` short-circuits on collected_flag==0 → no gray). Removes the dominant over-report. **Offsets were CORRECT all along** (NOT the bug): @0x80 lot-wide getItemFlagId, @0x60 slot01, @0x04 lotItemId02, row 0x98 BLOCKED layout (web Paramdex DS3-interleaved layout is WRONG for ER). Q3: textDisableFlagId* = map-icon gate via same IsEventFlag, SEPARATE field from item-obtained flag (can diverge under ERR randomizer).
**#2 RESIDUAL (the BIG remaining over-report — LOG-VERIFIED 2026-06-21, save = Nexus mod 4259 100%-completion save).** Re-ran with the ≥2³⁰ fix live (log 17:31, after md5 06f2…): counts dropped (Crafting 538→417, Talismans 111→91, GoldenRunes 219→204) but MASSIVE residual remains, and the unset flags are now **<2³⁰** (5-8 digit). Pattern = whole categories at EXACTLY 0 looted on a 100% endgame save (Great Runes 6/6, MP-Fingers 8/8, Memory Stones 6/6 prove endgame): **Greases 0/126, Utilities 0/43, Stat Boosts 0/77, Rune Arcs 0/70, Whetblades 0/5, Larval Tears 0/11, Lost Ashes 0/69, Reusables 0/8, Smithing(Rare) 0/11** → systematically WRONG flag, not "uncollected". **NOT an overlay regression — PROVEN by code:** legacy native census (goblin_inject.cpp:3550) reads `row->textDisableFlagId1` directly; overlay `resolve_loot_flag` falls back to the SAME baked textDisableFlagId1 for multi-item lots (@0x80=0 && item2≠0). These categories = stackable/multi-item → both paths use the baked icon-gate flag, which under ERR DIVERGES from the real obtained flag (RE Q3). Native map shows identical (it just never had a numeric badge to reveal it; icons stay un-hidden on 100% save too). **Hexinton CT checked (user ask): only flag WRITERS (Unlock Graces/Map/Cookbooks via EventFlag_C1/EventFlagUnlocker), NO collected-item detector → no shortcut.** RE truth: ER world-item collected-state = pure event flag (MSB-treasure flag mirrored into textDisableFlagId1); no separate isCollected() fn.
**`[LOOTDIAG]` field-dump diagnostic (commit 5821fcd, config `diag_loot_flags`, default false) RAN → BLOCKER CLOSED, NO BUG.** Log 17:45 dump decisive: **resolution mechanism is CORRECT.** `item2=0` everywhere (single-item lots, NOT multi — earlier hypothesis wrong), `@0x80 == baked` everywhere (NO ERR divergence — also wrong), slots60 all 0. 84 markers read `(SET)` with the EXACT same structure as the unset ones → when an item IS collected the flag reads SET and resolve_loot_flag returns the right flag. The `0/N` categories (greases 10007XXX, whetblades 61XXX, utilities, rune arcs) read unset because **those world-pickup flags simply aren't set in this save.**
**ROOT (user 2026-06-21, CONCLUSIVE): the test save = a VANILLA ER 100% save ported to ERR (only md5 hash + SteamID changed). ERR ships a DIFFERENT `regulation.bin` (remapped ItemLotParam / world conversion), so the vanilla-set pickup flags map to DIFFERENT lots under ERR → cross-regulation MISMATCH. Whole flag groups (10007xxx etc.) read false (matches the RE group-absent mechanism: a group not in the persistent manager → false for the whole group → exactly 0/N), while migrated groups (11007xxx) read SET.** This is INHERENT to running a foreign-regulation save, NOT a mod bug — the census is FAITHFUL to the live flags; the flags just don't correspond to ERR's lots. On a genuine ERR-native playthrough save the census will be accurate. **NO CODE FIX. The ≥2³⁰ exclusion (commit 204b62b) is the only real correctness fix and stays.** Diag left in (gated off; `diag_loot_flags=false` restored in the user INI). orp_flag_set FINE. The proposed in-world "is the item still physically there" test would confirm but is unnecessary given the regulation-mismatch root. See [[overlay-rendered-markers]], [[live-param-vs-baked-data]].
